# Hypergraph v3 acceptance vault

Open this folder as an Obsidian vault at a fixed 1440×900 viewport and DPR 1. It covers coincident simplex/hyperedge kinds, closure deficit, a recurring crystallization source, a simplicial hole, and a contextual obstruction seam. `expected-light.svg` and `expected-dark.svg` are fixed reference captures for automated pixel comparison; the manual matrix is in `docs/V3/hypergraph-verification.md`.
