"use strict";
var Pe = Object.defineProperty;
var xn = Object.getOwnPropertyDescriptor;
var wn = Object.getOwnPropertyNames;
var Cn = Object.prototype.hasOwnProperty;
var Nn = (r, e) => {
    for (var t in e) Pe(r, t, { get: e[t], enumerable: !0 });
  },
  Mn = (r, e, t, n) => {
    if ((e && typeof e == "object") || typeof e == "function")
      for (let i of wn(e))
        !Cn.call(r, i) && i !== t && Pe(r, i, { get: () => e[i], enumerable: !(n = xn(e, i)) || n.enumerable });
    return r;
  };
var En = (r) => Mn(Pe({}, "__esModule", { value: !0 }), r);
var Ki = {};
Nn(Ki, { default: () => Ee });
module.exports = En(Ki);
var M = require("obsidian");
function _(r) {
  let e = 5381;
  for (let t = 0; t < r.length; t++) ((e = ((e << 5) + e) ^ r.charCodeAt(t)), (e = e >>> 0));
  return e;
}
function z(r) {
  let e = ["purple", "teal", "coral", "pink", "blue", "amber"];
  return r ? e[_(r) % e.length] : "purple";
}
function X(r) {
  return r.toLowerCase().trim();
}
function N(r) {
  return [...r].map(X).sort().join("|");
}
var Tn = { simplex: "s", hyperedge: "h" };
function P(r, e) {
  return `${Tn[r]}:${N(e)}`;
}
function k(r) {
  return [...r].sort((e, t) => X(e).localeCompare(X(t)));
}
function j(r) {
  let e = new Set(),
    t = [];
  for (let n of k(r)) {
    let i = X(n);
    e.has(i) || (e.add(i), t.push(n));
  }
  return t;
}
var De = null,
  et = -1,
  Re = 0;
function U() {
  Re++;
}
function kn(r) {
  if (De && et === Re) return De;
  let e = new Map();
  for (let t of r.vault.getMarkdownFiles()) {
    let n = r.metadataCache.getFileCache(t)?.frontmatter?.aliases;
    (Array.isArray(n) ? n : typeof n == "string" ? [n] : []).forEach((s) => {
      let o = X(String(s));
      o && !e.has(o) && e.set(o, t);
    });
  }
  return ((De = e), (et = Re), e);
}
function re(r, e, t) {
  let n = r.trim();
  if (!n) return null;
  let i = t.metadataCache.getFirstLinkpathDest(n, e);
  return i || (kn(t).get(X(n)) ?? null);
}
var ee = "simplicial-view",
  q = "simplicial-panel";
function nt(r) {
  return r.nodes.length - 1;
}
function* Z(r, e) {
  if (e === 0) {
    yield [];
    return;
  }
  for (let t = 0; t <= r.length - e; t++) for (let n of Z(r.slice(t + 1), e - 1)) yield [r[t], ...n];
}
function Fe(r, e) {
  if (nt(e) >= 4) return [];
  let n = k(e.nodes),
    i = [];
  for (let s = 2; s < n.length; s++) for (let o of Z(n, s)) r.simplices.has(N(o)) || i.push(k(o));
  return i;
}
function it(r, e) {
  let t = nt(e);
  if (t > 4) {
    console.warn(`[Simplicial] Simplex dim=${t} exceeds cap. Faces not auto-generated.`);
    return;
  }
  let n = N(e.nodes),
    i = e.colorKey ?? z(e.label);
  for (let s of Fe(r, e))
    r.simplices.set(N(s), { nodes: s, autoGenerated: !0, userDefined: !1, parentKey: n, colorKey: i });
}
function Ae(r, e = 2) {
  let t = [...r.simplices.values()],
    n = [...r.nodes.keys()],
    i = t.filter((p) => p.nodes.length === 2),
    s = t.filter((p) => p.nodes.length === 3),
    o = t.filter((p) => p.nodes.length === 4),
    a = In(n, i),
    l = [];
  e >= 1 && l.push(...Pn(n, i, s));
  let d = [];
  return (e >= 2 && d.push(...Dn(s, o)), { b0: a, b1: l.length, b2: d.length, holes: [...l, ...d] });
}
function In(r, e) {
  if (r.length === 0) return 0;
  let t = new Map();
  for (let s of r) t.set(s, new Set());
  for (let s of e) {
    let [o, a] = s.nodes;
    (t.get(o)?.add(a), t.get(a)?.add(o));
  }
  let n = 0,
    i = new Set();
  for (let s of r) {
    if (i.has(s)) continue;
    n++;
    let o = [s];
    for (; o.length > 0; ) {
      let a = o.pop();
      if (!i.has(a)) {
        i.add(a);
        for (let l of t.get(a) ?? []) i.has(l) || o.push(l);
      }
    }
  }
  return n;
}
function Pn(r, e, t) {
  let n = [],
    i = new Set();
  for (let a of e) {
    let [l, d] = a.nodes.sort();
    i.add(`${l}|${d}`);
  }
  let s = new Set();
  for (let a of t) s.add(a.nodes.sort().join("|"));
  let o = new Map();
  for (let a of r) o.set(a, new Set());
  for (let a of e) {
    let [l, d] = a.nodes;
    (o.get(l)?.add(d), o.get(d)?.add(l));
  }
  for (let a = 0; a < r.length; a++) {
    let l = r[a],
      d = [...(o.get(l) ?? [])];
    for (let p = 0; p < d.length; p++)
      for (let c = p + 1; c < d.length; c++) {
        let u = d[p],
          h = d[c];
        if (!o.get(u)?.has(h)) continue;
        let f = [l, u, h].sort(),
          m = f.join("|");
        s.has(m) || n.push({ dimension: 1, boundaryNodes: f, missingSimplex: f });
      }
  }
  return n;
}
function Dn(r, e) {
  let t = [],
    n = new Set(),
    i = new Map();
  for (let l of r) {
    let d = l.nodes.sort(),
      p = d.join("|");
    (n.add(p), i.set(p, d));
  }
  let s = new Set();
  for (let l of e) s.add(l.nodes.sort().join("|"));
  let o = new Map();
  for (let [l, d] of i)
    for (let p = 0; p < 3; p++)
      for (let c = p + 1; c < 3; c++) {
        let u = [d[p], d[c]].sort().join("|");
        (o.has(u) || o.set(u, new Set()), o.get(u).add(l));
      }
  let a = new Map();
  for (let [, l] of o) {
    if (l.size < 2) continue;
    let d = [...l];
    for (let p = 0; p < d.length; p++)
      for (let c = p + 1; c < d.length; c++) {
        let u = i.get(d[p]),
          h = i.get(d[c]),
          g = new Set(u),
          f = new Set(h),
          m = [...g].filter((w) => f.has(w));
        if (m.length !== 2) continue;
        let v = [...g].filter((w) => !f.has(w))[0],
          y = [...f].filter((w) => !g.has(w))[0];
        if (!v || !y) continue;
        let b = [v, y, ...m].sort(),
          S = b.join("|");
        a.has(S) || a.set(S, new Set(b));
      }
  }
  for (let [l, d] of a) {
    if (s.has(l)) continue;
    let p = [...d].sort();
    if (p.length !== 4) continue;
    let c = 0;
    for (let u = 0; u < 4; u++) {
      let h = p.filter((g, f) => f !== u).sort();
      n.has(h.join("|")) && c++;
    }
    c === 4 && t.push({ dimension: 2, boundaryNodes: p, missingSimplex: p });
  }
  return t;
}
function Rn(r) {
  if (!r) return "";
  try {
    return ` ${JSON.stringify(r)}`;
  } catch {
    return " [unserializable-details]";
  }
}
function oe(r, e, t, n) {
  let i = `[Simplicial:${e}] ${t}${Rn(n)}`;
  if (r === "error") {
    console.error(i);
    return;
  }
  if (r === "warn") {
    console.warn(i);
    return;
  }
  if (r === "debug") {
    console.debug(i);
    return;
  }
  console.debug(i);
}
var C = {
  debug(r, e, t) {
    oe("debug", r, e, t);
  },
  info(r, e, t) {
    oe("info", r, e, t);
  },
  warn(r, e, t) {
    oe("warn", r, e, t);
  },
  error(r, e, t) {
    oe("error", r, e, t);
  },
};
function ae(r, e) {
  return r + Math.random() * (e - r);
}
function Fn(r, e, t) {
  let n = r / 2,
    i = e / 2,
    s = Math.max(t + 40, n - t),
    o = Math.max(t + 40, i - t);
  switch (Math.floor(Math.random() * 4)) {
    case 0:
      return { px: ae(-s, s), py: -o };
    case 1:
      return { px: s, py: ae(-o, o) };
    case 2:
      return { px: ae(-s, s), py: o };
    default:
      return { px: -s, py: ae(-o, o) };
  }
}
function An(r, e, t = !1) {
  let n = e?.width ?? 960,
    i = e?.height ?? 640,
    { px: s, py: o } = Fn(n, i, 80);
  return { id: r, px: s, py: o, vx: 0, vy: 0, isVirtual: t, isPinned: !1, displayAlpha: 1 };
}
var de = class {
  constructor() {
    this.nodes = new Map();
    this.simplices = new Map();
    this.hyperedges = new Map();
    this.listeners = new Set();
    this.batchDepth = 0;
    this.hasPendingEmit = !1;
    this._analysisCache = null;
    this._analysisDirty = !0;
    this._crossLayerCache = null;
  }
  subscribe(e) {
    return (this.listeners.add(e), () => this.listeners.delete(e));
  }
  emitChange() {
    if (this.batchDepth > 0) {
      this.hasPendingEmit = !0;
      return;
    }
    this.flushChange();
  }
  flushChange() {
    this.listeners.forEach((e) => e());
  }
  batch(e) {
    this.batchDepth++;
    try {
      return e();
    } finally {
      (this.batchDepth--,
        this.batchDepth === 0 && this.hasPendingEmit && ((this.hasPendingEmit = !1), this.flushChange()));
    }
  }
  setNode(e, t) {
    let n = this.nodes.get(e);
    if (n) {
      (Object.assign(n, t ?? {}), t?.isVirtual === !1 && (n.isVirtual = !1), this.emitChange());
      return;
    }
    let i = An(e, void 0, t?.isVirtual ?? !1);
    (t?.px !== void 0 && (i.px = t.px),
      t?.py !== void 0 && (i.py = t.py),
      t?.isPinned !== void 0 && (i.isPinned = t.isPinned),
      this.nodes.set(e, i),
      this.invalidateAnalysisCache(),
      this.emitChange());
  }
  removeNode(e) {
    this.nodes.delete(e);
    for (let [t, n] of [...this.simplices]) n.nodes.includes(e) && this.simplices.delete(t);
    for (let [t, n] of [...this.hyperedges]) n.nodes.includes(e) && this.hyperedges.delete(t);
    (this.invalidateAnalysisCache(), this.emitChange());
  }
  updateNodeId(e, t) {
    if (e === t) return;
    let n = this.nodes.get(e);
    n && (this.nodes.set(t, { ...n, id: t }), this.nodes.delete(e));
    for (let [i, s] of [...this.simplices]) {
      if (!s.nodes.includes(e)) continue;
      let o = { ...s, nodes: k(s.nodes.map((a) => (a === e ? t : a))) };
      (this.simplices.delete(i), this.simplices.set(N(o.nodes), o));
    }
    for (let [i, s] of [...this.hyperedges]) {
      if (!s.nodes.includes(e)) continue;
      let o = { ...s, nodes: k(s.nodes.map((a) => (a === e ? t : a))) };
      (this.hyperedges.delete(i), this.hyperedges.set(P("hyperedge", o.nodes), o));
    }
    (this.invalidateAnalysisCache(), this.emitChange());
  }
  addSimplex(e) {
    if (e.kind === "hyperedge")
      return (C.error("model", "Refused to add a hyperedge through addSimplex", { nodes: e.nodes }), "");
    let t = j(e.nodes);
    if (t.length < 2) return "";
    t.forEach((s) => {
      this.nodes.has(s) || this.setNode(s, { isVirtual: !1 });
    });
    let n = {
        ...e,
        nodes: k(t),
        weight: le(e.weight),
        autoGenerated: e.autoGenerated ?? !1,
        userDefined: e.userDefined ?? !e.autoGenerated,
        colorKey: e.autoGenerated ? (e.colorKey ?? "neutral") : (e.colorKey ?? z(e.label)),
      },
      i = N(n.nodes);
    return (this.simplices.set(i, n), it(this, n), this.invalidateAnalysisCache(), this.emitChange(), i);
  }
  removeSimplex(e) {
    if (this.simplices.delete(e)) {
      for (let [t, n] of [...this.simplices]) {
        if (!n.autoGenerated) continue;
        [...this.simplices.entries()].some(([s, o]) =>
          s === t || o.autoGenerated ? !1 : n.nodes.every((a) => o.nodes.includes(a)),
        ) || this.simplices.delete(t);
      }
      (this.invalidateAnalysisCache(), this.emitChange());
    }
  }
  addHyperedge(e) {
    let t = j(e.nodes);
    if (t.length < 2) return "";
    t.forEach((a) => {
      this.nodes.has(a) || this.setNode(a, { isVirtual: !1 });
    });
    let n = k(t),
      i = P("hyperedge", n),
      s = this.hyperedges.get(i),
      o = {
        ...s,
        ...e,
        nodes: n,
        weight: le(e.weight ?? s?.weight),
        colorKey: e.colorKey ?? s?.colorKey ?? z(e.label),
        occurredAt: s?.occurredAt ?? e.occurredAt ?? Date.now(),
      };
    return (this.hyperedges.set(i, o), this.invalidateAnalysisCache(), this.emitChange(), i);
  }
  removeHyperedge(e) {
    return this.hyperedges.delete(e) ? (this.invalidateAnalysisCache(), this.emitChange(), !0) : !1;
  }
  getHyperedge(e) {
    return this.hyperedges.get(e);
  }
  getHyperedgesForNode(e) {
    return [...this.hyperedges.values()].filter((t) => t.nodes.includes(e));
  }
  updateHyperedge(e, t) {
    let n = this.hyperedges.get(e);
    if (!n) return;
    let i = { ...n, ...t, nodes: n.nodes };
    return (
      t.label !== void 0 && (i.colorKey = z(t.label)),
      t.weight !== void 0 && (i.weight = le(t.weight)),
      this.hyperedges.set(e, i),
      this.invalidateAnalysisCache(),
      this.emitChange(),
      i
    );
  }
  facesImpliedByPromotion(e) {
    let t = this.hyperedges.get(e);
    return t ? Fe(this, { nodes: t.nodes }) : [];
  }
  promoteToSimplex(e, t = {}) {
    let n = this.hyperedges.get(e);
    if (!n) return null;
    let i = this.facesImpliedByPromotion(e),
      s = "";
    return (
      this.batch(() => {
        ((s = this.addSimplex({
          nodes: n.nodes,
          label: n.label,
          weight: n.weight,
          sourcePath: n.sourcePath,
          userDefined: !0,
          autoGenerated: !1,
        })),
          t.retainEncounter === !1 ? this.hyperedges.delete(e) : this.hyperedges.set(e, { ...n, promotedTo: s }),
          this.invalidateAnalysisCache(),
          this.emitChange());
      }),
      s ? { simplexKey: s, createdFaces: i } : null
    );
  }
  relaxToHyperedge(e) {
    let t = this.simplices.get(e);
    if (!t || t.autoGenerated) return null;
    let n = P("hyperedge", t.nodes),
      i = this.hyperedges.get(n),
      s = "";
    return (
      this.batch(() => {
        ((s = this.addHyperedge({
          ...i,
          nodes: t.nodes,
          label: i?.label ?? t.label,
          weight: i?.weight ?? t.weight,
          sourcePath: i?.sourcePath ?? t.sourcePath,
          occurredAt: i?.occurredAt,
          promotedTo: void 0,
        })),
          this.removeSimplex(e));
      }),
      s || null
    );
  }
  crystallizeHyperedge(e, t) {
    let n = this.hyperedges.get(e);
    return n
      ? (this.batch(() => {
          (this.nodes.has(t) || this.setNode(t, { isVirtual: !1 }),
            this.hyperedges.set(e, { ...n, crystallizedInto: t }),
            this.invalidateAnalysisCache(),
            this.emitChange());
        }),
        !0)
      : !1;
  }
  getAllRelations() {
    let e = [];
    return (
      this.simplices.forEach((t, n) => {
        e.push({ key: P("simplex", t.nodes), relation: { kind: "simplex", ...t } });
      }),
      this.hyperedges.forEach((t, n) => {
        e.push({ key: n, relation: { kind: "hyperedge", ...t } });
      }),
      e
    );
  }
  replaceInferredSimplices(e) {
    this.batch(() => {
      for (let [t, n] of [...this.simplices]) n.inferred && this.simplices.delete(t);
      (e.forEach((t) => {
        (t.nodes.forEach((n) => {
          this.nodes.has(n) || this.setNode(n, { isVirtual: !1 });
        }),
          this.addSimplex(t));
      }),
        this.invalidateAnalysisCache(),
        this.emitChange());
    });
  }
  replaceSourceRelations(e, t, n) {
    this.batch(() => {
      for (let [i, s] of [...this.simplices]) s.sourcePath === e && !s.autoGenerated && this.simplices.delete(i);
      for (let [i, s] of [...this.hyperedges]) s.sourcePath === e && this.hyperedges.delete(i);
      (t.forEach((i) => {
        (i.nodes.forEach((s) => {
          this.nodes.has(s) || this.setNode(s, { isVirtual: !0 });
        }),
          this.addSimplex({ ...i, sourcePath: e }));
      }),
        n.forEach((i) => {
          (i.nodes.forEach((s) => {
            this.nodes.has(s) || this.setNode(s, { isVirtual: !0 });
          }),
            this.addHyperedge({ ...i, sourcePath: e }));
        }),
        this.invalidateAnalysisCache(),
        this.emitChange());
    });
  }
  replaceSourceSimplices(e, t) {
    this.replaceSourceRelations(e, t, []);
  }
  updateMetadata(e, t) {
    let n = this.simplices.get(e);
    if (!n) return;
    let i = { ...n, ...t };
    (t.label !== void 0 && (i.colorKey = n.autoGenerated ? "neutral" : z(t.label)),
      t.weight !== void 0 && (i.weight = le(t.weight)),
      this.simplices.set(e, i),
      this.invalidateAnalysisCache(),
      this.emitChange());
  }
  setPinnedState(e, t, n, i) {
    let s = this.nodes.get(e);
    s && ((s.isPinned = t), n !== void 0 && (s.px = n), i !== void 0 && (s.py = i), this.emitChange());
  }
  getSimplicesForNode(e) {
    return [...this.simplices.values()].filter((t) => t.nodes.includes(e));
  }
  getNeighbors(e) {
    let t = new Set();
    return (
      this.getSimplicesForNode(e).forEach((n) => {
        n.nodes.forEach((i) => {
          i !== e && t.add(i);
        });
      }),
      [...t]
    );
  }
  getSimplicesByDim(e) {
    return [...this.simplices.values()].filter((t) => t.nodes.length - 1 === e);
  }
  getAllNodes() {
    return [...this.nodes.values()];
  }
  getSimplex(e) {
    return this.simplices.get(e);
  }
  invalidateAnalysisCache() {
    ((this._analysisDirty = !0), (this._crossLayerCache = null));
  }
  readCrossLayerCache() {
    return this._crossLayerCache;
  }
  writeCrossLayerCache(e) {
    return ((this._crossLayerCache = e), e);
  }
  getAnalysisSummary() {
    return !this._analysisDirty && this._analysisCache
      ? this._analysisCache
      : ((this._analysisCache = this.computeAnalysisSummary()), (this._analysisDirty = !1), this._analysisCache);
  }
  getCachedBetti() {
    return !this._analysisDirty && this._analysisCache
      ? (this._analysisCache.betti ?? { b0: 0, b1: 0, b2: 0, holes: [] })
      : Ae(this, 2);
  }
  computeAnalysisSummary() {
    let e = [...this.simplices.values()],
      t = e.filter((m) => m.nodes.length === 2),
      n = new Map(),
      i = new Map();
    (this.nodes.forEach((m, v) => n.set(v, new Set())),
      this.nodes.forEach((m, v) => i.set(v, 0)),
      t.forEach((m) => {
        let [v, y] = m.nodes;
        (n.get(v)?.add(y), n.get(y)?.add(v));
      }),
      e.forEach((m) => {
        m.nodes.forEach((v) => {
          i.set(v, (i.get(v) ?? 0) + 1);
        });
      }));
    let s = 0,
      o = new Set();
    n.forEach((m, v) => {
      if (o.has(v)) return;
      s++;
      let y = [v];
      for (; y.length > 0; ) {
        let b = y.pop();
        o.has(b) ||
          (o.add(b),
          n.get(b)?.forEach((S) => {
            o.has(S) || y.push(S);
          }));
      }
    });
    let a = null,
      l = -1,
      d = 0;
    n.forEach((m, v) => {
      let y = m.size;
      ((d += y), y > l && ((l = y), (a = v)));
    });
    let p = null,
      c = -1,
      u = 0;
    i.forEach((m, v) => {
      ((u += m), m > c && ((c = m), (p = v)));
    });
    let h = Ae(this, 2),
      g = h.holes.length,
      f = [...this.hyperedges.values()];
    return {
      nodeCount: this.nodes.size,
      simplexCount: e.length,
      hyperedgeCount: f.length,
      recurringEncounterCount: f.filter((m) => m.persistence === "recurring").length,
      edgeCount: t.length,
      clusterCount: e.filter((m) => m.nodes.length === 3).length,
      coreCount: e.filter((m) => m.nodes.length >= 4).length,
      inferredCount: e.filter((m) => m.inferred).length,
      suggestedCount: e.filter((m) => m.suggested).length,
      connectedComponents: s,
      averageDegree: this.nodes.size ? Number((d / this.nodes.size).toFixed(2)) : 0,
      maxDegreeNodeId: a,
      maxDegree: Math.max(0, l),
      maxSimplexCentralityNodeId: p,
      maxSimplexCentrality: Math.max(0, c),
      averageSimplexCentrality: this.nodes.size ? Number((u / this.nodes.size).toFixed(2)) : 0,
      betti: h,
      holeCount: g,
    };
  }
};
function le(r) {
  return r === void 0 || Number.isNaN(r) ? 1 : Math.max(0.1, Math.min(1, r));
}
var ce = class {
  constructor() {
    this.events = [];
    this.listeners = new Set();
  }
  onAppend(e) {
    return (this.listeners.add(e), () => this.listeners.delete(e));
  }
  append(e) {
    let t = [...e.nodes],
      n = Object.freeze({
        timestamp: e.timestamp ?? Date.now(),
        type: e.type,
        kind: e.kind,
        relationKey: P(e.kind, t),
        nodeKey: N(t),
        nodes: Object.freeze(t),
        actor: e.actor,
        ...(e.prior ? { prior: Object.freeze({ ...e.prior }) } : {}),
        ...(e.detail ? { detail: Object.freeze({ ...e.detail }) } : {}),
      });
    return (this.events.push(n), this.listeners.forEach((i) => i(n)), n);
  }
  all() {
    return [...this.events];
  }
  get size() {
    return this.events.length;
  }
  forRelation(e) {
    return this.events.filter((t) => t.relationKey === e);
  }
  forNodes(e) {
    let t = N(e);
    return this.events.filter((n) => n.nodeKey === t);
  }
  occurrencesOf(e) {
    return this.forNodes(e)
      .filter((t) => t.type === "encountered" || t.type === "recurred")
      .map((t) => t.timestamp)
      .sort((t, n) => t - n);
  }
  isRecurring(e, t) {
    return this.occurrencesOf(e).length >= Math.max(2, t);
  }
  load(e) {
    (e.forEach((t) => this.events.push(Object.freeze({ ...t, nodes: Object.freeze([...t.nodes]) }))),
      this.events.sort((t, n) => t.timestamp - n.timestamp));
  }
};
function st(r, e, t) {
  let n = 0;
  return (
    [...r.hyperedges].forEach(([i, s]) => {
      let o = e.occurrencesOf(s.nodes),
        a = e.isRecurring(s.nodes, t) ? "recurring" : "momentary";
      ((s.occurrences?.length ?? 0) === o.length && s.persistence === a) ||
        (r.updateHyperedge(i, { occurrences: o, persistence: a }), n++);
    }),
    n
  );
}
var Hn = new Set(["encountered", "recurred", "created", "promoted", "relaxed", "crystallized", "dissolved"]);
function rt(r) {
  return JSON.stringify({
    t: r.timestamp,
    e: r.type,
    k: r.kind,
    n: r.nodes,
    a: r.actor,
    ...(r.prior ? { p: r.prior } : {}),
    ...(r.detail ? { d: r.detail } : {}),
  });
}
function ot(r) {
  let e = r.trim();
  if (!e.startsWith("{")) return null;
  let t;
  try {
    t = JSON.parse(e);
  } catch {
    return null;
  }
  let n = String(t.e ?? ""),
    i = t.k === "hyperedge" ? "hyperedge" : "simplex",
    s = Array.isArray(t.n) ? t.n.map(String) : [],
    o = typeof t.t == "number" ? t.t : NaN;
  return !Hn.has(n) || s.length === 0 || Number.isNaN(o)
    ? null
    : Object.freeze({
        timestamp: o,
        type: n,
        kind: i,
        relationKey: P(i, s),
        nodeKey: N(s),
        nodes: Object.freeze(s),
        actor: t.a === "user" || t.a === "inference" || t.a === "parser" ? t.a : "user",
        ...(t.p && typeof t.p == "object" ? { prior: Object.freeze(t.p) } : {}),
        ...(t.d && typeof t.d == "object" ? { detail: Object.freeze(t.d) } : {}),
      });
}
var Ln = { k: 8, minClusterSize: 3, maxIterations: 100 };
function He(r, e = {}) {
  let t = { ...Ln, ...e };
  if (r.length < t.minClusterSize) return new Map(r.map((o) => [o.path, "default"]));
  let n = Math.min(t.k, Math.floor(r.length / t.minClusterSize));
  if (n < 2) return new Map(r.map((o) => [o.path, "default"]));
  let i = Le(r),
    s = On(i, n, t.maxIterations);
  return new Map(s.map((o, a) => [i[a].path, `cluster-${o}`]));
}
function Le(r) {
  let e = r.length,
    t = new Map(),
    n = [];
  for (let s of r) {
    let o = new Map();
    for (let a of s.titleTokens) o.set(a, (o.get(a) ?? 0) + 3);
    for (let a of s.contentTokens) o.set(a, (o.get(a) ?? 0) + 1);
    for (let a of s.tags) {
      let l = a.replace(/^#/, "");
      o.set(l, (o.get(l) ?? 0) + 5);
    }
    for (let a of o.keys()) t.set(a, (t.get(a) ?? 0) + 1);
    n.push(o);
  }
  let i = new Map();
  for (let [s, o] of t) i.set(s, Math.log(e / (o + 1)) + 1);
  return r.map((s, o) => {
    let a = n[o],
      l = new Map(),
      d = 0;
    for (let [p, c] of a) {
      let u = c * (i.get(p) ?? 1);
      (l.set(p, u), (d += u * u));
    }
    return { path: s.path, vector: l, magnitude: Math.sqrt(d) };
  });
}
function Oe(r, e) {
  if (r.magnitude === 0 || e.magnitude === 0) return 0;
  let t = 0;
  for (let [n, i] of r.vector) {
    let s = e.vector.get(n);
    s !== void 0 && (t += i * s);
  }
  return t / (r.magnitude * e.magnitude);
}
function On(r, e, t) {
  let n = r.length,
    i = new Array(n).fill(0).map(() => Math.floor(Math.random() * e)),
    s = [];
  for (let o = 0; o < t; o++) {
    for (let l = 0; l < e; l++) {
      let d = r.filter((u, h) => i[h] === l);
      if (d.length === 0) {
        s[l] = new Map(r[Math.floor(Math.random() * n)].vector);
        continue;
      }
      let p = new Map(),
        c = new Map();
      for (let u of d) for (let [h, g] of u.vector) (c.set(h, (c.get(h) ?? 0) + 1), p.set(h, (p.get(h) ?? 0) + g));
      for (let [u, h] of p) p.set(u, h / d.length);
      s[l] = p;
    }
    let a = !1;
    for (let l = 0; l < n; l++) {
      let d = r[l],
        p = 0,
        c = -1;
      for (let u = 0; u < e; u++) {
        let h = {
            path: `centroid-${u}`,
            vector: s[u],
            magnitude: Math.sqrt([...s[u].values()].reduce((f, m) => f + m * m, 0)),
          },
          g = Oe(d, h);
        g > c && ((c = g), (p = u));
      }
      i[l] !== p && ((i[l] = p), (a = !0));
    }
    if (!a) break;
  }
  return i;
}
function at(r, e) {
  let t = new Map();
  for (let n of r) {
    let i = e.get(n.path);
    !i || i === "default" ? t.set(n.path, n.topFolder || n.folder || "misc") : t.set(n.path, i);
  }
  return t;
}
function lt(r, e) {
  return [r, e].sort().join("|");
}
function Kn(r, e) {
  let t = new Set(r);
  return e.filter((n) => t.has(n));
}
function $n(r, e, t, n, i = 0) {
  let s = 0,
    o = t.get(r.id),
    a = t.get(e.id),
    l = o?.outgoingLinks.has(e.id) ?? !1,
    d = a?.outgoingLinks.has(r.id) ?? !1;
  l && d ? (s += 0.8) : (l || d) && (s += 0.5);
  let p = Kn(r.tags, e.tags),
    c = [...t.values()].flatMap((y) => [...y.tags]),
    u = new Map();
  for (let y of c) u.set(y, (u.get(y) ?? 0) + 1);
  let h = t.size,
    g = new Set(),
    f = new Set();
  for (let [y, b] of u.entries()) b / h < n.tagRarityThreshold ? g.add(y) : f.add(y);
  let m = p.filter((y) => g.has(y)),
    v = p.filter((y) => f.has(y));
  return (
    (s += m.length * 0.15),
    (s -= v.length * 0.1),
    r.domain && e.domain && r.domain === e.domain && (s += 0.15),
    (s += i * 0.35),
    Math.max(0, Math.min(1, s))
  );
}
function te(r, e) {
  let t = new Map(),
    n = new Map(r.map((u) => [u.path, u])),
    i;
  if (e.domainSource === "content-cluster") i = He(r, { k: e.contentClusterCount });
  else if (e.domainSource === "hybrid") {
    let u = He(r, { k: e.contentClusterCount });
    i = at(r, u);
  } else i = new Map(r.map((u) => [u.path, u.topFolder || u.folder || ""]));
  for (let u of r)
    t.set(u.path, {
      id: u.path,
      role: u.role,
      domain: i.get(u.path) || u.topFolder || u.folder || "",
      tags: [...u.tags],
      modifiedAt: u.modifiedAt,
      linkCount: u.outgoingLinks.size,
    });
  let s = new Map(),
    o = [...t.values()],
    a = Le(r),
    l = new Map(a.map((u) => [u.path, u]));
  for (let u = 0; u < o.length; u++)
    for (let h = u + 1; h < o.length; h++) {
      let g = o[u],
        f = o[h],
        m = l.get(g.id),
        v = l.get(f.id),
        y = m && v ? Oe(m, v) : 0,
        b = $n(g, f, n, e, y),
        S = lt(g.id, f.id);
      s.set(S, { a: g.id, b: f.id, strength: b });
    }
  let d = new Map();
  for (let u of s.values()) u.strength > 0 && (d.set(u.a, (d.get(u.a) ?? 0) + 1), d.set(u.b, (d.get(u.b) ?? 0) + 1));
  let c = [...d.values()].reduce((u, h) => u + h, 0) / Math.max(d.size, 1);
  for (let [u, h] of s) {
    let g = d.get(h.a) ?? 1,
      f = d.get(h.b) ?? 1,
      v = 0.5 + 0.5 * Math.min(1, (c * c) / (g * f));
    s.set(u, { ...h, strength: Math.min(1, h.strength * v) });
  }
  return { nodes: t, edges: s };
}
function F(r, e, t) {
  let n = lt(r, e);
  return t.edges.get(n)?.strength ?? 0;
}
function K(r, e, t) {
  let n = [];
  for (let i of t.edges.values()) i.strength < e || (i.a === r ? n.push(i.b) : i.b === r && n.push(i.a));
  return n;
}
function dt(r, e) {
  let t = te(r, e);
  return (n) => Wn(n, t);
}
function Wn(r, e) {
  if (r.length < 2) return 0;
  let t = 0,
    n = 0;
  for (let i = 0; i < r.length; i++) for (let s = i + 1; s < r.length; s++) ((t += F(r[i], r[s], e)), n++);
  return n > 0 ? t / n : 0;
}
function Ke() {
  return { events: [], nodeScores: new Map(), simplexScores: new Map() };
}
function W(r, e) {
  let t = { ...e, timestamp: Date.now() };
  r.events.push(t);
  let n = Bn(e.type);
  if (e.simplexKey && e.type !== "hover") {
    let i = r.simplexScores.get(e.simplexKey) ?? 1;
    r.simplexScores.set(e.simplexKey, Math.min(3, i + n));
  }
  if (e.nodeIds)
    for (let i of e.nodeIds) {
      let s = r.nodeScores.get(i) ?? 1;
      r.nodeScores.set(i, Math.min(3, s + n * 0.5));
    }
  _n(r);
}
function Bn(r) {
  switch (r) {
    case "confirm":
      return 0.5;
    case "promote":
      return 0.8;
    case "create":
      return 0.6;
    case "reject":
      return -0.3;
    case "dissolve":
      return -0.5;
    case "select":
      return 0.1;
    case "hover":
      return 0.02;
    default:
      return 0;
  }
}
function _n(r) {
  let e = Date.now() - 2592e6;
  r.events = r.events.filter((t) => t.timestamp > e);
}
function ct(r, e, t) {
  let n = 1;
  if (e) {
    let i = r.simplexScores.get(e) ?? 1;
    n *= i;
  }
  if (t && t.length > 0) {
    let i = t.reduce((s, o) => s + (r.nodeScores.get(o) ?? 1), 0) / t.length;
    n *= 1 + (i - 1) * 0.3;
  }
  return Math.min(3, Math.max(0.3, n));
}
function ht(r) {
  return {
    events: r.events.slice(-100),
    nodeScores: Array.from(r.nodeScores.entries()),
    simplexScores: Array.from(r.simplexScores.entries()),
  };
}
function ut(r) {
  let e = Ke();
  if (typeof r != "object" || r === null) return e;
  let t = r;
  if (
    (Array.isArray(t.events) &&
      (e.events = t.events.filter((n) => {
        if (typeof n != "object" || n === null) return !1;
        let i = n;
        return typeof i.timestamp == "number" && typeof i.type == "string";
      })),
    Array.isArray(t.nodeScores))
  )
    for (let [n, i] of t.nodeScores) typeof i == "number" && e.nodeScores.set(n, i);
  if (Array.isArray(t.simplexScores))
    for (let [n, i] of t.simplexScores) typeof i == "number" && e.simplexScores.set(n, i);
  return e;
}
var V = require("obsidian");
function pt(r, e, t) {
  return `---
${t.stringify(r).trimEnd()}
---
${e.replace(/^\n*/, "")}`;
}
function gt(r, e) {
  let t = r.match(/^---\n([\s\S]*?)\n---\n?/);
  if (!t) return { frontmatter: {}, body: r };
  let n = r.replace(/^---\n[\s\S]*?\n---\n?/, "");
  try {
    return { frontmatter: e.parse(t[1]) ?? {}, body: n };
  } catch {
    return { frontmatter: {}, body: n };
  }
}
function $e(r, e, t, n) {
  let i = Object.prototype.hasOwnProperty.call(r, e),
    o = (Array.isArray(r[e]) ? [...r[e]] : []).filter((a) => {
      let l = a && typeof a == "object" && Array.isArray(a.nodes) ? a.nodes.map(String) : [];
      return N(l) !== t;
    });
  return (n && o.push(n), (o.length > 0 || i) && (r[e] = o), r);
}
var mt = { parse: (r) => (0, V.parseYaml)(r) ?? null, stringify: (r) => (0, V.stringifyYaml)(r) };
function J(r, e) {
  return pt(r, e, mt);
}
function Q(r) {
  return gt(r, mt);
}
function ft(r) {
  return {
    nodes: r.nodes,
    ...(r.label ? { label: r.label } : {}),
    ...(r.weight !== void 0 ? { weight: r.weight } : {}),
  };
}
function yt(r) {
  return {
    nodes: r.nodes,
    ...(r.label ? { label: r.label } : {}),
    ...(r.weight !== void 0 ? { weight: r.weight } : {}),
    ...(r.mode ? { mode: r.mode } : {}),
    ...(r.occurredAt !== void 0 ? { occurredAt: r.occurredAt } : {}),
    ...(r.persistence ? { persistence: r.persistence } : {}),
  };
}
function We(r, e, t) {
  return $e(r, "simplices", e, t);
}
function Be(r, e, t) {
  return $e(r, "hyperedges", e, t);
}
async function _e(r, e, t) {
  let n = await r.vault.read(e),
    { frontmatter: i, body: s } = Q(n),
    o = N(t.nodes);
  We(i, o, ft(t));
  let a = Array.isArray(i.simplices) ? i.simplices.length : 0;
  return (
    C.info("persistence", "Prepared source-note write", {
      mode: "source-note",
      file: e.path,
      simplexKey: o,
      simplexCount: a,
    }),
    J(i, s)
  );
}
async function vt(r, e, t) {
  let n = await r.vault.read(e),
    { frontmatter: i, body: s } = Q(n),
    o = N(t.nodes);
  return (
    Be(i, o, yt(t)),
    C.info("persistence", "Prepared source-note hyperedge write", {
      mode: "source-note",
      file: e.path,
      nodeKey: o,
      hyperedgeCount: Array.isArray(i.hyperedges) ? i.hyperedges.length : 0,
    }),
    J(i, s)
  );
}
async function bt(r, e, t) {
  let n = await B(r, e),
    i = await r.vault.read(n),
    { frontmatter: s, body: o } = Q(i),
    a = N(t.nodes);
  ((s.managedBy = "simplicial-complex"), Be(s, a, yt(t)));
  let l = J(
    s,
    o ||
      `<!-- managed by Simplicial Complex plugin -->
`,
  );
  return (
    C.info("persistence", "Prepared central-file hyperedge write", {
      mode: "central-file",
      file: n.path,
      nodeKey: a,
      hyperedgeCount: Array.isArray(s.hyperedges) ? s.hyperedges.length : 0,
    }),
    { file: n, content: l }
  );
}
async function St(r, e, t) {
  let n = await r.vault.read(e),
    { frontmatter: i, body: s } = Q(n);
  return (
    Be(i, t),
    C.info("persistence", "Prepared hyperedge removal", {
      file: e.path,
      nodeKey: t,
      remainingHyperedgeCount: Array.isArray(i.hyperedges) ? i.hyperedges.length : 0,
    }),
    J(i, s)
  );
}
async function B(r, e) {
  let t = r.vault.getAbstractFileByPath(e);
  if (t instanceof V.TFile) return t;
  let n = [
      "---",
      "managedBy: simplicial-complex",
      "simplices: []",
      "hyperedges: []",
      "---",
      "",
      "<!-- managed by Simplicial Complex plugin -->",
      "",
    ].join(`
`),
    i = await r.vault.create(e, n);
  return (C.info("persistence", "Created central file", { path: e }), i);
}
async function xt(r, e, t) {
  let n = await B(r, e),
    i = await r.vault.read(n),
    { frontmatter: s, body: o } = Q(i),
    a = N(t.nodes);
  ((s.managedBy = "simplicial-complex"), We(s, a, ft(t)));
  let l = Array.isArray(s.simplices) ? s.simplices.length : 0,
    d = J(
      s,
      o ||
        `<!-- managed by Simplicial Complex plugin -->
`,
    );
  return (
    C.info("persistence", "Prepared central-file write", {
      mode: "central-file",
      file: n.path,
      simplexKey: a,
      simplexCount: l,
    }),
    { file: n, content: d }
  );
}
async function ne(r, e, t) {
  let n = await r.vault.read(e),
    { frontmatter: i, body: s } = Q(n);
  return (
    We(i, t),
    C.info("persistence", "Prepared simplex removal", {
      file: e.path,
      simplexKey: t,
      remainingSimplexCount: Array.isArray(i.simplices) ? i.simplices.length : 0,
    }),
    J(i, s)
  );
}
async function wt(r, e) {
  let t = r.vault.getAbstractFileByPath(e);
  if (!(t instanceof V.TFile))
    return (
      C.warn("persistence", "Central file does not exist", { mode: "central-file", path: e }),
      { exists: !1, path: e, length: 0 }
    );
  let n = await r.vault.read(t);
  return (
    C.info("persistence", "Central file state", { mode: "central-file", path: e, exists: !0, length: n.length }),
    { exists: !0, path: t.path, length: n.length }
  );
}
function Ct() {
  return {
    domainSource: "hybrid",
    contentClusterCount: 8,
    enableBettiComputation: !1,
    bettiDisplayOnCanvas: !0,
    maxBettiDim: 2,
    showFiltrationSlider: !0,
    enableExplanationPanel: !0,
    enableInteractionReinforcement: !0,
    reinforcementStrength: 0.05,
    persistenceMode: "source-note",
    centralFile: "_simplicial.md",
    showEdges: !0,
    showClusters: !0,
    showCores: !0,
    maxRenderedDim: 12,
    noiseAmount: 0.12,
    sleepThreshold: 0.01,
    repulsionStrength: 2400,
    cohesionStrength: 0.005,
    gravityStrength: 7e-4,
    dampingFactor: 0.84,
    boundaryPadding: 50,
    darkMode: "auto",
    inferenceMode: "hybrid",
    insightThreshold: 0.45,
    linkStrengthThreshold: 0.4,
    linkThresholdLowerBound: 0,
    linkThresholdUpperBound: 1,
    closureThreshold: 0.25,
    tagRarityThreshold: 0.05,
    decayHalfLifeDays: 90,
    decayMinimumWeight: 0.1,
    minDomainsForTriangle: 2,
    minDomainsForTetra: 2,
    minRolesForTetra: 2,
    roleDiversityWeight: 0.2,
    domainDiversityWeight: 0.25,
    actionBonus: 0.3,
    rareTagWeight: 0.15,
    commonTagPenalty: 0.12,
    linkGraphBaseline: !0,
    enableInferredEdges: !0,
    inferenceThreshold: 0.12,
    enableLinkInference: !0,
    enableMutualLinkBonus: !0,
    enableSharedTags: !0,
    enableTitleOverlap: !0,
    enableContentOverlap: !0,
    enableSameFolderInference: !0,
    enableSameTopFolderInference: !0,
    linkWeight: 0.25,
    mutualLinkBonus: 0.25,
    sharedTagWeight: 0.08,
    titleOverlapWeight: 0.18,
    contentOverlapWeight: 0.16,
    sameFolderWeight: 0.08,
    sameTopFolderWeight: 0.04,
    showSuggestions: !0,
    suggestionThreshold: 0.34,
    commandSimplexSize: 3,
    commandAutoOpenPanel: !0,
    metadataHoverDelayMs: 1e3,
    formalMode: !0,
    sparseEdgeLength: 150,
    sparseGravityBoost: 1.8,
    labelDensity: 0.42,
    renderFilterMetric: "weight",
    renderFilterThreshold: 0.1,
    pinnedNodes: {},
    showHyperedges: !0,
    hyperedgeOpacity: 0.55,
    encounterRecurrenceThreshold: 3,
    crystallizeFolder: "",
    historyFile: "_simplicial-history.md",
    enableRelationHistory: !0,
  };
}
var ze = require("obsidian");
var zn = [
    "---",
    "managedBy: simplicial-complex",
    "---",
    "",
    "<!-- Append-only relation history. One JSON event per line, oldest first.",
    "     Corrections are new events, never edits: this file is only ever appended to. -->",
    "",
  ].join(`
`),
  he = class {
    constructor(e, t) {
      this.app = e;
      this.historyFile = t;
      this.queue = [];
      this.flushing = null;
    }
    setPath(e) {
      this.historyFile = e;
    }
    get path() {
      return this.historyFile;
    }
    async load(e) {
      let t = this.app.vault.getAbstractFileByPath(this.historyFile);
      if (!(t instanceof ze.TFile)) return 0;
      let i = (await this.app.vault.read(t))
        .split(
          `
`,
        )
        .map((s) => ot(s))
        .filter((s) => s !== null);
      return (
        e.load(i),
        C.info("history", "Loaded relation history", { path: this.historyFile, eventCount: i.length }),
        i.length
      );
    }
    record(e) {
      (this.queue.push(e), this.flush());
    }
    async flush() {
      return this.flushing
        ? this.flushing
        : ((this.flushing = (async () => {
            try {
              for (; this.queue.length > 0; ) {
                let t = this.queue.splice(0, this.queue.length).map((n) => rt(n)).join(`
`);
                await this.appendLines(t);
              }
            } catch (e) {
              C.error("history", "Failed to append relation history", {
                path: this.historyFile,
                error: e instanceof Error ? e.message : String(e),
              });
            } finally {
              this.flushing = null;
            }
          })()),
          this.flushing);
    }
    async appendLines(e) {
      let t = this.app.vault.getAbstractFileByPath(this.historyFile);
      if (t instanceof ze.TFile) {
        await this.app.vault.append(
          t,
          `${e}
`,
        );
        return;
      }
      (await this.app.vault.create(
        this.historyFile,
        `${zn}${e}
`,
      ),
        C.info("history", "Created relation history file", { path: this.historyFile }));
    }
  };
var G = require("obsidian");
function Nt(r, e, t) {
  let n = [];
  for (let i of r) {
    if (i.dimension !== 1 || i.boundaryNodes.length !== 3) continue;
    let [s, o, a] = i.boundaryNodes,
      l = F(s, o, e),
      d = F(o, a, e),
      p = F(s, a, e);
    if (l === 0 || d === 0 || p === 0) continue;
    let c = l + d + p,
      u = Math.min(0.9, Math.max(0.3, c / 3)),
      h = K(s, 0, e).length,
      g = K(o, 0, e).length,
      f = K(a, 0, e).length,
      m = [s, o, a].sort((v, y) => (y === s ? h : y === o ? g : f) - (v === s ? h : v === o ? g : f))[0];
    n.push({ nodes: [s, o, a], source: "inferred-bridge", bridgeNode: m, triadScore: c, label: null, weight: u });
  }
  return n;
}
function Mt(r, e) {
  let t = [],
    n = [...r.nodes.keys()],
    i = e.linkStrengthThreshold,
    s = e.closureThreshold;
  for (let o of n) {
    let a = K(o, i, r);
    for (let l = 0; l < a.length; l++)
      for (let d = l + 1; d < a.length; d++) {
        let p = a[l],
          c = a[d],
          u = F(p, c, r);
        if (u >= s) continue;
        let h = F(p, o, r),
          g = F(o, c, r),
          f = h + g - u * 2,
          m = Math.min(0.9, Math.max(0.3, f / 2));
        t.push({ nodes: [p, o, c], source: "inferred-bridge", bridgeNode: o, triadScore: f, label: null, weight: m });
      }
  }
  return t;
}
function jn(r) {
  return [...r].sort().join("|");
}
function Vn(r, e) {
  if (r.length < 2) return { average: 0, density: 0 };
  let t = 0,
    n = 0,
    i = (r.length * (r.length - 1)) / 2;
  for (let s = 0; s < r.length; s++)
    for (let o = s + 1; o < r.length; o++) {
      let a = F(r[s], r[o], e);
      ((t += a), a > 0 && n++);
    }
  return { average: i > 0 ? t / i : 0, density: i > 0 ? n / i : 0 };
}
function je(r, e) {
  let t = Math.max(0.22, e.linkStrengthThreshold * 0.7),
    n = 2,
    i = 0.6,
    s = Math.max(0.16, e.closureThreshold * 0.8),
    o = new Set(),
    a = new Set(),
    l = [];
  for (let d of r.nodes.keys()) {
    if (o.has(d) || K(d, t, r).length < n) continue;
    let c = new Set([d]),
      u = [d];
    for (o.add(d); u.length > 0; ) {
      let v = u.shift(),
        y = K(v, t, r);
      if (!(y.length < n)) for (let b of y) (c.add(b), !o.has(b) && (o.add(b), u.push(b)));
    }
    if (c.size < 3) continue;
    let h = [...c].sort(),
      g =
        h.length > 5
          ? h
              .map((v) => ({
                id: v,
                strength: K(v, t, r)
                  .filter((y) => c.has(y))
                  .reduce((y, b) => y + F(v, b, r), 0),
              }))
              .sort((v, y) => y.strength - v.strength)
              .slice(0, 5)
              .map(({ id: v }) => v)
              .sort()
          : h,
      f = Vn(g, r);
    if (f.density < i || f.average < s) continue;
    let m = jn(g);
    a.has(m) ||
      (a.add(m),
      l.push({
        nodes: g,
        source: "inferred-cross",
        label: "density cluster",
        weight: Math.min(0.95, Math.max(0.35, f.average * 0.7 + f.density * 0.3)),
        triadScore: Number((f.average * g.length).toFixed(2)),
      }));
  }
  return l;
}
function Et(r, e) {
  let t = new Set(r.map((i) => i.role)),
    n = new Set(r.map((i) => i.domain));
  return e === 1 ? t.size >= 2 || n.size >= 2 : e === 2 ? n.size >= 2 && t.size >= 2 : !0;
}
function Tt(r, e, t) {
  let n = new Set(r.map((l) => l.domain)),
    i = new Set(r.map((l) => l.role)),
    s = r.some((l) => l.role === "action");
  if (n.size < e) return { qualifies: !1, isSuper: !1, class: "folder-cluster" };
  let o = n.size >= 3 && i.size >= 3;
  return { qualifies: !0, isSuper: o, class: s ? "project-nucleus" : o ? "super-insight" : "cross-domain-core" };
}
var Gn = {
  halfLifeDays: 90,
  minimumWeight: 0.1,
  roleModifier: { action: 0.3, project: 0.5, research: 0.7, idea: 1, creative: 1.2, reference: 1.5 },
  enableReinforcement: !1,
  reinforcementStrength: 0.5,
};
function kt(r, e, t = Gn, n, i) {
  let s = Date.now(),
    o = Math.max(...e.map((c) => c.modifiedAt)),
    a = (s - o) / (1e3 * 60 * 60 * 24),
    l = e.reduce((c, u) => c + t.roleModifier[u.role], 0) / e.length,
    d = Math.pow(0.5, (a * l) / t.halfLifeDays),
    p = r * d;
  if (t.enableReinforcement && n) {
    let c = e.map((g) => g.id),
      h = 1 + (ct(n, i, c) - 1) * t.reinforcementStrength;
    p *= h;
  }
  return Math.max(t.minimumWeight, Math.min(1, p));
}
function Ve(r, e, t) {
  let n = r.nodes.map((f) => e.find((m) => m.id === f)),
    i = n.length - 1,
    s = r.triadScore ?? 0,
    o = new Set(n.map((f) => f.role)).size;
  s += o * t.roleDiversityWeight;
  let a = new Set(n.map((f) => f.domain)).size;
  ((s += a * t.domainDiversityWeight), n.some((f) => f.role === "action") && (s += t.actionBonus));
  let d = n.flatMap((f) => f.tags),
    p = new Map();
  for (let f of d) p.set(f, (p.get(f) ?? 0) + 1);
  let c = [...p.entries()].filter(([, f]) => f > 1 && f <= 2).length;
  s += c * t.rareTagWeight;
  let u = [...p.entries()].filter(([, f]) => f > 2).length;
  if (((s -= u * t.commonTagPenalty), !Et(n, i)))
    return { ...r, insightScore: 0, class: "folder-cluster", decayedWeight: 0 };
  let h =
    i === 2 ? Tt(n, t.minDomainsForTetra, t.minRolesForTetra) : { qualifies: !0, isSuper: !1, class: "cross-domain" };
  if (!h.qualifies) return { ...r, insightScore: 0, class: "folder-cluster", decayedWeight: 0 };
  let g = kt(r.weight ?? 1, n, {
    halfLifeDays: t.decayHalfLifeDays,
    minimumWeight: t.decayMinimumWeight,
    roleModifier: { action: 0.3, project: 0.5, research: 0.7, idea: 1, creative: 1.2, reference: 1.5 },
    enableReinforcement: !1,
    reinforcementStrength: 0.5,
  });
  return { ...r, insightScore: s, class: h.class, decayedWeight: g };
}
function Yn(r, e) {
  let t = te(r, e),
    n = Mt(t, e),
    i = je(t, e),
    s = new Map();
  [...n, ...i].forEach((h) => {
    let g = [...h.nodes].sort().join("|"),
      f = s.get(g);
    (!f || (h.weight ?? 0) > (f.weight ?? 0)) && s.set(g, h);
  });
  let a = [...s.values()]
      .map((h) => Ve(h, [...t.nodes.values()], e))
      .filter((h) => h.insightScore >= e.insightThreshold && h.class !== "folder-cluster")
      .sort((h, g) => g.insightScore - h.insightScore)
      .map((h) => ({
        nodes: h.nodes,
        weight: h.decayedWeight,
        label: h.label ?? void 0,
        source: h.source,
        sourcePath: void 0,
        autoGenerated: !0,
        inferred: !0,
        userDefined: !1,
        suggested: !0,
        colorKey: "neutral",
        confidence: h.insightScore,
        decayedWeight: h.decayedWeight,
        inferredSignals: h.label === "density cluster" ? ["density-cluster"] : [],
        dominantSignal: h.label === "density cluster" ? "semantic" : "soft-cluster",
      })),
    l = [],
    d = [...t.edges.values()]
      .map((h) => h.strength)
      .filter((h) => h > 0)
      .sort((h, g) => g - h),
    p = 1 - e.linkStrengthThreshold,
    c = Math.max(0, Math.floor(d.length * p) - 1),
    u = d[c] ?? 0;
  for (let h of t.edges.values())
    h.strength >= u &&
      l.push({
        nodes: [h.a, h.b],
        weight: h.strength,
        source: "suggested",
        sourcePath: void 0,
        autoGenerated: !0,
        inferred: !0,
        userDefined: !1,
        suggested: h.strength >= e.closureThreshold,
        colorKey: "neutral",
        confidence: h.strength,
        decayedWeight: h.strength,
        inferredSignals: ["emergent-edge"],
        dominantSignal: "link",
      });
  return [...l, ...a];
}
function It(r, e) {
  return Yn(r, e);
}
function Pt(r, e, t) {
  let n = te(r, e),
    i = Nt(t, n, e),
    s = je(n, e),
    o = new Map();
  [...i, ...s].forEach((g) => {
    let f = [...g.nodes].sort().join("|"),
      m = o.get(f);
    (!m || (g.weight ?? 0) > (m.weight ?? 0)) && o.set(f, g);
  });
  let l = [...o.values()]
      .map((g) => Ve(g, [...n.nodes.values()], e))
      .filter((g) => g.insightScore >= e.insightThreshold && g.class !== "folder-cluster")
      .sort((g, f) => f.insightScore - g.insightScore)
      .map((g) => ({
        nodes: g.nodes,
        weight: g.decayedWeight,
        label: g.label ?? void 0,
        source: g.source,
        sourcePath: void 0,
        autoGenerated: !0,
        inferred: !0,
        userDefined: !1,
        suggested: !0,
        colorKey: "neutral",
        confidence: g.insightScore,
        decayedWeight: g.decayedWeight,
        inferredSignals: g.label === "density cluster" ? ["density-cluster"] : [],
        dominantSignal: g.label === "density cluster" ? "semantic" : "soft-cluster",
      })),
    d = [],
    p = [...n.edges.values()]
      .map((g) => g.strength)
      .filter((g) => g > 0)
      .sort((g, f) => f - g),
    c = 1 - e.linkStrengthThreshold,
    u = Math.max(0, Math.floor(p.length * c) - 1),
    h = p[u] ?? 0;
  for (let g of n.edges.values())
    g.strength >= h &&
      d.push({
        nodes: [g.a, g.b],
        weight: g.strength,
        source: "suggested",
        sourcePath: void 0,
        autoGenerated: !0,
        inferred: !0,
        userDefined: !1,
        suggested: g.strength >= e.closureThreshold,
        colorKey: "neutral",
        confidence: g.strength,
        decayedWeight: g.strength,
        inferredSignals: ["emergent-edge"],
        dominantSignal: "link",
      });
  return [...d, ...l];
}
function Dt(r, e, t) {
  let n = (e?.tags ?? []).map((s) => s.tag?.toLowerCase?.() ?? ""),
    i = e?.frontmatter ?? {};
  return /- \[ \]/.test(t)
    ? "action"
    : i.status || n.some((s) => ["#project", "#plan", "#initiative"].includes(s))
      ? "project"
      : n.some((s) => ["#research", "#paper", "#study", "#analysis"].includes(s))
        ? "research"
        : n.some((s) => ["#story", "#fiction", "#game", "#worldbuilding", "#writing"].includes(s))
          ? "creative"
          : n.some((s) => ["#idea", "#concept", "#hypothesis", "#thought"].includes(s))
            ? "idea"
            : "reference";
}
var Rt = new Set([
    "the",
    "a",
    "an",
    "this",
    "that",
    "these",
    "those",
    "my",
    "your",
    "his",
    "her",
    "its",
    "our",
    "their",
    "in",
    "on",
    "at",
    "to",
    "for",
    "with",
    "from",
    "by",
    "about",
    "into",
    "onto",
    "of",
    "off",
    "over",
    "under",
    "again",
    "further",
    "then",
    "once",
    "here",
    "there",
    "when",
    "where",
    "why",
    "how",
    "all",
    "each",
    "few",
    "more",
    "most",
    "other",
    "some",
    "such",
    "no",
    "nor",
    "not",
    "only",
    "own",
    "same",
    "so",
    "than",
    "too",
    "very",
    "just",
    "now",
    "also",
    "after",
    "before",
    "above",
    "below",
    "up",
    "down",
    "out",
    "on",
    "any",
    "both",
    "during",
    "through",
    "between",
    "against",
    "until",
    "while",
    "and",
    "or",
    "but",
    "if",
    "because",
    "as",
    "until",
    "while",
    "although",
    "though",
    "unless",
    "whether",
    "either",
    "neither",
    "both",
    "yet",
    "so",
    "since",
    "than",
    "i",
    "me",
    "my",
    "myself",
    "we",
    "our",
    "ours",
    "ourselves",
    "you",
    "your",
    "yours",
    "yourself",
    "yourselves",
    "he",
    "him",
    "his",
    "himself",
    "she",
    "her",
    "hers",
    "herself",
    "it",
    "its",
    "itself",
    "they",
    "them",
    "their",
    "theirs",
    "themselves",
    "what",
    "which",
    "who",
    "whom",
    "whose",
    "whoever",
    "whomever",
    "whatever",
    "whichever",
    "one",
    "another",
    "each",
    "everyone",
    "everybody",
    "anyone",
    "anybody",
    "someone",
    "somebody",
    "noone",
    "nobody",
    "nothing",
    "everything",
    "something",
    "anything",
    "is",
    "are",
    "was",
    "were",
    "be",
    "been",
    "being",
    "have",
    "has",
    "had",
    "having",
    "do",
    "does",
    "did",
    "doing",
    "get",
    "gets",
    "got",
    "gotten",
    "make",
    "makes",
    "made",
    "making",
    "take",
    "takes",
    "took",
    "taken",
    "go",
    "goes",
    "went",
    "gone",
    "going",
    "say",
    "says",
    "said",
    "saying",
    "come",
    "comes",
    "came",
    "coming",
    "see",
    "sees",
    "saw",
    "seen",
    "know",
    "knows",
    "knew",
    "known",
    "think",
    "thinks",
    "thought",
    "look",
    "looks",
    "looked",
    "looking",
    "want",
    "wants",
    "wanted",
    "use",
    "uses",
    "used",
    "using",
    "find",
    "finds",
    "found",
    "finding",
    "give",
    "gives",
    "gave",
    "given",
    "giving",
    "tell",
    "tells",
    "told",
    "telling",
    "ask",
    "asks",
    "asked",
    "asking",
    "work",
    "works",
    "worked",
    "working",
    "seem",
    "seems",
    "seemed",
    "seeming",
    "feel",
    "feels",
    "felt",
    "feeling",
    "try",
    "tries",
    "tried",
    "trying",
    "leave",
    "leaves",
    "left",
    "leaving",
    "call",
    "calls",
    "called",
    "calling",
    "note",
    "notes",
    "todo",
    "idea",
    "link",
    "tags",
    "file",
    "folder",
    "vault",
  ]),
  Xn = new Set([
    "\u0627\u06CC\u0646",
    "\u0622\u0646",
    "\u0622\u0646\u0627\u0646",
    "\u0627\u06CC\u0646\u0627\u0646",
    "\u0647\u0645\u06CC\u0646",
    "\u0647\u0645\u0627\u0646",
    "\u0647\u06CC\u0686",
    "\u0647\u0645\u0647",
    "\u0647\u06CC\u0686\u06A9\u0633",
    "\u0647\u0645\u0647",
    "\u0647\u0631",
    "\u0628\u0639\u0636\u06CC",
    "\u0686\u0646\u062F",
    "\u0686\u0646\u06CC\u0646",
    "\u0686\u0646\u0627\u0646",
    "\u0627\u0646",
    "\u0627\u06CC",
    "\u0627\u06CC\u0646",
    "\u0622\u0646",
    "\u062F\u0631",
    "\u0628\u0647",
    "\u0627\u0632",
    "\u0628\u0627",
    "\u0628\u0631",
    "\u0628\u0631\u0627\u06CC",
    "\u062A\u0627",
    "\u0686\u0648\u0646",
    "\u0645\u062B\u0644",
    "\u0645\u0627\u0646\u0646\u062F",
    "\u0631\u0627\u062C\u0639",
    "\u0646\u0633\u0628\u062A",
    "\u0637\u0631\u0641",
    "\u0648\u0633\u0637",
    "\u067E\u0634\u062A",
    "\u0631\u0648\u06CC",
    "\u06A9\u0646\u0627\u0631",
    "\u0646\u0632\u062F",
    "\u062F\u0646\u0628\u0627\u0644",
    "\u0628\u0627\u0644\u0627",
    "\u067E\u0627\u06CC\u06CC\u0646",
    "\u062F\u0627\u062E\u0644",
    "\u062E\u0627\u0631\u062C",
    "\u0632\u06CC\u0631",
    "\u0628\u06CC\u0631\u0648\u0646",
    "\u0627\u062B\u0631",
    "\u0645\u0648\u062C\u0628",
    "\u062F\u0644\u06CC\u0644",
    "\u0633\u0628\u0628",
    "\u062C\u0627\u0646\u0628",
    "\u0637\u0631\u0641",
    "\u0648\u0642\u062A",
    "\u062D\u06CC\u0646",
    "\u0639\u0647\u062F",
    "\u0645\u062F\u062A",
    "\u0644\u062D\u0638\u0647",
    "\u0633\u0627\u0639\u062A",
    "\u062F\u0642\u06CC\u0642\u0647",
    "\u062B\u0627\u0646\u06CC\u0647",
    "\u0631\u0648\u0632",
    "\u0634\u0628",
    "\u0635\u0628\u062D",
    "\u0638\u0647\u0631",
    "\u0639\u0635\u0631",
    "\u0628\u0627\u0645\u062F\u0627\u062F",
    "\u0634\u0627\u0645",
    "\u067E\u06CC\u0634",
    "\u0628\u0639\u062F",
    "\u0642\u0628\u0644",
    "\u0646\u0632\u062F\u06CC\u06A9",
    "\u062F\u0648\u0631",
    "\u0646\u0632\u062F\u06CC\u06A9\u06CC",
    "\u062F\u0648\u0631\u06CC",
    "\u0627\u0648\u0644",
    "\u0622\u062E\u0631",
    "\u0645\u06CC\u0627\u0646",
    "\u0628\u06CC\u0646",
    "\u0648",
    "\u06CC\u0627",
    "\u0627\u0645\u0627",
    "\u0648\u0644\u06CC",
    "\u0686\u0648\u0646",
    "\u0686\u0631\u0627\u06A9\u0647",
    "\u0632\u06CC\u0631\u0627",
    "\u0628\u0644\u06A9\u0647",
    "\u0646\u0647",
    "\u0647\u0645",
    "\u0646\u06CC\u0632",
    "\u06CC\u0639\u0646\u06CC",
    "\u06A9\u0647",
    "\u0686\u0648\u0646\u06A9\u0647",
    "\u0627\u06AF\u0631",
    "\u062A\u0627",
    "\u0686\u0647",
    "\u0622\u0646\u06A9\u0647",
    "\u0648\u0644\u06CC\u06A9\u0646",
    "\u0644\u06CC\u06A9\u0646",
    "\u0647\u0646\u0648\u0632",
    "\u062D\u0627\u0644",
    "\u0648\u0642\u062A\u06CC",
    "\u0628\u06CC\u0646",
    "\u0645\u0646",
    "\u062A\u0648",
    "\u0627\u0648",
    "\u0645\u0627",
    "\u0634\u0645\u0627",
    "\u0627\u06CC\u0634\u0627\u0646",
    "\u0622\u0646\u0647\u0627",
    "\u0645\u0631\u0627",
    "\u062A\u0631\u0627",
    "\u0634\u0645\u0627 \u0631\u0627",
    "\u0645\u0627 \u0631\u0627",
    "\u0622\u0646\u0647\u0627 \u0631\u0627",
    "\u0627\u0645",
    "\u0627\u06CC",
    "\u0627\u0633\u062A",
    "\u0627\u06CC\u0645",
    "\u0627\u06CC\u062F",
    "\u0627\u0646\u062F",
    "\u0628\u0648\u062F\u0645",
    "\u0628\u0648\u062F\u06CC",
    "\u0628\u0648\u062F",
    "\u0628\u0648\u062F\u06CC\u0645",
    "\u0628\u0648\u062F\u06CC\u062F",
    "\u0628\u0648\u062F\u0646\u062F",
    "\u062E\u0648\u062F",
    "\u062E\u0648\u062F\u0645",
    "\u062E\u0648\u062F\u062A",
    "\u062E\u0648\u062F\u0634",
    "\u062E\u0648\u062F\u0645\u0627\u0646",
    "\u062E\u0648\u062F\u062A\u0627\u0646",
    "\u062E\u0648\u062F\u0634\u0627\u0646",
    "\u06A9\u0631\u062F",
    "\u06A9\u0631\u062F\u0645",
    "\u06A9\u0631\u062F\u06CC",
    "\u06A9\u0631\u062F",
    "\u06A9\u0631\u062F\u06CC\u0645",
    "\u06A9\u0631\u062F\u06CC\u062F",
    "\u06A9\u0631\u062F\u0646\u062F",
    "\u06A9\u0646",
    "\u06A9\u0646\u0645",
    "\u06A9\u0646\u06CC",
    "\u06A9\u0646\u062F",
    "\u06A9\u0646\u06CC\u0645",
    "\u06A9\u0646\u06CC\u062F",
    "\u06A9\u0646\u0646\u062F",
    "\u0634\u062F",
    "\u0634\u062F\u0645",
    "\u0634\u062F\u06CC",
    "\u0634\u062F",
    "\u0634\u062F\u06CC\u0645",
    "\u0634\u062F\u06CC\u062F",
    "\u0634\u062F\u0646\u062F",
    "\u0634\u0648",
    "\u0634\u0648\u0645",
    "\u0634\u0648\u06CC",
    "\u0634\u0648\u062F",
    "\u0634\u0648\u06CC\u0645",
    "\u0634\u0648\u06CC\u062F",
    "\u0634\u0648\u0646\u062F",
    "\u0628\u0648\u062F",
    "\u0628\u0648\u062F\u0645",
    "\u0628\u0648\u062F\u06CC",
    "\u0628\u0648\u062F",
    "\u0628\u0648\u062F\u06CC\u0645",
    "\u0628\u0648\u062F\u06CC\u062F",
    "\u0628\u0648\u062F\u0646\u062F",
    "\u0628\u0627\u0634",
    "\u0628\u0627\u0634\u0645",
    "\u0628\u0627\u0634\u06CC",
    "\u0628\u0627\u0634\u062F",
    "\u0628\u0627\u0634\u06CC\u0645",
    "\u0628\u0627\u0634\u06CC\u062F",
    "\u0628\u0627\u0634\u0646\u062F",
    "\u062F\u0627\u062F",
    "\u062F\u0627\u062F\u0645",
    "\u062F\u0627\u062F\u06CC",
    "\u062F\u0627\u062F",
    "\u062F\u0627\u062F\u06CC\u0645",
    "\u062F\u0627\u062F\u06CC\u062F",
    "\u062F\u0627\u062F\u0646\u062F",
    "\u062F\u0647",
    "\u062F\u0647\u0645",
    "\u062F\u0647\u06CC",
    "\u062F\u0647\u062F",
    "\u062F\u0647\u06CC\u0645",
    "\u062F\u0647\u06CC\u062F",
    "\u062F\u0647\u0646\u062F",
    "\u06AF\u0631\u0641\u062A",
    "\u06AF\u0631\u0641\u062A\u0645",
    "\u06AF\u0631\u0641\u062A\u06CC",
    "\u06AF\u0631\u0641\u062A",
    "\u06AF\u0631\u0641\u062A\u06CC\u0645",
    "\u06AF\u0631\u0641\u062A\u06CC\u062F",
    "\u06AF\u0631\u0641\u062A\u0646\u062F",
    "\u06AF\u06CC\u0631",
    "\u06AF\u06CC\u0631\u0645",
    "\u06AF\u06CC\u0631\u06CC",
    "\u06AF\u06CC\u0631\u062F",
    "\u06AF\u06CC\u0631\u06CC\u0645",
    "\u06AF\u06CC\u0631\u06CC\u062F",
    "\u06AF\u06CC\u0631\u0646\u062F",
    "\u0632\u062F",
    "\u0632\u062F\u0645",
    "\u0632\u062F\u06CC",
    "\u0632\u062F",
    "\u0632\u062F\u06CC\u0645",
    "\u0632\u062F\u06CC\u062F",
    "\u0632\u062F\u0646\u062F",
    "\u0632\u0646",
    "\u0632\u0646\u0645",
    "\u0632\u0646\u06CC",
    "\u0632\u0646\u062F",
    "\u0632\u0646\u06CC\u0645",
    "\u0632\u0646\u06CC\u062F",
    "\u0632\u0646\u0646\u062F",
    "\u0631\u0641\u062A",
    "\u0631\u0641\u062A\u0645",
    "\u0631\u0641\u062A\u06CC",
    "\u0631\u0641\u062A",
    "\u0631\u0641\u062A\u06CC\u0645",
    "\u0631\u0641\u062A\u06CC\u062F",
    "\u0631\u0641\u062A\u0646\u062F",
    "\u0631\u0648",
    "\u0631\u0648\u0645",
    "\u0631\u0648\u06CC",
    "\u0631\u0648\u062F",
    "\u0631\u0648\u06CC\u0645",
    "\u0631\u0648\u06CC\u062F",
    "\u0631\u0648\u0646\u062F",
    "\u0622\u0645\u062F",
    "\u0622\u0645\u062F\u0645",
    "\u0622\u0645\u062F\u06CC",
    "\u0622\u0645\u062F",
    "\u0622\u0645\u062F\u06CC\u0645",
    "\u0622\u0645\u062F\u06CC\u062F",
    "\u0622\u0645\u062F\u0646\u062F",
    "\u0628\u06CC\u0627",
    "\u0628\u06CC\u0627\u06CC\u0645",
    "\u0628\u06CC\u0627\u06CC\u06CC",
    "\u0628\u06CC\u0627\u06CC\u062F",
    "\u0628\u06CC\u0627\u06CC\u06CC\u0645",
    "\u0628\u06CC\u0627\u06CC\u06CC\u062F",
    "\u0628\u06CC\u0627\u06CC\u0646\u062F",
    "\u0645\u06CC",
    "\u0646\u0645\u06CC",
    "\u0646\u0647",
    "\u0628\u0644\u0647",
    "\u062E\u0648\u0628",
    "\u0628\u062F",
    "\u0628\u0633\u06CC\u0627\u0631",
    "\u062E\u06CC\u0644\u06CC",
    "\u0632\u06CC\u0627\u062F",
    "\u06A9\u0645",
    "\u06A9\u0645\u06CC",
    "\u062A\u0646\u0647\u0627",
    "\u0641\u0642\u0637",
    "\u062D\u062A\u06CC",
    "\u062F\u06CC\u06AF\u0631",
    "\u062F\u06CC\u06AF\u0631\u06CC",
    "\u062F\u06CC\u06AF\u0631\u0627\u0646",
    "\u062D\u0627\u0644\u0627",
    "\u0627\u0644\u0627\u0646",
    "\u0627\u0645\u0631\u0648\u0632",
    "\u062F\u06CC\u0631\u0648\u0632",
    "\u0641\u0631\u062F\u0627",
    "\u0635\u0628\u062D",
    "\u0638\u0647\u0631",
    "\u0639\u0635\u0631",
    "\u0634\u0628",
    "\u06AF\u0627\u0647",
    "\u06AF\u0627\u0647\u06CC",
    "\u0647\u0645\u06CC\u0634\u0647",
    "\u0647\u0631\u06AF\u0632",
    "\u06CC\u06A9",
    "\u062F\u0648",
    "\u0633\u0647",
    "\u0686\u0647\u0627\u0631",
    "\u067E\u0646\u062C",
    "\u0627\u0648\u0644",
    "\u062F\u0648\u0627\u0645",
    "\u0633\u0648\u0645",
    "\u0686\u0647\u0627\u0631\u0645",
    "\u067E\u0646\u062C\u0645",
    "\u06CC\u06A9\u06CC",
    "\u0686\u06CC\u0632\u06CC",
    "\u06A9\u0633\u06CC",
    "\u062C\u0627\u06CC\u06CC",
    "\u0628\u0627\u06CC\u062F",
    "\u0634\u0627\u06CC\u062F",
    "\u0644\u0637\u0641\u0627",
    "\u0645\u062A\u0634\u06A9\u0631\u0645",
    "\u0633\u067E\u0627\u0633",
    "\u0645\u0631\u0633\u06CC",
    "\u0628\u0641\u0631\u0645\u0627\u06CC\u06CC\u062F",
    "\u0628\u0631\u0648",
    "\u0628\u06CC\u0627",
    "\u0647\u0627",
    "\u0647\u0627\u06CC",
    "\u0627\u062A",
    "\u06CC\u062A",
    "\u0627\u0634",
    "\u0645\u0627\u0646",
    "\u062A\u0627\u0646",
    "\u0634\u0627\u0646",
    "\u06CC",
    "\u0627\u06CC",
    "\u0627\u06CC\u0645",
    "\u0627\u06CC\u062F",
    "\u0627\u0646\u062F",
    "\u0645",
    "\u06CC",
    "\u0633\u062A",
    "\u0627\u06CC\u0645",
    "\u0627\u06CC\u062F",
    "\u0646\u062F",
    "\u0631\u0645",
    "\u0631\u06CC",
    "\u0631\u062F",
    "\u0631\u06CC\u0645",
    "\u0631\u06CC\u062F",
    "\u0631\u0646\u062F",
    "note",
    "notes",
    "todo",
    "idea",
    "link",
    "tags",
    "file",
    "folder",
    "vault",
  ]),
  Un = new Set([
    "el",
    "la",
    "de",
    "que",
    "y",
    "a",
    "en",
    "un",
    "ser",
    "se",
    "no",
    "haber",
    "por",
    "con",
    "su",
    "para",
    "como",
    "estar",
    "tener",
    "le",
    "lo",
    "todo",
    "pero",
    "m\xE1s",
    "hacer",
    "o",
    "poder",
    "decir",
    "este",
    "ir",
    "otro",
    "ese",
    "la",
    "si",
    "me",
    "ya",
    "ver",
    "porque",
    "dar",
    "cuando",
    "\xE9l",
    "muy",
    "sin",
    "vez",
    "mucho",
    "saber",
    "qu\xE9",
    "sobre",
    "mi",
    "alguno",
    "mismo",
    "yo",
    "tambi\xE9n",
    "hasta",
    "a\xF1o",
    "dos",
    "querer",
    "entre",
    "as\xED",
    "primero",
    "desde",
    "grande",
    "esto",
    "ni",
    "nos",
    "llegar",
    "pasar",
    "tiempo",
    "ella",
    "s\xED",
    "d\xEDa",
    "uno",
    "bien",
    "poco",
    "deber",
    "entonces",
    "poner",
    "cosa",
    "tanto",
    "hombre",
    "parecer",
    "nuestro",
    "tan",
    "donde",
    "ahora",
    "parte",
    "despu\xE9s",
    "vida",
    "quedar",
    "siempre",
    "creer",
    "hablar",
    "llevar",
    "dejar",
    "nada",
    "cada",
    "seguir",
    "menos",
    "nuevo",
    "encontrar",
    "algo",
    "casa",
    "gente",
    "momento",
    "note",
    "notes",
    "todo",
    "idea",
    "link",
    "tags",
    "file",
    "folder",
    "vault",
  ]),
  qn = new Set([
    "le",
    "de",
    "un",
    "\xEAtre",
    "et",
    "\xE0",
    "il",
    "avoir",
    "ne",
    "je",
    "son",
    "que",
    "se",
    "qui",
    "ce",
    "dans",
    "en",
    "du",
    "elle",
    "au",
    "de",
    "ce",
    "pour",
    "pas",
    "que",
    "vous",
    "par",
    "sur",
    "faire",
    "plus",
    "dire",
    "me",
    "on",
    "mon",
    "lui",
    "nous",
    "comme",
    "mais",
    "pouvoir",
    "tout",
    "y",
    "aller",
    "voir",
    "bien",
    "o\xF9",
    "sans",
    "tu",
    "ou",
    "leur",
    "l\xE0",
    "deux",
    "mari",
    "vouloir",
    "venir",
    "quand",
    "grand",
    "celui",
    "si",
    "notre",
    "m\xEAme",
    "ont",
    "o\xF9",
    "tout",
    "savoir",
    "votre",
    "doit",
    "bon",
    "tr\xE8s",
    "aucun",
    "peu",
    "m\xEAme",
    "trop",
    "assez",
    "quelque",
    "fois",
    "tant",
    "rien",
    "toujours",
    "encore",
    "quel",
    "ainsi",
    "moins",
    "aussi",
    "note",
    "notes",
    "todo",
    "id\xE9e",
    "lien",
    "tags",
    "fichier",
    "dossier",
    "coffre",
  ]),
  Zn = new Set([
    "der",
    "die",
    "und",
    "in",
    "den",
    "von",
    "zu",
    "mit",
    "ist",
    "das",
    "f\xFCr",
    "auf",
    "sich",
    "dem",
    "er",
    "nicht",
    "ein",
    "eine",
    "als",
    "auch",
    "es",
    "an",
    "werden",
    "aus",
    "er",
    "hat",
    "dass",
    "sie",
    "nach",
    "wird",
    "bei",
    "einer",
    "um",
    "am",
    "sind",
    "noch",
    "wie",
    "einen",
    "so",
    "zum",
    "sein",
    "oder",
    "wurde",
    "ihr",
    "bis",
    "mehr",
    "durch",
    "man",
    "mein",
    "mich",
    "hatte",
    "sein",
    "kann",
    "sei",
    "war",
    "wenn",
    "w\xFCrde",
    "seine",
    "um",
    "haben",
    "keine",
    "vom",
    "wo",
    "geht",
    "k\xF6nnen",
    "ja",
    "sein",
    "sehr",
    "hier",
    "ganz",
    "also",
    "dann",
    "schon",
    "wohl",
    "immer",
    "m\xFCssen",
    "ohne",
    "etwas",
    "sagen",
    "jedoch",
    "da",
    "note",
    "notes",
    "todo",
    "idee",
    "link",
    "tags",
    "datei",
    "ordner",
    "tresor",
  ]),
  Jn = { en: Rt, fa: Xn, es: Un, fr: qn, de: Zn };
function Qn(r) {
  let e = r.toLowerCase().split("-")[0];
  return Jn[e] ?? Rt;
}
function Ft(r, e = "en") {
  return Qn(e).has(r.toLowerCase());
}
var ei = "en";
function At(r, e = ei) {
  return new Set(
    r
      .toLocaleLowerCase()
      .split(/[^\p{L}\p{N}_]+/gu)
      .filter((t) => t.length >= 3 && !Ft(t, e)),
  );
}
function Ht(r) {
  return r.replace(/^#/, "").trim().toLowerCase();
}
function Ot(r, e) {
  let t = 0;
  for (let n of r) e.has(n) && t++;
  return t;
}
function ue(r, e) {
  return [r, e].sort().join("|");
}
function ti(r) {
  if (r.includes("soft-cluster")) return "soft-cluster";
  if (r.some((e) => e.startsWith("tags:"))) return "tags";
  if (r.some((e) => e === "folder:same" || e === "folder:top")) return "folder";
  if (r.some((e) => e.startsWith("title:") || e.startsWith("content:"))) return "semantic";
  if (r.some((e) => e.startsWith("link:"))) return "link";
}
function ni(r) {
  let e = new Map();
  return (
    r.forEach((t) => {
      (t.titleTokens.forEach((n) => {
        (e.has(n) || e.set(n, new Set()), e.get(n).add(t.path));
      }),
        t.contentTokens.forEach((n) => {
          (e.get(n)?.size ?? 0) < Math.max(3, r.length * 0.1) &&
            (e.has(n) || e.set(n, new Set()), e.get(n).add(t.path));
        }),
        t.tags.forEach((n) => {
          let i = `tag:${n}`;
          (e.has(i) || e.set(i, new Set()), e.get(i).add(t.path));
        }));
    }),
    e
  );
}
function ii(r, e, t) {
  let n = new Set();
  (r.titleTokens.forEach((o) => {
    e.get(o)?.forEach((a) => {
      a !== r.path && n.add(a);
    });
  }),
    r.contentTokens.forEach((o) => {
      e.get(o)?.forEach((a) => {
        a !== r.path && n.add(a);
      });
    }),
    r.tags.forEach((o) => {
      let a = `tag:${o}`;
      e.get(a)?.forEach((l) => {
        l !== r.path && n.add(l);
      });
    }));
  let i = `folder:${r.folder}`;
  e.has(i) &&
    e.get(i).forEach((o) => {
      o !== r.path && n.add(o);
    });
  let s = Array.from(n);
  return (s.length > 50 && s.splice(50), s);
}
function Lt(r, e, t) {
  if (!r.size || !e.size) return 0;
  let n = Ot(r, e);
  return n ? Math.min(t, (n / Math.max(r.size, e.size)) * t * 2) : 0;
}
function si(r) {
  let e = new Set();
  r?.tags?.forEach((i) => e.add(Ht(i.tag)));
  let t = r?.frontmatter?.tags;
  return ((Array.isArray(t) ? t : typeof t == "string" ? [t] : []).forEach((i) => e.add(Ht(String(i)))), e);
}
function ri(r, e, t) {
  let n = new Set();
  return (
    e?.links?.forEach((i) => {
      let s = t.metadataCache.getFirstLinkpathDest(i.link, r.path);
      s && n.add(s.path);
    }),
    n
  );
}
function Kt(r, e, t) {
  let n = r.metadataCache.getFileCache(e),
    i = e.parent?.path ?? "",
    s = i.split("/")[0] ?? "",
    o = t.replace(/^---\n[\s\S]*?\n---\n?/, "");
  return {
    path: e.path,
    folder: i,
    topFolder: s,
    titleTokens: At(e.basename),
    contentTokens: At(o),
    tags: si(n),
    outgoingLinks: ri(e, n, r),
    role: Dt(e, n, t),
    modifiedAt: e.stat?.mtime ?? Date.now(),
  };
}
function Ge(r, e) {
  if (!e.enableInferredEdges && !e.linkGraphBaseline)
    return (C.info("inference", "Inferred simplices disabled by settings"), []);
  let t = [],
    n = new Map(),
    i = new Map(r.map((a) => [a.path, a])),
    s = ni(r),
    o = new Set(r.map((a) => a.path));
  if (
    (r.forEach((a) => {
      let l = `folder:${a.folder}`;
      (s.has(l) || s.set(l, new Set()), s.get(l).add(a.path));
    }),
    r.forEach((a) => {
      ii(a, s, o).forEach((d) => {
        let p = i.get(d);
        if (!p || a.path > d) return;
        let c = a,
          u = p,
          h = 0,
          g = [],
          f = !1;
        if (
          (e.enableLinkInference !== !1 &&
            c.outgoingLinks.has(u.path) &&
            ((f = !0), (h += e.linkWeight), g.push("link:a->b")),
          e.enableMutualLinkBonus !== !1 &&
            u.outgoingLinks.has(c.path) &&
            ((f = !0), (h += e.mutualLinkBonus), g.push("link:b->a")),
          !e.linkGraphBaseline && f && ((h = 0), (f = !1), (g.length = 0)),
          !e.enableInferredEdges && !f)
        )
          return;
        let m = Ot(c.tags, u.tags);
        if (e.enableInferredEdges && e.enableSharedTags !== !1 && m > 0) {
          let S = Math.min(e.sharedTagWeight * 3, m * e.sharedTagWeight);
          ((h += S), g.push(`tags:${m}`));
        }
        let v =
          e.enableInferredEdges && e.enableTitleOverlap !== !1
            ? Lt(c.titleTokens, u.titleTokens, e.titleOverlapWeight)
            : 0;
        v > 0 && ((h += v), g.push(`title:${v.toFixed(2)}`));
        let y =
          e.enableInferredEdges && e.enableContentOverlap !== !1
            ? Lt(c.contentTokens, u.contentTokens, e.contentOverlapWeight)
            : 0;
        if (
          (y > 0 && ((h += y), g.push(`content:${y.toFixed(2)}`)),
          e.enableInferredEdges && e.enableSameFolderInference !== !1 && c.folder && c.folder === u.folder
            ? ((h += e.sameFolderWeight), g.push("folder:same"))
            : e.enableInferredEdges &&
              e.enableSameTopFolderInference !== !1 &&
              c.topFolder &&
              c.topFolder === u.topFolder &&
              ((h += e.sameTopFolderWeight), g.push("folder:top")),
          !f && h < e.inferenceThreshold)
        )
          return;
        let b = Math.max(0.1, Math.min(1, Number(h.toFixed(2))));
        (t.push({
          nodes: [c.path, u.path],
          weight: b,
          label: f && !e.enableInferredEdges ? "vault link" : "inferred relation",
          inferred: !0,
          userDefined: !1,
          autoGenerated: !1,
          colorKey: "neutral",
          inferredSignals: g,
          dominantSignal: ti(g),
          confidence: b,
          suggested: b >= e.suggestionThreshold,
        }),
          n.set(ue(c.path, u.path), { nodes: [c.path, u.path], weight: b, signals: [...g] }));
      });
    }),
    e.enableInferredEdges)
  ) {
    let a = new Map();
    (n.forEach((d) => {
      let [p, c] = d.nodes;
      d.weight >= Math.max(e.inferenceThreshold, 0.18) &&
        (a.has(p) || a.set(p, []),
        a.has(c) || a.set(c, []),
        a.get(p).push({ partner: c, weight: d.weight, signals: d.signals }),
        a.get(c).push({ partner: p, weight: d.weight, signals: d.signals }));
    }),
      Array.from(a.entries())
        .filter(([, d]) => d.length >= 2)
        .map(([d]) => d)
        .forEach((d) => {
          let p = a.get(d) || [];
          for (let c = 0; c < p.length; c++)
            for (let u = c + 1; u < p.length; u++) {
              let h = p[c].partner,
                g = p[u].partner;
              if ([d, h, g].sort()[0] !== d) continue;
              let m = n.get(ue(d, h)),
                v = n.get(ue(d, g)),
                y = n.get(ue(h, g));
              if (!m || !v || !y) continue;
              let b = new Set([...m.signals, ...v.signals, ...y.signals, "soft-cluster"]);
              t.push({
                nodes: [d, h, g],
                weight: Math.min(1, Number(((m.weight + v.weight + y.weight) / 3 + 0.05).toFixed(2))),
                label: "soft cluster",
                inferred: !0,
                userDefined: !1,
                autoGenerated: !1,
                colorKey: "neutral",
                inferredSignals: [...b],
                dominantSignal: "soft-cluster",
                confidence: Math.min(1, Number(((m.weight + v.weight + y.weight) / 3 + 0.05).toFixed(2))),
                suggested: !0,
              });
            }
        }));
  }
  return (
    C.debug("inference", "Rebuilt inferred simplices (optimized)", {
      fileCount: r.length,
      inferredSimplexCount: t.length,
    }),
    t
  );
}
function $t(r, e) {
  let t = e.inferenceMode ?? "taxonomic",
    n = [];
  return (
    (t === "taxonomic" || t === "hybrid") && n.push(...Ge(r, e)),
    (t === "emergent" || t === "hybrid") && n.push(...It(r, e)),
    n
  );
}
var jt = require("obsidian");
var Wt = /^(△+|simplex:)\s+(.+)$/gim,
  Bt = /^(◇|hyperedge:|encounter:)\s+(.+)$/gim;
function oi(r, e) {
  let t = r.match(/^---\n([\s\S]*?)\n---/);
  if (!t) return null;
  try {
    return e.parseYaml(t[1]);
  } catch (n) {
    return (
      C.warn("parser", "Failed to parse frontmatter", { error: n instanceof Error ? n.message : String(n) }),
      null
    );
  }
}
function _t(r, e) {
  let t = Array.isArray(r.nodes) ? r.nodes.map(String) : [];
  return j(t.map((n) => e.canonicalize(n)));
}
function ai(r, e, t) {
  if (!r || typeof r != "object") return null;
  let n = r,
    i = _t(n, t);
  return i.length < 2
    ? null
    : {
        nodes: k(i),
        label: typeof n.label == "string" ? n.label : void 0,
        weight: typeof n.weight == "number" ? n.weight : void 0,
        userDefined: !0,
        autoGenerated: !1,
        sourcePath: e,
      };
}
function li(r, e, t) {
  if (!r || typeof r != "object") return null;
  let n = r,
    i = _t(n, t);
  if (i.length < 2) return null;
  let s = n.persistence === "recurring" ? "recurring" : "momentary";
  return {
    nodes: k(i),
    label: typeof n.label == "string" ? n.label : void 0,
    weight: typeof n.weight == "number" ? n.weight : void 0,
    mode: typeof n.mode == "string" ? n.mode : void 0,
    occurredAt: typeof n.occurredAt == "number" ? n.occurredAt : void 0,
    persistence: s,
    sourcePath: e,
  };
}
function di(r, e, t) {
  let n = [];
  Wt.lastIndex = 0;
  let i;
  for (; (i = Wt.exec(r)) !== null; ) {
    let s = i[1],
      o = i[2].split(/\s+/).filter(Boolean),
      a = s === "\u25B3\u25B3" ? 4 : s === "\u25B3" ? 3 : o.length,
      l = j(o.slice(0, a).map((d) => t.canonicalize(d)));
    l.length < 2 || n.push({ nodes: k(l), sourcePath: e, userDefined: !0, autoGenerated: !1 });
  }
  return n;
}
function ci(r, e, t) {
  let n = [];
  Bt.lastIndex = 0;
  let i;
  for (; (i = Bt.exec(r)) !== null; ) {
    let s = i[2].split(/\s+/).filter(Boolean),
      o = j(s.map((a) => t.canonicalize(a)));
    o.length < 2 || n.push({ nodes: k(o), sourcePath: e, persistence: "momentary" });
  }
  return n;
}
function zt(r, e, t) {
  let n = oi(r, t),
    i = { nodeIds: new Set(), simplices: [], hyperedges: [] };
  if (Array.isArray(n?.simplices))
    for (let o of n.simplices) {
      let a = ai(o, e, t);
      a && i.simplices.push(a);
    }
  if (Array.isArray(n?.hyperedges))
    for (let o of n.hyperedges) {
      let a = li(o, e, t);
      a && i.hyperedges.push(a);
    }
  (i.simplices.push(...di(r, e, t)), i.hyperedges.push(...ci(r, e, t)));
  let s = hi(i);
  return (
    s.simplices.forEach((o) => o.nodes.forEach((a) => s.nodeIds.add(a))),
    s.hyperedges.forEach((o) => o.nodes.forEach((a) => s.nodeIds.add(a))),
    C.info("parser", "Parsed relations", {
      sourcePath: e,
      simplexCount: s.simplices.length,
      hyperedgeCount: s.hyperedges.length,
      nodeCount: s.nodeIds.size,
    }),
    s
  );
}
function hi(r) {
  let e = new Set();
  r.simplices = r.simplices.filter((n) => {
    let i = N(n.nodes);
    return e.has(i) ? !1 : (e.add(i), !0);
  });
  let t = new Set();
  return (
    (r.hyperedges = r.hyperedges.filter((n) => {
      let i = N(n.nodes);
      return t.has(i) ? !1 : (t.add(i), !0);
    })),
    r
  );
}
function Vt(r, e, t) {
  return zt(r, e, {
    canonicalize: (i) => re(i, e, t)?.path ?? i.trim(),
    parseYaml: (i) => (0, jt.parseYaml)(i) ?? null,
  });
}
var pe = class {
  constructor(e, t, n, i, s) {
    this.app = e;
    this.model = t;
    this.settings = n;
    this.onExternalChange = i;
    this.onEncounterParsed = s;
    this.fullScanChunkSize = 24;
    this.inferenceRebuildDelayMs = 40;
    this.lastWrittenHash = new Map();
    this.fileSimplexKeys = new Map();
    this.inferenceContexts = new Map();
    this.lastInferredSnapshot = "";
    this.inferenceRebuildTimer = null;
    this.inferenceRebuildPromise = null;
    this.resolveInferenceRebuild = null;
    ((this.debouncedChange = (0, G.debounce)(
      (o) => {
        this.onFileChange(o);
      },
      100,
      !0,
    )),
      this.app.vault.on("modify", (o) => {
        (U(), o instanceof G.TFile && this.debouncedChange(o));
      }),
      this.app.vault.on("create", (o) => {
        (U(), o instanceof G.TFile && this.debouncedChange(o));
      }),
      this.app.vault.on("delete", (o) => {
        (U(), this.onFileDelete(o));
      }),
      this.app.vault.on("rename", (o, a) => {
        (U(), o instanceof G.TFile && this.onFileRename(o, a));
      }));
  }
  getInferenceContexts() {
    return [...this.inferenceContexts.values()];
  }
  recordWrite(e, t) {
    (this.lastWrittenHash.set(e, _(t)),
      C.debug("vault-index", "Recorded plugin write hash", { path: e, hash: this.lastWrittenHash.get(e) }));
  }
  updateSettings(e) {
    ((this.settings = e), this.rebuildInferredSimplices());
  }
  isPluginInternalFile(e) {
    return e === this.settings.historyFile;
  }
  async fullScan() {
    U();
    let e = this.app.vault.getMarkdownFiles().filter((n) => !this.isPluginInternalFile(n.path));
    C.info("vault-index", "Starting full vault scan", { fileCount: e.length });
    let t = [];
    for (let n of e) {
      let i = await this.app.vault.read(n);
      (t.push({ file: n, content: i }),
        t.length >= this.fullScanChunkSize && (this.flushFullScanChunk(t), (t = []), await this.yieldToBrowser()));
    }
    (t.length > 0 && this.flushFullScanChunk(t),
      await this.scheduleInferenceRebuild(0),
      C.info("vault-index", "Completed full vault scan", {
        fileCount: e.length,
        indexedNodeCount: this.model.nodes.size,
        simplexCount: this.model.simplices.size,
      }));
  }
  async onFileChange(e) {
    if (e.extension !== "md" || this.isPluginInternalFile(e.path)) return;
    let t = await this.app.vault.read(e),
      n = _(t);
    if (this.lastWrittenHash.get(e.path) === n) {
      C.debug("vault-index", "Suppressed self-triggered modify event", { path: e.path, hash: n });
      return;
    }
    (C.info("vault-index", "Processing changed file", { path: e.path, hash: n }),
      this.processFile(e, t),
      await this.scheduleInferenceRebuild(),
      this.onExternalChange?.());
  }
  onFileDelete(e) {
    e instanceof G.TFile &&
      (C.info("vault-index", "File deleted", { path: e.path }),
      this.inferenceContexts.delete(e.path),
      this.model.removeNode(e.path),
      this.model.replaceSourceRelations(e.path, [], []),
      this.scheduleInferenceRebuild().then(() => this.onExternalChange?.()));
  }
  onFileRename(e, t) {
    (C.info("vault-index", "File renamed", { oldPath: t, newPath: e.path }), this.model.updateNodeId(t, e.path));
    let n = this.fileSimplexKeys.get(t);
    n && (this.fileSimplexKeys.set(e.path, n), this.fileSimplexKeys.delete(t));
    let i = this.inferenceContexts.get(t);
    (i && (this.inferenceContexts.set(e.path, { ...i, path: e.path }), this.inferenceContexts.delete(t)),
      this.scheduleInferenceRebuild().then(() => this.onExternalChange?.()));
  }
  processFile(e, t) {
    this.model.setNode(e.path, { isVirtual: !1 });
    let n = Vt(t, e.path, this.app);
    (this.model.replaceSourceRelations(e.path, n.simplices, n.hyperedges),
      this.fileSimplexKeys.set(e.path, new Set(n.simplices.map((i) => i.nodes.join("|")))),
      this.inferenceContexts.set(e.path, Kt(this.app, e, t)),
      n.hyperedges.forEach((i) => this.onEncounterParsed?.(i)),
      C.info("vault-index", "Indexed file", {
        path: e.path,
        parsedSimplexCount: n.simplices.length,
        parsedHyperedgeCount: n.hyperedges.length,
        parsedNodeCount: n.nodeIds.size,
        totalNodeCount: this.model.nodes.size,
        totalSimplexCount: this.model.simplices.size,
        totalHyperedgeCount: this.model.hyperedges.size,
      }));
  }
  flushFullScanChunk(e) {
    this.model.batch(() => {
      e.forEach(({ file: t, content: n }) => {
        this.processFile(t, n);
      });
    });
  }
  async yieldToBrowser() {
    await new Promise((e) => window.setTimeout(e, 0));
  }
  scheduleInferenceRebuild(e = this.inferenceRebuildDelayMs) {
    return (
      this.inferenceRebuildPromise === null &&
        (this.inferenceRebuildPromise = new Promise((t) => {
          this.resolveInferenceRebuild = t;
        })),
      this.inferenceRebuildTimer !== null && window.clearTimeout(this.inferenceRebuildTimer),
      (this.inferenceRebuildTimer = window.setTimeout(() => {
        this.inferenceRebuildTimer = null;
        try {
          this.rebuildInferredSimplices();
        } finally {
          let t = this.resolveInferenceRebuild;
          ((this.resolveInferenceRebuild = null), (this.inferenceRebuildPromise = null), t?.());
        }
      }, e)),
      this.inferenceRebuildPromise
    );
  }
  rebuildInferredSimplices() {
    let e;
    if (
      this.settings.enableBettiComputation &&
      (this.settings.inferenceMode === "emergent" || this.settings.inferenceMode === "hybrid")
    ) {
      let n = this.model.getCachedBetti().holes;
      if (
        ((e = Pt([...this.inferenceContexts.values()], this.settings, n)), this.settings.inferenceMode === "hybrid")
      ) {
        let i = Ge([...this.inferenceContexts.values()], this.settings),
          s = new Set(e.map((a) => a.nodes.sort().join("|"))),
          o = i.filter((a) => !s.has(a.nodes.sort().join("|")));
        e.push(...o);
      }
    } else e = $t([...this.inferenceContexts.values()], this.settings);
    this.model.replaceInferredSimplices(e);
    let t = JSON.stringify({
      inferredSimplexCount: e.length,
      totalSimplexCount: this.model.simplices.size,
      totalNodeCount: this.model.nodes.size,
      enabled: this.settings.enableInferredEdges,
    });
    t !== this.lastInferredSnapshot &&
      ((this.lastInferredSnapshot = t), C.debug("vault-index", "Updated inferred graph state", JSON.parse(t)));
  }
  destroy() {
    this.inferenceRebuildTimer !== null &&
      (window.clearTimeout(this.inferenceRebuildTimer), (this.inferenceRebuildTimer = null));
  }
};
function ui(r, e, t) {
  return r + (e - r) * t;
}
var ge = class {
  constructor(e, t, n, i, s, o) {
    this.model = e;
    this.onWake = t;
    this.onSelection = n;
    this.onHoverIntent = i;
    this.onPinnedStateChanged = s;
    this.onInteraction = o;
    this.hoveredNodeId = null;
    this.lockedNodeId = null;
    this.hoveredSimplexKey = null;
    this.hoveredHyperedgeKey = null;
    this.holdNode = null;
    this.holdTimer = null;
    this.hoverIntentTimer = null;
    this.pressedNodeId = null;
    this.draggingNodeId = null;
    this.draggedNodeWasPinned = !1;
    this.dragOffsetX = 0;
    this.dragOffsetY = 0;
    this.pressX = 0;
    this.pressY = 0;
    this.movedDuringPointerDown = !1;
    this.interactionTracker = Ke();
  }
  getInteractionTracker() {
    return this.interactionTracker;
  }
  setInteractionTracker(e) {
    this.interactionTracker = e;
  }
  logConfirm(e, t) {
    (W(this.interactionTracker, { type: "confirm", simplexKey: e, nodeIds: t, weight: 0.5 }),
      this.onInteraction?.(this.interactionTracker));
  }
  logReject(e, t) {
    (W(this.interactionTracker, { type: "reject", simplexKey: e, nodeIds: t, weight: -0.3 }),
      this.onInteraction?.(this.interactionTracker));
  }
  logPromote(e, t) {
    (W(this.interactionTracker, { type: "promote", simplexKey: e, nodeIds: t, weight: 0.8 }),
      this.onInteraction?.(this.interactionTracker));
  }
  logDissolve(e, t) {
    (W(this.interactionTracker, { type: "dissolve", simplexKey: e, nodeIds: t, weight: -0.5 }),
      this.onInteraction?.(this.interactionTracker));
  }
  logCreate(e) {
    (W(this.interactionTracker, { type: "create", nodeIds: e, weight: 0.6 }),
      this.onInteraction?.(this.interactionTracker));
  }
  getFocusState() {
    let e = new Set(),
      t = new Set(),
      n = new Set(),
      i = this.lockedNodeId ?? this.hoveredNodeId;
    (i &&
      (e.add(i),
      this.model.getSimplicesForNode(i).forEach((o) => {
        (t.add(N(o.nodes)), o.nodes.forEach((a) => e.add(a)));
      }),
      this.model.getHyperedgesForNode(i).forEach((o) => {
        (n.add(P("hyperedge", o.nodes)), o.nodes.forEach((a) => e.add(a)));
      })),
      this.hoveredSimplexKey &&
        (t.add(this.hoveredSimplexKey), this.model.getSimplex(this.hoveredSimplexKey)?.nodes.forEach((o) => e.add(o))),
      this.hoveredHyperedgeKey &&
        (n.add(this.hoveredHyperedgeKey),
        this.model.getHyperedge(this.hoveredHyperedgeKey)?.nodes.forEach((o) => e.add(o))));
    let s = (o, a) => {
      if (o.kind === "hyperedge") {
        let d = a ?? P("hyperedge", o.nodes);
        return n.has(d) || o.nodes.some((p) => e.has(p));
      }
      let l = a ?? N(o.nodes);
      return t.has(l) || o.nodes.some((d) => e.has(d));
    };
    return {
      isActive: e.size > 0 || t.size > 0 || n.size > 0,
      lockedNodeId: this.lockedNodeId,
      hoveredNodeId: this.hoveredNodeId,
      hoveredSimplexKey: this.hoveredSimplexKey,
      hoveredHyperedgeKey: this.hoveredHyperedgeKey,
      activeNodeIds: e,
      activeSimplexKeys: t,
      activeHyperedgeKeys: n,
      involvesNode: (o) => e.has(o),
      involvesSimplex: (o, a) => s({ kind: "simplex", ...o }, a),
      involvesRelation: s,
    };
  }
  updateHover(e, t, n, i = 1e3) {
    this.onWake?.();
    let s = n.find((o) => (o.px - e) ** 2 + (o.py - t) ** 2 <= 400)?.id ?? null;
    s !== this.hoveredNodeId && ((this.hoveredNodeId = s), this.scheduleHoverIntent(i));
  }
  clearFocus() {
    (this.clearHoverIntent(),
      (this.hoveredNodeId = null),
      (this.hoveredSimplexKey = null),
      (this.hoveredHyperedgeKey = null),
      (this.lockedNodeId = null),
      this.onSelection?.(null));
  }
  focusHoveredNode() {
    this.lockedNodeId = this.hoveredNodeId;
  }
  onMousedown(e, t, n) {
    let i = this.model.nodes.get(e);
    i &&
      ((this.pressedNodeId = e),
      (this.draggingNodeId = null),
      (this.draggedNodeWasPinned = i.isPinned),
      (this.dragOffsetX = t - i.px),
      (this.dragOffsetY = n - i.py),
      (this.pressX = t),
      (this.pressY = n),
      (this.movedDuringPointerDown = !1),
      (this.holdTimer = window.setTimeout(() => {
        ((this.holdNode = e), this.onWake?.());
      }, 200)));
  }
  onPointerMove(e, t) {
    if (!this.pressedNodeId || !this.model.nodes.get(this.pressedNodeId)) return !1;
    let i = (e - this.pressX) ** 2 + (t - this.pressY) ** 2;
    if (
      (i > 1 && (this.clearHoldTimer(), (this.holdNode = null)),
      i > 1 && ((this.movedDuringPointerDown = !0), (this.draggingNodeId = this.pressedNodeId)),
      !this.draggingNodeId)
    )
      return !1;
    ((this.hoveredNodeId = this.draggingNodeId),
      this.model.setPinnedState(
        this.draggingNodeId,
        this.draggedNodeWasPinned,
        e - this.dragOffsetX,
        t - this.dragOffsetY,
      ));
    let o = this.model.nodes.get(this.draggingNodeId);
    return (o && ((o.vx = 0), (o.vy = 0)), this.onWake?.(), !0);
  }
  onMouseup() {
    let e = this.movedDuringPointerDown && !!this.draggingNodeId;
    return (
      this.clearHoldTimer(),
      (this.pressedNodeId = null),
      (this.draggingNodeId = null),
      (this.draggedNodeWasPinned = !1),
      (this.movedDuringPointerDown = !1),
      (this.holdNode = null),
      e && this.onWake?.(),
      e
    );
  }
  clearHoldTimer() {
    this.holdTimer !== null && (window.clearTimeout(this.holdTimer), (this.holdTimer = null));
  }
  cancelHoverSelection() {
    this.clearHoverIntent();
  }
  togglePin(e) {
    let t = this.model.nodes.get(e);
    return t
      ? (this.model.setPinnedState(e, !t.isPinned, t.px, t.py),
        this.onWake?.(),
        this.onPinnedStateChanged?.(),
        !t.isPinned)
      : !1;
  }
  selectSimplex(e) {
    if (
      (this.clearHoverIntent(),
      (this.hoveredSimplexKey = e),
      (this.hoveredHyperedgeKey = null),
      this.onSelection?.(e ? { kind: "simplex", key: e } : null),
      e)
    ) {
      let t = this.model.getSimplex(e);
      t && W(this.interactionTracker, { type: "select", simplexKey: e, nodeIds: t.nodes, weight: 0.1 });
    }
  }
  selectHyperedge(e) {
    if (
      (this.clearHoverIntent(),
      (this.hoveredHyperedgeKey = e),
      (this.hoveredSimplexKey = null),
      this.onSelection?.(e ? { kind: "hyperedge", key: e } : null),
      e)
    ) {
      let t = this.model.getHyperedge(e);
      t && W(this.interactionTracker, { type: "select", simplexKey: e, nodeIds: t.nodes, weight: 0.1 });
    }
  }
  selectRelation(e) {
    if (!e) {
      this.selectSimplex(null);
      return;
    }
    e.kind === "hyperedge" ? this.selectHyperedge(e.key) : this.selectSimplex(e.key);
  }
  lerpAlpha(e, t) {
    let n = !t.isActive || t.involvesNode(e.id) ? 1 : 0.2;
    e.displayAlpha = ui(e.displayAlpha, n, 0.12);
  }
  scheduleHoverIntent(e) {
    if ((this.clearHoverIntent(), !this.hoveredNodeId)) {
      this.onHoverIntent?.(null);
      return;
    }
    this.hoverIntentTimer = window.setTimeout(() => {
      let t = this.model.getSimplicesForNode(this.hoveredNodeId).sort((i, s) => (s.weight ?? 1) - (i.weight ?? 1))[0],
        n = t ? N(t.nodes) : null;
      ((this.hoveredSimplexKey = n), this.onHoverIntent?.(n ? { kind: "simplex", key: n } : null));
    }, e);
  }
  clearHoverIntent() {
    this.hoverIntentTimer !== null && (window.clearTimeout(this.hoverIntentTimer), (this.hoverIntentTimer = null));
  }
};
function Yt(r, e) {
  return r < e ? `${r}|${e}` : `${e}|${r}`;
}
function pi(r) {
  let e = 2166136261;
  for (let t = 0; t < r.length; t++) ((e ^= r.charCodeAt(t)), (e = Math.imul(e, 16777619)));
  return (e >>> 0) / 4294967295;
}
var Ye = class r {
    constructor(e, t, n, i) {
      this.mass = 0;
      this.cx = 0;
      this.cy = 0;
      this.children = [null, null, null, null];
      this.node = null;
      ((this.x = e), (this.y = t), (this.width = n), (this.height = i));
    }
    insert(e) {
      if (this.node === null && this.children[0] === null) {
        ((this.node = e), (this.mass = 1), (this.cx = e.px), (this.cy = e.py));
        return;
      }
      if (this.node !== null) {
        this.subdivide();
        let t = this.node;
        ((this.node = null), this.insertIntoChildren(t), this.insertIntoChildren(e));
      } else this.insertIntoChildren(e);
      this.updateCenterOfMass();
    }
    subdivide() {
      let e = this.width / 2,
        t = this.height / 2;
      ((this.children[0] = new r(this.x, this.y, e, t)),
        (this.children[1] = new r(this.x + e, this.y, e, t)),
        (this.children[2] = new r(this.x, this.y + t, e, t)),
        (this.children[3] = new r(this.x + e, this.y + t, e, t)));
    }
    insertIntoChildren(e) {
      let t = this.width / 2,
        n = this.height / 2;
      e.px < this.x + t
        ? e.py < this.y + n
          ? this.children[0].insert(e)
          : this.children[2].insert(e)
        : e.py < this.y + n
          ? this.children[1].insert(e)
          : this.children[3].insert(e);
    }
    updateCenterOfMass() {
      let e = 0,
        t = 0,
        n = 0;
      for (let i of this.children) i && ((e += i.mass), (t += i.cx * i.mass), (n += i.cy * i.mass));
      ((this.mass = e), e > 0 && ((this.cx = t / e), (this.cy = n / e)));
    }
    calculateForce(e, t, n) {
      if (this.mass === 0) return { fx: 0, fy: 0 };
      let i = e.px - this.cx,
        s = e.py - this.cy,
        o = Math.hypot(i, s) || 1,
        a = o * o + 1;
      if (this.node === e && this.children[0] === null) return { fx: 0, fy: 0 };
      if (this.width / o < t || this.node !== null) {
        let l = (n * this.mass) / a;
        return { fx: (l * i) / o, fy: (l * s) / o };
      } else {
        let l = 0,
          d = 0;
        for (let p of this.children)
          if (p) {
            let c = p.calculateForce(e, t, n);
            ((l += c.fx), (d += c.fy));
          }
        return { fx: l, fy: d };
      }
    }
  },
  me = class {
    constructor() {
      this.MIN_NODE_SEPARATION = 72;
      this.REPULSION = 1800;
      this.COHESION = 0.005;
      this.GRAVITY = 7e-4;
      this.NOISE = 0.06;
      this.DAMPING = 0.9;
      this.SLEEP_THRESHOLD = 0.02;
      this.BOUNDARY_PADDING = 50;
      this.SPARSE_EDGE_LENGTH = 150;
      this.SPARSE_GRAVITY_BOOST = 1.8;
      this.BARNES_HUT_THETA = 0.65;
      this.MAX_VELOCITY = 30;
      this.USE_BARNES_HUT = !0;
      this.isAsleep = !1;
      this.animFrame = null;
      this.renderFn = null;
      this.getState = null;
    }
    configure(e) {
      (e.noiseAmount !== void 0 && (this.NOISE = e.noiseAmount),
        e.sleepThreshold !== void 0 && (this.SLEEP_THRESHOLD = e.sleepThreshold),
        e.repulsionStrength !== void 0 && (this.REPULSION = e.repulsionStrength),
        e.cohesionStrength !== void 0 && (this.COHESION = e.cohesionStrength),
        e.gravityStrength !== void 0 && (this.GRAVITY = e.gravityStrength),
        e.dampingFactor !== void 0 && (this.DAMPING = e.dampingFactor),
        e.boundaryPadding !== void 0 && (this.BOUNDARY_PADDING = e.boundaryPadding),
        e.sparseEdgeLength !== void 0 && (this.SPARSE_EDGE_LENGTH = e.sparseEdgeLength),
        e.sparseGravityBoost !== void 0 && (this.SPARSE_GRAVITY_BOOST = e.sparseGravityBoost));
    }
    start(e, t) {
      ((this.renderFn = e),
        (this.getState = t),
        this.animFrame !== null && window.cancelAnimationFrame(this.animFrame));
      let n = () => {
        let { nodes: i, simplices: s, hyperedges: o, bounds: a, holdNode: l } = t();
        (this.tick(i, s, a, l, o), e(), this.isAsleep || (this.animFrame = window.requestAnimationFrame(n)));
      };
      ((this.isAsleep = !1), (this.animFrame = window.requestAnimationFrame(n)));
    }
    stop() {
      (this.animFrame !== null && window.cancelAnimationFrame(this.animFrame),
        (this.animFrame = null),
        (this.isAsleep = !0));
    }
    wake() {
      !this.isAsleep ||
        !this.renderFn ||
        !this.getState ||
        ((this.isAsleep = !1), this.start(this.renderFn, this.getState));
    }
    tick(e, t, n, i, s = []) {
      let a =
          t.filter((m) => m.nodes.length === 2).length > 0 &&
          t.every((m) => m.nodes.length <= 2 || m.inferred) &&
          !s.some((m) => m.nodes.length >= 3),
        l = new Map(e.map((m) => [m.id, m])),
        d = new Map(),
        p = new Map();
      (e.forEach((m) => {
        p.set(m.id, { edgeCount: 0, clusterCount: 0, maxDim: 0 });
      }),
        t.forEach((m) => {
          let v = m.nodes.length - 1,
            y = m.weight ?? 1,
            b = m.nodes.length === 2 ? 1 : 1 + Math.min(1.2, (m.nodes.length - 2) * 0.4);
          m.nodes.forEach((S) => {
            let w = p.get(S);
            w &&
              (m.nodes.length === 2 && w.edgeCount++,
              m.nodes.length >= 3 && w.clusterCount++,
              (w.maxDim = Math.max(w.maxDim, v)));
          });
          for (let S = 0; S < m.nodes.length; S++)
            for (let w = S + 1; w < m.nodes.length; w++) {
              let E = Yt(m.nodes[S], m.nodes[w]),
                I = (d.get(E) ?? 0) + y * b;
              d.set(E, Math.min(3.5, I));
            }
        }));
      let c = e.length > 0 ? this.calculateBounds(e) : { x: 0, y: 0, width: 1e3, height: 1e3 },
        u = new Ye(c.x, c.y, c.width, c.height);
      (e.forEach((m) => u.insert(m)),
        e.forEach((m) => {
          if (m.isPinned) return;
          let v = u.calculateForce(m, this.BARNES_HUT_THETA, this.REPULSION);
          ((m.vx += v.fx), (m.vy += v.fy));
        }));
      for (let m = 0; m < e.length; m++)
        for (let v = m + 1; v < e.length; v++) {
          let y = e[m],
            b = e[v],
            S = b.px - y.px,
            w = b.py - y.py,
            E = Math.hypot(S, w) || 1,
            I = d.get(Yt(y.id, b.id)) ?? 0,
            D = S / E,
            R = w / E,
            Te = Math.max(0, this.MIN_NODE_SEPARATION - E);
          if (Te > 0 && !this.USE_BARNES_HUT) {
            let O = Te * this.COHESION * 0.55;
            (y.isPinned || ((y.vx -= O * D), (y.vy -= O * R)), b.isPinned || ((b.vx += O * D), (b.vy += O * R)));
          }
          if (I > 0) {
            let O = Math.min(1.6, I),
              se = Math.max(this.MIN_NODE_SEPARATION * 1.05, this.SPARSE_EDGE_LENGTH * (1.08 - O * 0.2)),
              Y = (E - se) * this.COHESION * 0.16 * (1 + I * 0.9),
              Qe = se * 0.68,
              $ = Math.max(0, Qe - E) * this.COHESION * 0.28 * (1 + I * 1.1);
            (y.isPinned || ((y.vx += Y * D - $ * D), (y.vy += Y * R - $ * R)),
              b.isPinned || ((b.vx -= Y * D - $ * D), (b.vy -= Y * R - $ * R)));
          }
        }
      if (
        (t.forEach((m) => {
          if (m.nodes.length < 3) return;
          let v = m.nodes.map((w) => l.get(w)).filter(Boolean);
          if (!v.length) return;
          let y = v.reduce((w, E) => w + E.px, 0) / v.length,
            b = v.reduce((w, E) => w + E.py, 0) / v.length,
            S = m.weight ?? 1;
          v.forEach((w) => {
            w.isPinned ||
              ((w.vx += (y - w.px) * this.COHESION * S * 0.75), (w.vy += (b - w.py) * this.COHESION * S * 0.75));
          });
        }),
        s.forEach((m) => {
          let v = m.nodes.map((w) => l.get(w)).filter(Boolean);
          if (v.length < 2) return;
          let y = v.reduce((w, E) => w + E.px, 0) / v.length,
            b = v.reduce((w, E) => w + E.py, 0) / v.length,
            S = m.weight ?? 1;
          v.forEach((w) => {
            w.isPinned ||
              ((w.vx += (y - w.px) * this.COHESION * S * 0.5), (w.vy += (b - w.py) * this.COHESION * S * 0.5));
          });
        }),
        i)
      ) {
        let m = l.get(i);
        m &&
          e.forEach((v) => {
            if (v === m || v.isPinned) return;
            let y = v.px - m.px,
              b = v.py - m.py,
              S = y * y + b * b + 1;
            ((v.vx += (y / S) * 900), (v.vy += (b / S) * 900));
          });
      }
      let h =
        e.length > 0
          ? { x: e.reduce((m, v) => m + v.px, 0) / e.length, y: e.reduce((m, v) => m + v.py, 0) / e.length }
          : { x: 0, y: 0 };
      e.forEach((m) => {
        if (m.isPinned) return;
        let v = p.get(m.id) ?? { edgeCount: 0, clusterCount: 0, maxDim: 0 },
          y = a ? this.GRAVITY * this.SPARSE_GRAVITY_BOOST : this.GRAVITY,
          b = v.clusterCount > 0 ? 1.2 : v.edgeCount > 0 ? 0.7 : 0.08,
          S = v.clusterCount > 0 ? 0.14 : v.edgeCount > 0 ? 0.08 : 0.01;
        if (
          ((m.vx += (0 - m.px) * y * b + (h.x - m.px) * y * S + (Math.random() - 0.5) * this.NOISE),
          (m.vy += (0 - m.py) * y * b + (h.y - m.py) * y * S + (Math.random() - 0.5) * this.NOISE),
          v.edgeCount === 0 && v.clusterCount === 0)
        ) {
          let E = pi(m.id) * Math.PI * 2,
            I = Math.max(this.SPARSE_EDGE_LENGTH * 2.8, 320),
            D = Math.cos(E) * I,
            R = Math.sin(E) * I;
          ((m.vx += (D - m.px) * y * 0.22), (m.vy += (R - m.py) * y * 0.22));
        }
        ((m.vx *= this.DAMPING), (m.vy *= this.DAMPING));
        let w = Math.hypot(m.vx, m.vy);
        if (w > this.MAX_VELOCITY) {
          let E = this.MAX_VELOCITY / w;
          ((m.vx *= E), (m.vy *= E));
        }
        ((m.px += m.vx), (m.py += m.vy));
      });
      let g = e.reduce((m, v) => m + v.vx * v.vx + v.vy * v.vy, 0);
      (e.length > 0 ? g / e.length : 0) < this.SLEEP_THRESHOLD && (this.isAsleep = !0);
    }
    calculateBounds(e) {
      if (e.length === 0) return { x: -500, y: -500, width: 1e3, height: 1e3 };
      let t = 1 / 0,
        n = 1 / 0,
        i = -1 / 0,
        s = -1 / 0;
      e.forEach((d) => {
        ((t = Math.min(t, d.px)), (n = Math.min(n, d.py)), (i = Math.max(i, d.px)), (s = Math.max(s, d.py)));
      });
      let o = 100,
        a = Math.max(i - t + 2 * o, 1e3),
        l = Math.max(s - n + 2 * o, 1e3);
      return { x: t - o, y: n - o, width: a, height: l };
    }
  };
var A = {
  purple: [127, 119, 221],
  teal: [29, 158, 117],
  coral: [216, 90, 48],
  pink: [208, 98, 156],
  blue: [76, 125, 212],
  amber: [214, 151, 52],
  neutral: [136, 135, 128],
  "default-purple": [127, 119, 221],
  "default-teal": [29, 158, 117],
  "default-coral": [216, 90, 48],
};
function gi(r) {
  let e = r.inferredSignals ?? [];
  return e.includes("soft-cluster")
    ? A.teal
    : e.some((t) => t.startsWith("tags:"))
      ? A.amber
      : e.some((t) => t === "folder:same" || t === "folder:top")
        ? A.pink
        : e.some((t) => t.startsWith("title:") || t.startsWith("content:"))
          ? A.coral
          : e.some((t) => t.startsWith("link:"))
            ? A.blue
            : A.neutral;
}
function Zt(r) {
  return `${r.nodes.join("|")}|${r.label ?? ""}|${r.sourcePath ?? ""}`;
}
function Jt(r, e) {
  let n = 0.82 + (_(Zt(e)) % 29) / 100;
  return r.map((i) => Math.max(0, Math.min(255, Math.round(i * n))));
}
function Xt(r) {
  let e = Math.max(2, r.nodes.length),
    t = [A.blue, A.teal, A.coral, A.pink, A.amber, A.purple],
    n = _(Zt(r)),
    s = (((e - 2) % t.length) + (n % t.length)) % t.length;
  return Jt(t[s], r);
}
function Ut(r, e) {
  return r.every((t) => e.includes(t));
}
function qt(r) {
  if (r.inferred) {
    let e = gi(r);
    return r.nodes.length >= 3 ? Xt(r) : Jt(e, r);
  }
  return Xt(r);
}
function mi(r, e) {
  let t = [...r.simplices.values()].filter((s) => s.nodes.length > e.nodes.length && Ut(e.nodes, s.nodes));
  if (!t.length) return null;
  let n = t.sort((s, o) => o.nodes.length - s.nodes.length || s.nodes.join("|").localeCompare(o.nodes.join("|")))[0];
  return t.some((s) => !Ut(s.nodes, n.nodes)) ? null : n;
}
function H(r, e) {
  if (!r) return qt(e);
  let t = mi(r, e);
  return qt(t ?? e);
}
var Qt = new Map();
function Ue(r, e) {
  return r.nodes.map((t) => e.find((n) => n.id === t)).filter(Boolean);
}
function en(r) {
  return { x: r.reduce((e, t) => e + t.x, 0) / r.length, y: r.reduce((e, t) => e + t.y, 0) / r.length };
}
function fi(r) {
  let e = en(r);
  return [...r].sort((t, n) => Math.atan2(t.y - e.y, t.x - e.x) - Math.atan2(n.y - e.y, n.x - e.x));
}
function yi(r, e) {
  let t = en(r);
  return r.map((n) => {
    let i = n.x - t.x,
      s = n.y - t.y,
      o = Math.hypot(i, s) || 1;
    return { x: n.x + (i / o) * e, y: n.y + (s / o) * e };
  });
}
function vi(r, e) {
  let t = e.length;
  r.beginPath();
  for (let n = 0; n < t; n++) {
    let i = e[(n - 1 + t) % t],
      s = e[n],
      o = e[(n + 1) % t],
      a = e[(n + 2) % t],
      l = 0.45,
      d = s.x + ((o.x - i.x) * l) / 2,
      p = s.y + ((o.y - i.y) * l) / 2,
      c = o.x - ((a.x - s.x) * l) / 2,
      u = o.y - ((a.y - s.y) * l) / 2;
    (n === 0 && r.moveTo(s.x, s.y), r.bezierCurveTo(d, p, c, u, o.x, o.y));
  }
  r.closePath();
}
function Xe(r, e, t, n) {
  let i = t.x - e.x,
    s = t.y - e.y,
    o = Math.hypot(i, s) || 1,
    a = (-s / o) * n,
    l = (i / o) * n,
    d = Math.atan2(s, i);
  (r.beginPath(),
    r.moveTo(e.x + a, e.y + l),
    r.lineTo(t.x + a, t.y + l),
    r.arc(t.x, t.y, n, d - Math.PI / 2, d + Math.PI / 2),
    r.lineTo(e.x - a, e.y - l),
    r.arc(e.x, e.y, n, d + Math.PI / 2, d - Math.PI / 2),
    r.closePath());
}
function tn(r) {
  if (r.length < 3) return !0;
  let [e, t] = r;
  for (let n = 2; n < r.length; n++) {
    let i = r[n],
      s = (t.x - e.x) * (i.y - e.y) - (t.y - e.y) * (i.x - e.x);
    if (Math.abs(s) > 0.001) return !1;
  }
  return !0;
}
function bi(r, e, t, n) {
  let i = r,
    s = Ue(e, t);
  if (!s.length) return null;
  let o = s.map((y) => ({ x: y.px, y: y.py })),
    a = Qt.get(i);
  if (a && o.every((y, b) => Math.hypot(y.x - a.positions[b].x, y.y - a.positions[b].y) <= 2)) return a;
  let l = n * 2.5,
    d = s.map((y) => y.px),
    p = s.map((y) => y.py),
    c = Math.min(...d) - l,
    u = Math.min(...p) - l,
    h = Math.max(...d) - c + l,
    g = Math.max(...p) - u + l,
    f = activeDocument.createElement("canvas");
  ((f.width = Math.max(1, Math.ceil(h))), (f.height = Math.max(1, Math.ceil(g))));
  let m = f.getContext("2d");
  if (!m) return null;
  if ((m.translate(-c, -u), (m.fillStyle = "#ffffff"), s.length === 1))
    (m.beginPath(), m.arc(s[0].px, s[0].py, n, 0, Math.PI * 2), m.fill());
  else if (tn(o)) (Xe(m, o[0], o[o.length - 1], n), m.fill());
  else
    for (let y = 0; y < s.length; y++) {
      for (let b = y + 1; b < s.length; b++)
        (Xe(m, { x: s[y].px, y: s[y].py }, { x: s[b].px, y: s[b].py }, n), m.fill());
      (m.beginPath(), m.arc(s[y].px, s[y].py, n, 0, Math.PI * 2), m.fill());
    }
  let v = { canvas: f, x: c, y: u, positions: o };
  return (Qt.set(i, v), v);
}
var Si = 8;
function nn(r, e, t, n, i, s) {
  let o = Ue(e, t);
  if (o.length < 2) return;
  let [a, l, d] = n,
    p = i * (s ? 1 : 0.35),
    c = o.map((h) => ({ x: h.px, y: h.py })),
    u = 30 + (e.weight ?? 1) * 14;
  if (
    (r.save(),
    (r.lineJoin = "round"),
    r.setLineDash([9, 7]),
    (r.lineWidth = s ? 2 : 1.4),
    (r.strokeStyle = `rgba(${a},${l},${d},${Math.min(1, p * 1.5)})`),
    (r.fillStyle = `rgba(${a},${l},${d},${p * 0.12})`),
    o.length > Si)
  ) {
    (c.forEach((h) => {
      (r.beginPath(), r.arc(h.x, h.y, u * 0.42, 0, Math.PI * 2), r.stroke());
    }),
      r.restore());
    return;
  }
  (o.length === 2 || tn(c) ? Xe(r, c[0], c[c.length - 1], u) : vi(r, yi(fi(c), u)),
    r.fill(),
    r.stroke(),
    r.setLineDash([]),
    (r.fillStyle = `rgba(${a},${l},${d},${Math.min(1, p * 1.2)})`),
    c.forEach((h) => {
      (r.beginPath(), r.arc(h.x, h.y, s ? 3.4 : 2.6, 0, Math.PI * 2), r.fill());
    }),
    r.restore());
}
function sn(r, e, t, n, i, s, o) {
  if (!Ue(t, i).length) return;
  let [l, d, p] = H(n, t),
    c = 36 + (t.weight ?? 1) * 24 + (t.nodes.length - 1 === 3 ? 20 : 0),
    u = o.isActive ? (o.involvesSimplex(t, e) ? s : s * 0.18) : s,
    h = bi(`s:${e}`, t, i, c);
  if (!h) return;
  [
    { alpha: u * 0.15, blur: 18 },
    { alpha: u * 0.42, blur: 12 },
    { alpha: u, blur: 8 },
  ].forEach((f) => {
    (r.save(),
      (r.globalAlpha = f.alpha),
      (r.filter = `blur(${f.blur}px)`),
      r.drawImage(h.canvas, h.x, h.y),
      (r.filter = "none"),
      (r.globalCompositeOperation = "source-atop"),
      (r.fillStyle = `rgb(${l},${d},${p})`),
      r.fillRect(h.x, h.y, h.canvas.width, h.canvas.height),
      (r.globalCompositeOperation = "source-over"),
      r.restore());
  });
}
function rn(r, e, t, n, i, s) {
  if (!i) return;
  let o = new Set();
  e.forEach((a) => {
    let l = a.nodes.map((h) => n.get(h)).filter(Boolean),
      d = !s.isActive || s.involvesSimplex(a),
      [p, c, u] = H(t, a);
    for (let h = 0; h < l.length; h++)
      for (let g = h + 1; g < l.length; g++) {
        let f = [l[h].id, l[g].id].sort().join("|");
        o.has(f) ||
          (o.add(f),
          (r.strokeStyle = `rgba(${p},${c},${u},${d ? 0.22 : 0.06})`),
          (r.lineWidth = d ? 1 : 0.5),
          r.beginPath(),
          r.moveTo(l[h].px, l[h].py),
          r.lineTo(l[g].px, l[g].py),
          r.stroke());
      }
  });
}
var fe = 10;
function on(r) {
  let e = r.readCrossLayerCache();
  if (e) return e;
  let t = new Map(),
    n = new Map();
  return (
    r.hyperedges.forEach((i, s) => {
      let o = i.nodes,
        a = o.length,
        l = a <= fe ? 2 ** a - a - 1 : Number.POSITIVE_INFINITY;
      if (a > fe) {
        t.set(s, { key: s, nodes: o, presentFaces: [], missingFaces: [], impliedFaceCount: l, unbounded: !0 });
        return;
      }
      let d = [],
        p = [];
      for (let c = 2; c <= a; c++)
        for (let u of Z(o, c)) {
          let h = N(u);
          if (r.simplices.has(h)) {
            d.push(h);
            let g = n.get(h) ?? [];
            (g.push(s), n.set(h, g));
          } else p.push(u);
        }
      t.set(s, { key: s, nodes: o, presentFaces: d, missingFaces: p, impliedFaceCount: l, unbounded: !1 });
    }),
    r.writeCrossLayerCache({ hyperedges: t, simplexCoveredBy: n })
  );
}
var xi = 1e3 * 60 * 60 * 24;
function ie(r) {
  return Number.isNaN(r) ? 0 : Math.max(0, Math.min(1, r));
}
function an(r, e) {
  let t = on(r).hyperedges.get(e);
  if (!t) return null;
  if (t.unbounded)
    return {
      key: e,
      nodes: t.nodes,
      missingFaces: [],
      missingCount: 0,
      impliedFaceCount: t.impliedFaceCount,
      deficit: null,
      unbounded: !0,
    };
  let n = t.missingFaces.length;
  return {
    key: e,
    nodes: t.nodes,
    missingFaces: t.missingFaces,
    missingCount: n,
    impliedFaceCount: t.impliedFaceCount,
    deficit: t.impliedFaceCount > 0 ? ie(n / t.impliedFaceCount) : 0,
    unbounded: !1,
  };
}
function ln(r) {
  let e = new Map(),
    t = 0;
  r.hyperedges.forEach((s, o) => {
    let a = an(r, o);
    if (!a || a.unbounded) {
      (t++, e.set(o, null));
      return;
    }
    e.set(o, a.deficit);
  });
  let n = [...e.values()].filter((s) => s !== null),
    i = wi(r).map((s) => {
      let o = s.encounters.map((a) => e.get(a)).filter((a) => a != null);
      return { ...s, simpliciality: o.length > 0 ? 1 - o.reduce((a, l) => a + l, 0) / o.length : null };
    });
  return {
    value: n.length > 0 ? 1 - n.reduce((s, o) => s + o, 0) / n.length : null,
    measuredEncounters: n.length,
    unboundedEncounters: t,
    components: i,
  };
}
function wi(r) {
  let e = new Map(),
    t = (i) => {
      let s = i;
      for (; e.get(s) !== s; ) s = e.get(s);
      let o = i;
      for (; e.get(o) !== s; ) {
        let a = e.get(o);
        (e.set(o, s), (o = a));
      }
      return s;
    };
  (r.hyperedges.forEach((i) => {
    i.nodes.forEach((s) => {
      e.has(s) || e.set(s, s);
    });
  }),
    r.hyperedges.forEach((i) => {
      let [s, ...o] = i.nodes;
      s !== void 0 &&
        o.forEach((a) => {
          let l = t(s),
            d = t(a);
          l !== d && e.set(d, l);
        });
    }));
  let n = new Map();
  return (
    e.forEach((i, s) => {
      let o = t(s),
        a = n.get(o) ?? { nodes: [], encounters: [] };
      (a.nodes.push(s), n.set(o, a));
    }),
    r.hyperedges.forEach((i, s) => {
      let o = i.nodes[0];
      o !== void 0 && n.get(t(o))?.encounters.push(s);
    }),
    [...n.values()].map((i) => ({ nodes: i.nodes.sort(), encounters: i.encounters.sort() }))
  );
}
function Ci(r, e, t) {
  let n = r.getHyperedge(e);
  if (!n) return null;
  let i = n.nodes;
  if (i.length > fe)
    return {
      key: e,
      independence: null,
      fullSetScore: 0,
      maxSubsetScore: 0,
      meanSubsetScore: 0,
      strongestSubset: null,
      evaluatedSubsets: 0,
      unbounded: !0,
    };
  let s = ie(t(i)),
    o = 0,
    a = 0,
    l = 0,
    d = null;
  for (let p = 2; p < i.length; p++)
    for (let c of Z(i, p)) {
      let u = ie(t(c));
      ((a += u), l++, u > o && ((o = u), (d = c)));
    }
  return {
    key: e,
    independence: l > 0 ? ie(1 - o) : null,
    fullSetScore: s,
    maxSubsetScore: o,
    meanSubsetScore: l > 0 ? a / l : 0,
    strongestSubset: d,
    evaluatedSubsets: l,
    unbounded: !1,
  };
}
function Ni(r, e, t = Date.now()) {
  return r.length === 0 || e <= 0
    ? 0
    : r.reduce((n, i) => {
        let s = Math.max(0, (t - i) / xi);
        return n + Math.pow(0.5, s / e);
      }, 0);
}
function Mi(r, e) {
  let t = r.getHyperedgesForNode(e),
    n = t.length;
  if (n < 2) return { nodeId: e, incidentEncounters: n, meanJaccard: n === 1 ? 1 : 0, pressure: 0 };
  let i = 0,
    s = 0;
  for (let a = 0; a < n; a++)
    for (let l = a + 1; l < n; l++) {
      let d = new Set(t[a].nodes),
        p = new Set(t[l].nodes),
        c = 0;
      p.forEach((h) => {
        d.has(h) && c++;
      });
      let u = d.size + p.size - c;
      ((i += u > 0 ? c / u : 0), s++);
    }
  let o = s > 0 ? i / s : 0;
  return { nodeId: e, incidentEncounters: n, meanJaccard: o, pressure: ie((1 - o) * (1 - 1 / n)) };
}
function dn(r, e, t = {}) {
  let n = r.getHyperedge(e);
  if (!n) return null;
  let i = t.occurrences ?? n.occurrences ?? [],
    o = n.nodes.map((a) => Mi(r, a)).reduce((a, l) => (a === null || l.pressure > a.pressure ? l : a), null);
  return {
    key: e,
    nodes: n.nodes,
    closure: an(r, e),
    independence: t.score ? Ci(r, e, t.score) : null,
    peakOverlap: o,
    occurrences: i,
    vitality: Ni(i, t.halfLifeDays ?? 90, t.now),
    persistence: n.persistence === "recurring" ? "recurring" : "momentary",
  };
}
function cn(r, e, t, n, i, s = null) {
  let a = r.measureText(e).width + 20,
    l = 26;
  return (
    (r.fillStyle = i ? "rgba(20, 24, 32, 0.85)" : "rgba(255, 255, 255, 0.85)"),
    r.beginPath(),
    r.roundRect(t, n, a, l, 8),
    r.fill(),
    (r.strokeStyle = s ?? (i ? "rgba(255, 255, 255, 0.15)" : "rgba(0, 0, 0, 0.1)")),
    (r.lineWidth = 1),
    r.stroke(),
    (r.fillStyle = i ? "rgba(235, 240, 248, 0.9)" : "rgba(24, 28, 34, 0.85)"),
    (r.textAlign = "left"),
    (r.textBaseline = "middle"),
    r.fillText(e, t + 10, n + l / 2),
    l
  );
}
function hn(r, e, t) {
  let n = e.getAnalysisSummary();
  if (!n.betti) return;
  let { b0: i, b1: s, b2: o } = n.betti,
    a = `\u03B2\u2080 ${i}   \u03B2\u2081 ${s}${o !== void 0 ? `   \u03B2\u2082 ${o}` : ""}`;
  (r.save(), (r.font = "500 14px ui-monospace, SFMono-Regular, Menlo, monospace"));
  let l = cn(r, a, 14, 14, t);
  r.restore();
}
function un(r, e, t, n = 14) {
  let i = e.hyperedges.size;
  if (i === 0) return;
  let s = ln(e),
    o = s.value === null ? "\u2013" : s.value.toFixed(2),
    a = e.getAnalysisSummary().recurringEncounterCount,
    l = `\u25C7 ${i}${a > 0 ? ` \xB7 ${a} recurring` : ""}   simpliciality ${o}`;
  (r.save(),
    (r.font = "500 14px ui-monospace, SFMono-Regular, Menlo, monospace"),
    cn(r, l, 14, n, t, "rgba(127, 119, 221, 0.35)"),
    r.restore());
}
function pn(r, e, t, n, i) {
  let s = e.getAnalysisSummary();
  if (!s.betti?.holes?.length) return;
  let o = e.getAllNodes(),
    a = new Map(o.map((l) => [l.id, l]));
  (r.save(), r.setLineDash([8, 4]), (r.lineWidth = 1.5));
  for (let l of s.betti.holes) Ei(r, l, a, t, n, i);
  r.restore();
}
function Ei(r, e, t, n, i, s) {
  let o = e.boundaryNodes.map((S) => t.get(S)).filter(Boolean);
  if (o.length < 3) return;
  let a = 200,
    l = o.filter((S) => Math.hypot(S.px, S.py) > a);
  if (l.length < o.length) return;
  let d = l.reduce((S, w) => ({ x: S.x + w.px, y: S.y + w.py }), { x: 0, y: 0 });
  if (((d.x /= l.length), (d.y /= l.length), Math.max(...l.map((S) => Math.hypot(S.px - d.x, S.py - d.y))) < 150))
    return;
  let u = 300,
    h = Math.min(...l.map((S) => S.px)),
    g = Math.max(...l.map((S) => S.px)),
    f = Math.min(...l.map((S) => S.py)),
    m = Math.max(...l.map((S) => S.py));
  if (g < i.minX - u || h > i.maxX + u || m < i.minY - u || f > i.maxY + u) return;
  let v = e.boundaryNodes.sort().join("|"),
    y = s === v;
  ((r.strokeStyle = y ? "rgba(255, 165, 0, 0.9)" : n ? "rgba(255, 165, 0, 0.5)" : "rgba(255, 140, 0, 0.6)"),
    r.beginPath(),
    r.moveTo(l[0].px, l[0].py));
  for (let S = 1; S < l.length; S++) r.lineTo(l[S].px, l[S].py);
  (r.closePath(), r.stroke(), (r.fillStyle = y ? "rgba(255, 165, 0, 0.15)" : "rgba(255, 165, 0, 0.05)"), r.fill());
  let b = Math.sqrt(l.reduce((S, w) => S + (w.px - d.x) ** 2 + (w.py - d.y) ** 2, 0) / l.length);
  if (y || b < 200) {
    ((r.fillStyle = y ? "rgba(255, 165, 0, 0.9)" : "rgba(255, 165, 0, 0.6)"),
      r.beginPath(),
      r.arc(d.x, d.y, y ? 8 : 5, 0, Math.PI * 2),
      r.fill(),
      (r.font = "500 12px system-ui, sans-serif"),
      (r.fillStyle = n ? "rgba(255, 255, 255, 0.8)" : "rgba(0, 0, 0, 0.7)"),
      (r.textAlign = "center"),
      (r.textBaseline = "middle"));
    let S = e.dimension === 1 ? "Missing \u0394" : "Missing \u25A1";
    (r.fillText(S, d.x, d.y - 14),
      (r.font = "400 10px system-ui, sans-serif"),
      (r.fillStyle = n ? "rgba(255, 255, 255, 0.5)" : "rgba(0, 0, 0, 0.5)"),
      r.fillText(`${l.length} nodes`, d.x, d.y + 14));
  }
}
function gn(r) {
  return r.split("/").pop()?.replace(/\.md$/, "") ?? r;
}
function Ti(r) {
  let e = r.closure;
  if (!e) return null;
  if (e.unbounded) return "Too large to enumerate what it implies. The closure deficit here is unmeasured, not zero.";
  if (e.missingCount === 0)
    return "Every relation this encounter implies already exists in the complex. Promoting it would assert nothing new.";
  let t = r.nodes.length,
    n = e.deficit ?? 0;
  return n >= 0.8
    ? `This cluster looks visually coherent, but its meaning exists only at order ${t} \u2014 ${e.missingCount} of the ${e.impliedFaceCount} relations it implies are absent.`
    : n >= 0.4
      ? `${e.missingCount} of the ${e.impliedFaceCount} implied relations are absent. Part of this group is already understood; part of it is not.`
      : `Most of what this encounter implies already exists \u2014 ${e.missingCount} implied relation${e.missingCount === 1 ? " is" : "s are"} still absent.`;
}
function ki(r) {
  let e = r.independence;
  if (!e) return null;
  if (e.unbounded) return "Too large to test its subgroups against the vault.";
  if (e.independence === null)
    return "A two-note encounter has no proper subgroup, so there is nothing for it to be irreducible to.";
  let t = e.strongestSubset?.map(gn).join(" \xB7 ");
  return e.independence >= 0.75
    ? e.fullSetScore >= 0.3
      ? "The group is evidenced by the vault; no subgroup inside it is. That is what irreducible looks like."
      : "Neither the group nor any subgroup inside it is evidenced by the vault. This encounter rests on your assertion alone \u2014 which is a reason to keep it, not to promote it."
    : e.independence >= 0.4
      ? `Some of this group stands on its own${t ? ` \u2014 ${t} most of all` : ""}, but not all of it.`
      : `${t ?? "A subgroup"} is already well evidenced without the rest. This may be a simplex you have not asserted yet.`;
}
function Ii(r, e) {
  let t = Math.max(1, r.occurrences.length),
    n = `${t}\xD7`;
  return r.persistence !== "recurring"
    ? `Encountered ${n}. Recurring at ${e} \u2014 repetition is evidence, never proof.`
    : r.vitality >= t * 0.7
      ? `Recurring and still warm: ${n}, most of it recent.`
      : r.vitality < 1
        ? `Recurring, but cooled: ${n}, none of it lately.`
        : `Recurring: ${n}, some of it a while ago.`;
}
function Pi(r) {
  let e = r.peakOverlap;
  return !e || e.pressure < 0.4
    ? null
    : `${gn(e.nodeId)} sits in ${e.incidentEncounters} encounters that barely overlap each other. It may be carrying contexts that do not belong together.`;
}
function mn(r, e) {
  return {
    headline:
      "These notes came together as one irreducible whole. No pair among them is asserted to be meaningful on its own.",
    closure: Ti(r),
    independence: ki(r),
    persistence: Ii(r, e),
    overlap: Pi(r),
  };
}
function fn(r, e, t, n) {
  let i = r.nodes,
    s = i.map((a) => e.find((l) => l.id === a)).filter(Boolean),
    o = n.find((a) => a.missingSimplex.length === i.length && a.missingSimplex.every((l) => i.includes(l)));
  return r.inferred ? Di(r, s, t, o) : r.autoGenerated ? Ri(r) : Fi(r, s);
}
function Di(r, e, t, n) {
  let i = r.nodes.length - 1,
    s = e.map((l) => l.id.replace(/\.md$/, "")),
    o = r.inferredSignals ?? [];
  if (n)
    return {
      headline: `This ${i === 1 ? "edge" : i === 2 ? "triangle" : "tetrahedron"} fills a topological hole in your complex.`,
      tension: `Notes ${s.join(" \xB7 ")} formed an unfilled cycle. This structure now binds them together.`,
      prompt: "Does this new connection reveal a synthesis you hadn't consciously noticed?",
      signals: [`Fills \u03B2${n.dimension} hole`, ...o],
    };
  if (i === 2 && e.length === 3) {
    let l = Ai(e, t);
    if (l) {
      let d = s.filter((p) => p !== l.replace(/\.md$/, ""));
      return {
        headline: "Three notes form a structural bridge through a common connection.",
        tension: `${d[0]} and ${d[1]} both connect to ${l.replace(/\.md$/, "")}, but have no direct link between them.`,
        prompt: `What would a note directly relating "${d[0]}" and "${d[1]}" say?`,
        signals: ["Bridge pattern detected", ...o],
      };
    }
  }
  let a = new Set(e.map((l) => l.domain));
  return a.size > 1
    ? {
        headline: `Cross-domain ${i === 1 ? "relation" : i === 2 ? "triangle" : "synthesis"} spanning ${a.size} conceptual areas.`,
        tension: `Connects: ${[...a].join(" \xB7 ")}. These domains rarely intersect in your vault.`,
        prompt: "What insight emerges from seeing these domains together?",
        signals: [`Cross-domain: ${[...a].join(" + ")}`, ...o],
      }
    : {
        headline: `${i === 1 ? "An inferred relation" : i === 2 ? "An inferred triangle" : "An inferred higher-order structure"} among ${e.length} notes.`,
        tension: `Based on ${o.length > 0 ? o.join(" \xB7 ") : "vault structure analysis"}.`,
        prompt: "Is this a meaningful connection worth confirming?",
        signals: o.length > 0 ? o : ["Pattern-based inference"],
      };
}
function Ri(r) {
  let e = r.nodes.length - 1;
  return {
    headline: `Auto-generated face of a ${e + 1}-simplex.`,
    tension: `This ${e === 1 ? "edge" : e === 2 ? "triangle" : "tetrahedron"} is the boundary of a higher-dimensional simplex you defined.`,
    prompt: "Faces help visualize the structure but cannot be edited independently.",
    signals: [`Face of ${r.parentKey || "parent simplex"}`],
  };
}
function Fi(r, e) {
  let t = r.nodes.length - 1,
    n = e.map((i) => i.id.replace(/\.md$/, ""));
  return {
    headline: `Confirmed ${t === 1 ? "relation" : t === 2 ? "triangle" : t === 3 ? "tetrahedron" : "simplex"} you defined.`,
    tension: `You explicitly marked ${n.join(" \xB7 ")} as forming a meaningful structure.`,
    prompt: r.label
      ? `Labeled as "${r.label}". Does this label still capture the relationship?`
      : "Consider adding a label to capture what binds these notes together.",
    signals: r.label ? [`Label: ${r.label}`] : ["User-defined"],
  };
}
function Ai(r, e) {
  if (r.length !== 3) return null;
  for (let t of r) {
    let n = e.get(t.id);
    if (!n) continue;
    let i = r.filter((o) => o.id !== t.id);
    if (i.every((o) => n.outgoingLinks.has(o.id) || e.get(o.id)?.outgoingLinks.has(t.id))) {
      let [o, a] = i,
        l = e.get(o.id),
        d = e.get(a.id),
        p = l?.outgoingLinks.has(a.id) ?? !1,
        c = d?.outgoingLinks.has(o.id) ?? !1;
      if (!p && !c) return t.id;
    }
  }
  return null;
}
function qe(r, e) {
  let t = r.boundaryNodes.map((n) => n.replace(/\.md$/, ""));
  return r.dimension === 1
    ? {
        headline: "A 1-dimensional hole in your complex \u2014 an unfilled triangle.",
        tension: `Notes ${t.join(" \xB7 ")} connect pairwise, but no synthesizing structure exists at the center.`,
        prompt: "What's at the center of this triangle? Writing a note that connects all three would close this hole.",
        signals: ["\u03B2\u2081 hole: three notes, no triangle"],
      }
    : {
        headline: "A 2-dimensional void \u2014 a hollow tetrahedron.",
        tension: "Four notes form a complete boundary of triangles, but the interior is empty.",
        prompt: "What synthesis would fill this void? A note at the center would complete the structure.",
        signals: ["\u03B2\u2082 void: four notes, no tetrahedron"],
      };
}
function ye(r, e) {
  let t = !1;
  for (let n = 0, i = e.length - 1; n < e.length; i = n++) {
    let s = e[n].x,
      o = e[n].y,
      a = e[i].x,
      l = e[i].y;
    o > r.y != l > r.y && r.x < ((a - s) * (r.y - o)) / (l - o || 1e-6) + s && (t = !t);
  }
  return t;
}
function ve(r, e) {
  let t = r.nodes
    .map((i) => e.find((s) => s.id === i))
    .filter(Boolean)
    .map((i) => ({ x: i.px, y: i.py }));
  if (t.length <= 2) return t;
  let n = { x: t.reduce((i, s) => i + s.x, 0) / t.length, y: t.reduce((i, s) => i + s.y, 0) / t.length };
  return [...t].sort((i, s) => Math.atan2(i.y - n.y, i.x - n.x) - Math.atan2(s.y - n.y, s.x - n.x));
}
var be = class {
  constructor(e, t, n, i, s = {}) {
    this.model = e;
    this.engine = t;
    this.controller = n;
    this.settings = i;
    this.callbacks = s;
    this.canvas = null;
    this.ctx = null;
    this.dpr = activeWindow.devicePixelRatio || 1;
    this.W = 0;
    this.H = 0;
    this.userZoom = 1;
    this.userPanX = 0;
    this.userPanY = 0;
    this.viewZoom = 1;
    this.viewOffsetX = 0;
    this.viewOffsetY = 0;
    this.MIN_ZOOM = 0.6;
    this.MAX_ZOOM = 3.5;
    this.FIT_PADDING = 84;
    this.MIN_NODE_RADIUS = 18;
    this.resizeHandler = () => this.resize();
    this.isDark = !1;
    this.mediaQuery = activeWindow.matchMedia("(prefers-color-scheme: dark)");
    this.pointer = { x: 0, y: 0 };
    this.lassoPath = [];
    this.isLassoActive = !1;
    this.hoveredHoleKey = null;
    this.isPanning = !1;
    this.panStartScreenX = 0;
    this.panStartScreenY = 0;
    this.panMoved = !1;
    this.suppressNextClick = !1;
    this.themeHandler = () => {
      ((this.isDark = this.detectDarkMode()), this.render());
    };
    this.textWidthCache = new Map();
    this.viewportPadding = 100;
    this.lastRenderedNodes = new Set();
    this.progressiveSceneKey = "";
    this.progressiveNodeBudget = 0;
    this.progressiveSimplexBudget = 0;
    this.progressiveNodeStep = 140;
    this.progressiveSimplexStep = 220;
  }
  measureTextWidth(e, t) {
    if (this.textWidthCache.has(t)) return this.textWidthCache.get(t);
    let n = e.measureText(t).width;
    return (this.textWidthCache.size > 1e3 && this.textWidthCache.clear(), this.textWidthCache.set(t, n), n);
  }
  getVisibleNodes(e) {
    let t = -this.viewOffsetX / this.viewZoom - this.viewportPadding,
      n = -this.viewOffsetY / this.viewZoom - this.viewportPadding,
      i = (-this.viewOffsetX + this.W) / this.viewZoom + this.viewportPadding,
      s = (-this.viewOffsetY + this.H) / this.viewZoom + this.viewportPadding;
    return e.filter((o) => o.px >= t && o.px <= i && o.py >= n && o.py <= s);
  }
  simplexStrength(e, t) {
    return t === "confidence"
      ? (e.confidence ?? e.weight ?? 0)
      : t === "decayed-weight"
        ? (e.decayedWeight ?? e.weight ?? e.confidence ?? 0)
        : (e.weight ?? e.decayedWeight ?? e.confidence ?? 0);
  }
  passesRenderFilter(e) {
    return this.simplexStrength(e, this.settings.renderFilterMetric) >= this.settings.renderFilterThreshold;
  }
  nodeVisualPriority(e) {
    let t = this.model.getSimplicesForNode(e.id),
      n = t.some((o) => o.nodes.length >= 3),
      i = t.some((o) => o.nodes.length === 2);
    return { bucket: n ? 3 : i ? 2 : t.length > 0 ? 1 : 0, simplexCount: t.length };
  }
  progressiveNodeKey(e) {
    let t = this.controller.hoveredNodeId ?? "",
      n = this.controller.lockedNodeId ?? "";
    return [
      this.viewZoom.toFixed(3),
      this.viewOffsetX.toFixed(1),
      this.viewOffsetY.toFixed(1),
      t,
      n,
      e.length,
      this.settings.renderFilterMetric,
      (this.settings.renderFilterThreshold ?? 0).toFixed(2),
    ].join("|");
  }
  rankVisibleNodes(e, t) {
    let n = (this.W / 2 - this.viewOffsetX) / this.viewZoom,
      i = (this.H / 2 - this.viewOffsetY) / this.viewZoom,
      s = this.controller.hoveredNodeId,
      o = this.controller.lockedNodeId;
    return [...e].sort((a, l) => {
      let d = this.nodeVisualPriority(a),
        p = this.nodeVisualPriority(l),
        c = d.bucket * 10 + Math.min(4, d.simplexCount),
        u = p.bucket * 10 + Math.min(4, p.simplexCount),
        h = (a.id === s ? 6 : 0) + (a.id === o ? 5 : 0) + (t.has(a.id) ? 4 : 0) + (a.isPinned ? 2 : 0) + c,
        g = (l.id === s ? 6 : 0) + (l.id === o ? 5 : 0) + (t.has(l.id) ? 4 : 0) + (l.isPinned ? 2 : 0) + u;
      if (h !== g) return g - h;
      let f = (a.px - n) ** 2 + (a.py - i) ** 2,
        m = (l.px - n) ** 2 + (l.py - i) ** 2;
      return f - m;
    });
  }
  getProgressiveRenderableNodes(e, t) {
    let n = this.rankVisibleNodes(e, t),
      i = this.progressiveNodeKey(n);
    return (
      i !== this.progressiveSceneKey
        ? ((this.progressiveSceneKey = i),
          (this.progressiveNodeBudget = Math.min(n.length, this.progressiveNodeStep)),
          (this.progressiveSimplexBudget = this.progressiveSimplexStep))
        : ((this.progressiveNodeBudget = Math.min(n.length, this.progressiveNodeBudget + this.progressiveNodeStep)),
          (this.progressiveSimplexBudget += this.progressiveSimplexStep)),
      n.slice(0, this.progressiveNodeBudget)
    );
  }
  getRenderableSimplices(e, t, n) {
    return e
      .filter(([, s]) => this.passesRenderFilter(s))
      .filter(([, s]) => s.nodes.every((o) => t.has(o)))
      .sort((s, o) => {
        let [a, l] = s,
          [d, p] = o,
          c = l.nodes.length >= 3 ? 20 : l.nodes.length === 2 ? 10 : 0,
          u = p.nodes.length >= 3 ? 20 : p.nodes.length === 2 ? 10 : 0,
          h =
            (n.has(a) ? 6 : 0) +
            (l.suggested ? 2 : 0) +
            this.simplexStrength(l, this.settings.renderFilterMetric) +
            l.nodes.length * 0.05 +
            c;
        return (
          (n.has(d) ? 6 : 0) +
          (p.suggested ? 2 : 0) +
          this.simplexStrength(p, this.settings.renderFilterMetric) +
          p.nodes.length * 0.05 +
          u -
          h
        );
      })
      .slice(0, this.progressiveSimplexBudget);
  }
  getRenderableHyperedges(e) {
    return this.settings.showHyperedges
      ? [...this.model.hyperedges.entries()].filter(([, t]) => t.nodes.some((n) => e.has(n)))
      : [];
  }
  canPlaceLabelFast(e, t, n, i) {
    if (!this.ctx) return !1;
    let s = this.measureTextWidth(this.ctx, t) + 12,
      o = n - s / 2,
      a = i - 14,
      l = { left: o, top: a, right: o + s, bottom: a + 18 };
    return !e.some((d) => l.left < d.right && l.right > d.left && l.top < d.bottom && l.bottom > d.top);
  }
  init(e) {
    (this.canvas && !this.canvas.isConnected && this.destroy(),
      !this.canvas &&
        ((this.canvas = e.createEl("canvas", { cls: "simplicial-canvas" })),
        (this.ctx = this.canvas.getContext("2d")),
        (this.isDark = this.detectDarkMode()),
        this.resize(),
        activeWindow.addEventListener("resize", this.resizeHandler),
        this.mediaQuery.addEventListener("change", this.themeHandler),
        this.engine.start(
          () => this.render(),
          () => ({
            nodes: this.model.getAllNodes(),
            simplices: [...this.model.simplices.values()],
            hyperedges: [...this.model.hyperedges.values()],
            bounds: { width: this.W, height: this.H },
            holdNode: this.controller.holdNode,
          }),
        ),
        this.bindCanvasEvents()));
  }
  bindCanvasEvents() {
    this.canvas &&
      (this.canvas.addEventListener(
        "wheel",
        (e) => {
          e.preventDefault();
          let t = this.canvas.getBoundingClientRect(),
            n = { x: ((e.clientX - t.left) * this.W) / t.width, y: ((e.clientY - t.top) * this.H) / t.height };
          (this.zoomAt(n, e.deltaY), this.render());
        },
        { passive: !1 },
      ),
      this.canvas.addEventListener("mousemove", (e) => {
        let t = this.canvas.getBoundingClientRect(),
          n = { x: ((e.clientX - t.left) * this.W) / t.width, y: ((e.clientY - t.top) * this.H) / t.height };
        if (this.isPanning) {
          let s = n.x - this.panStartScreenX,
            o = n.y - this.panStartScreenY;
          (s * s + o * o > 1 && (this.panMoved = !0),
            (this.userPanX += s),
            (this.userPanY += o),
            (this.panStartScreenX = n.x),
            (this.panStartScreenY = n.y),
            this.render());
          return;
        }
        let i = this.eventToCanvasPoint(e);
        if (((this.pointer = i), this.settings.enableBettiComputation)) {
          let s = this.findHoleAtPoint(i),
            o = this.hoveredHoleKey;
          if (s) {
            if (((this.hoveredHoleKey = s.boundaryNodes.sort().join("|")), this.hoveredHoleKey !== o)) {
              let a = qe(s, new Map());
              this.callbacks.onHoleHover?.(s, a);
            }
          } else ((this.hoveredHoleKey = null), o !== null && this.callbacks.onHoleHover?.(null, null));
        }
        (this.isLassoActive
          ? this.lassoPath.push(i)
          : this.controller.onPointerMove(i.x, i.y)
            ? (this.suppressNextClick = !0)
            : this.controller.updateHover(i.x, i.y, this.model.getAllNodes(), this.settings.metadataHoverDelayMs),
          this.render());
      }),
      this.canvas.addEventListener("mouseleave", () => {
        (this.controller.cancelHoverSelection(),
          (this.controller.hoveredNodeId = null),
          this.controller.onMouseup(),
          (this.isPanning = !1),
          (this.panMoved = !1),
          this.cancelLasso(),
          this.render());
      }),
      this.canvas.addEventListener("mousedown", (e) => {
        if (e.shiftKey) {
          let s = this.eventToCanvasPoint(e);
          ((this.isLassoActive = !0), (this.lassoPath = [s]), this.controller.cancelHoverSelection(), this.render());
          return;
        }
        let t = this.eventToCanvasPoint(e),
          n = this.findNodeNearPoint(t);
        if (n) {
          ((this.controller.hoveredNodeId = n.id), this.controller.onMousedown(n.id, t.x, t.y));
          return;
        }
        let i = this.canvas.getBoundingClientRect();
        ((this.isPanning = !0),
          (this.panMoved = !1),
          (this.panStartScreenX = ((e.clientX - i.left) * this.W) / i.width),
          (this.panStartScreenY = ((e.clientY - i.top) * this.H) / i.height));
      }),
      this.canvas.addEventListener("mouseup", () => {
        (this.controller.onMouseup() && (this.suppressNextClick = !0),
          this.isPanning && this.panMoved && (this.suppressNextClick = !0),
          (this.isPanning = !1),
          (this.panMoved = !1),
          this.isLassoActive && this.finishLasso());
      }),
      this.canvas.addEventListener("dblclick", () => {
        (this.controller.hoveredNodeId && this.callbacks.onNodeOpen?.(this.controller.hoveredNodeId), this.render());
      }),
      this.canvas.addEventListener("click", (e) => {
        if (this.isLassoActive || this.suppressNextClick) {
          this.suppressNextClick = !1;
          return;
        }
        let t = this.eventToCanvasPoint(e),
          n = this.settings.enableBettiComputation ? this.findHoleAtPoint(t) : null;
        if (n) {
          let s = qe(n, new Map());
          this.callbacks.onHoleClick?.(n, s);
          return;
        }
        let i = this.findSimplexAtPoint(t);
        if (i) this.controller.selectSimplex(N(i.nodes));
        else {
          let s = this.findHyperedgeAtPoint(t);
          this.controller.selectRelation(s ? { kind: "hyperedge", key: s } : null);
        }
        this.render();
      }),
      this.canvas.addEventListener("contextmenu", (e) => {
        e.preventDefault();
        let t = this.eventToCanvasPoint(e),
          n = this.findNodeNearPoint(t),
          i = this.findSimplexAtPoint(t),
          s = this.findHyperedgeAtPoint(t);
        (!n && !i && !s) ||
          this.callbacks.onContextMenu?.(
            {
              ...(n ? { nodeId: n.id } : {}),
              ...(i ? { simplexKey: N(i.nodes) } : {}),
              ...(s ? { hyperedgeKey: s } : {}),
            },
            e,
          );
      }));
  }
  eventToCanvasPoint(e) {
    let t = this.canvas.getBoundingClientRect(),
      n = { x: ((e.clientX - t.left) * this.W) / t.width, y: ((e.clientY - t.top) * this.H) / t.height };
    return this.screenToWorld(n);
  }
  screenToWorld(e) {
    return { x: (e.x - this.viewOffsetX) / this.viewZoom, y: (e.y - this.viewOffsetY) / this.viewZoom };
  }
  worldToScreen(e) {
    return { x: e.x * this.viewZoom + this.viewOffsetX, y: e.y * this.viewZoom + this.viewOffsetY };
  }
  worldRadius(e) {
    return e / this.viewZoom;
  }
  findNodeNearPoint(e) {
    let t = this.worldRadius(20);
    return this.model.getAllNodes().find((n) => (n.px - e.x) ** 2 + (n.py - e.y) ** 2 <= t * t) ?? null;
  }
  findSimplexAtPoint(e) {
    let t = [...this.model.simplices.values()]
      .filter((i) => i.nodes.length >= 2)
      .sort((i, s) => s.nodes.length - i.nodes.length);
    for (let i of t) {
      let s = ve(i, this.model.getAllNodes());
      if (s.length === 2) {
        let [o, a] = s,
          l = a.x - o.x,
          d = a.y - o.y,
          p = Math.abs(d * e.x - l * e.y + a.x * o.y - a.y * o.x) / (Math.hypot(l, d) || 1),
          c = this.worldRadius(8);
        if (
          e.x >= Math.min(o.x, a.x) - c &&
          e.x <= Math.max(o.x, a.x) + c &&
          e.y >= Math.min(o.y, a.y) - c &&
          e.y <= Math.max(o.y, a.y) + c &&
          p <= this.worldRadius(10)
        )
          return i;
        continue;
      }
      if (s.length >= 3 && ye(e, s)) return i;
    }
    let n = this.findNodeNearPoint(e);
    return n ? (this.model.getSimplicesForNode(n.id).sort((i, s) => s.nodes.length - i.nodes.length)[0] ?? null) : null;
  }
  findHyperedgeAtPoint(e) {
    if (!this.settings.showHyperedges) return null;
    let t = this.model.getAllNodes(),
      n = [...this.model.hyperedges.entries()].sort((i, s) => i[1].nodes.length - s[1].nodes.length);
    for (let [i, s] of n) {
      let o = ve(s, t);
      if (!(o.length < 2)) {
        if (o.length === 2) {
          let [a, l] = o,
            d = l.x - a.x,
            p = l.y - a.y,
            c = Math.abs(p * e.x - d * e.y + l.x * a.y - l.y * a.x) / (Math.hypot(d, p) || 1),
            u = this.worldRadius(8);
          if (
            e.x >= Math.min(a.x, l.x) - u &&
            e.x <= Math.max(a.x, l.x) + u &&
            e.y >= Math.min(a.y, l.y) - u &&
            e.y <= Math.max(a.y, l.y) + u &&
            c <= this.worldRadius(10)
          )
            return i;
          continue;
        }
        if (ye(e, o)) return i;
      }
    }
    return null;
  }
  findHoleAtPoint(e) {
    if (!this.settings.enableBettiComputation) return null;
    let t = this.model.getCachedBetti();
    if (!t?.holes?.length) return null;
    let n = this.model.getAllNodes(),
      i = new Map(n.map((o) => [o.id, o])),
      s = this.worldRadius(15);
    for (let o of t.holes) {
      let a = o.boundaryNodes.map((h) => i.get(h)).filter(Boolean);
      if (a.length < 3) continue;
      let l = a.filter((h) => Math.hypot(h.px, h.py) > 80);
      if (l.length < a.length) continue;
      let d = l.reduce((h, g) => ({ x: h.x + g.px, y: h.y + g.py }), { x: 0, y: 0 });
      if (((d.x /= l.length), (d.y /= l.length), Math.max(...l.map((h) => Math.hypot(h.px - d.x, h.py - d.y))) < 40))
        continue;
      if (Math.hypot(e.x - d.x, e.y - d.y) < s * 2) return o;
      let u = l.map((h) => ({ x: h.px, y: h.py }));
      if (ye(e, u)) return o;
    }
    return null;
  }
  finishLasso() {
    let e = this.lassoPath.length >= 3 ? this.lassoPath : [],
      t =
        e.length >= 3
          ? this.model
              .getAllNodes()
              .filter((n) => ye({ x: n.px, y: n.py }, e))
              .map((n) => n.id)
          : [];
    (this.cancelLasso(),
      (this.suppressNextClick = !0),
      t.length >= 2 && this.callbacks.onLassoCreate?.(t),
      this.render());
  }
  cancelLasso() {
    ((this.isLassoActive = !1), (this.lassoPath = []));
  }
  detectDarkMode() {
    return this.settings.darkMode === "force-dark"
      ? !0
      : this.settings.darkMode === "force-light"
        ? !1
        : activeWindow.matchMedia("(prefers-color-scheme: dark)").matches;
  }
  resize() {
    if (!this.canvas || !this.ctx) return;
    let e = this.canvas.parentElement?.getBoundingClientRect();
    e &&
      ((this.W = e.width),
      (this.H = e.height),
      (this.canvas.width = this.W * this.dpr),
      (this.canvas.height = this.H * this.dpr),
      (this.canvas.style.width = `${this.W}px`),
      (this.canvas.style.height = `${this.H}px`),
      this.ctx.setTransform(this.dpr, 0, 0, this.dpr, 0, 0));
  }
  zoomAt(e, t) {
    let n = Math.exp(-t * 0.0015),
      i = Math.max(this.MIN_ZOOM, Math.min(this.MAX_ZOOM, this.userZoom * n));
    Math.abs(i - this.userZoom) < 1e-4 || (this.userZoom = i);
  }
  updateViewTransform(e) {
    if (!e.length || this.W <= 0 || this.H <= 0) {
      ((this.viewZoom = this.userZoom), (this.viewOffsetX = this.W / 2), (this.viewOffsetY = this.H / 2));
      return;
    }
    let t = e.filter((m) => this.nodeVisualPriority(m).bucket >= 2),
      n = t.length > 0 ? t : e,
      i = this.MIN_NODE_RADIUS,
      s = Math.min(...n.map((m) => m.px)) - i,
      o = Math.max(...n.map((m) => m.px)) + i,
      a = Math.min(...n.map((m) => m.py)) - i,
      l = Math.max(...n.map((m) => m.py)) + i,
      d = Math.max(1, o - s),
      p = Math.max(1, l - a),
      c = Math.max(1, this.W - this.FIT_PADDING * 2),
      u = Math.max(1, this.H - this.FIT_PADDING * 2),
      h = Math.min(c / d, u / p),
      g = (s + o) / 2,
      f = (a + l) / 2;
    ((this.viewZoom = h * this.userZoom),
      (this.viewOffsetX = this.W / 2 - g * this.viewZoom + this.userPanX),
      (this.viewOffsetY = this.H / 2 - f * this.viewZoom + this.userPanY));
  }
  hyperedgeColor(e) {
    return H(null, { nodes: e.nodes, label: e.label, colorKey: e.colorKey, sourcePath: e.sourcePath });
  }
  alphaForDimension(e, t) {
    return e === 1 ? (t ? 0.18 : 0.1) : e === 2 ? (t ? 0.18 : 0.13) : e === 3 ? (t ? 0.11 : 0.07) : 0.05;
  }
  formatNodeLabel(e) {
    return e.split("/").pop()?.replace(/\.md$/, "") ?? e;
  }
  simplexPathText(e, t) {
    return [e, ...t.nodes.filter((i) => i !== e)].map((i) => this.formatNodeLabel(i)).join(" -> ");
  }
  simplexDescriptor(e) {
    let t = e.label?.trim();
    if (t && t.toLowerCase() !== "soft cluster") return t;
    let n = (e.inferredSignals ?? []).filter((i) => i !== "soft-cluster");
    if (n.length > 0) {
      let i = [],
        s = n
          .filter((h) => h.startsWith("tags:"))
          .map((h) => Number(h.slice(5)))
          .filter((h) => Number.isFinite(h) && h > 0),
        o = s.length ? Math.max(...s) : 0,
        a = n.includes("folder:same"),
        l = n.includes("folder:top"),
        d = n.includes("link:a->b"),
        p = n.includes("link:b->a"),
        c = n.filter((h) => h.startsWith("title:")).map((h) => h.slice(6)),
        u = n.filter((h) => h.startsWith("content:")).map((h) => h.slice(8));
      return (
        d && p ? i.push("mutual link") : (d || p) && i.push("direct link"),
        o > 0 && i.push(o === 1 ? "1 shared tag" : `${o} shared tags`),
        a ? i.push("same folder") : l && i.push("same top folder"),
        c.length && i.push("similar titles"),
        u.length && i.push("similar content"),
        i.join(" \xB7 ")
      );
    }
    return e.dominantSignal === "tags"
      ? "tag relation"
      : e.dominantSignal === "folder"
        ? "folder relation"
        : e.dominantSignal === "link"
          ? "link relation"
          : e.dominantSignal === "semantic"
            ? "semantic relation"
            : e.dominantSignal === "soft-cluster"
              ? "soft cluster"
              : e.sourcePath
                ? this.formatNodeLabel(e.sourcePath)
                : `dim ${e.nodes.length - 1} simplex`;
  }
  largestNodeContext(e) {
    let t = this.model
      .getSimplicesForNode(e)
      .sort((n, i) => i.nodes.length - n.nodes.length || (i.weight ?? 1) - (n.weight ?? 1));
    return t.length > 0 ? this.simplexDescriptor(t[0]) : null;
  }
  drawWrappedText(e, t, n, i, s, o) {
    let a = t.split(/\s+/).filter(Boolean);
    if (!a.length) return i;
    let l = "",
      d = i;
    return (
      a.forEach((p) => {
        let c = l ? `${l} ${p}` : p;
        if (e.measureText(c).width <= s || !l) {
          l = c;
          return;
        }
        (e.fillText(l, n, d), (d += o), (l = p));
      }),
      l && e.fillText(l, n, d),
      d + o
    );
  }
  drawHoveredNodeOverlay(e) {
    let t = this.controller.hoveredNodeId ?? this.controller.lockedNodeId;
    if (!t) return;
    let n = this.model.nodes.get(t);
    if (!n) return;
    let i = this.formatNodeLabel(t),
      s = this.largestNodeContext(t),
      o = this.model.getSimplicesForNode(t)[0],
      [a, l, d] = o ? H(this.model, o) : [136, 135, 128],
      p = this.worldToScreen({ x: n.px, y: n.py });
    (e.save(), (e.font = "700 18px system-ui, sans-serif"));
    let c = e.measureText(i).width;
    e.font = "500 12px system-ui, sans-serif";
    let u = s ? Math.min(320, e.measureText(s).width) : 0,
      h = Math.min(Math.max(c + 28, u + 28, 180), 360),
      g = s ? 48 : 26,
      f = Math.min(Math.max(12, p.x + 14), this.W - h - 12),
      m = Math.min(Math.max(24, p.y - g - 18), this.H - g - 12);
    ((e.fillStyle = `rgb(${a},${l},${d})`),
      (e.shadowColor = this.isDark ? "rgba(10, 14, 22, 0.9)" : "rgba(255, 255, 255, 0.92)"),
      (e.shadowBlur = 10),
      (e.font = "700 18px system-ui, sans-serif"),
      e.fillText(i, f, m + 18),
      s &&
        ((e.fillStyle = this.isDark ? "rgba(235,240,248,0.88)" : "rgba(24,28,34,0.84)"),
        (e.shadowBlur = 8),
        (e.font = "500 12px system-ui, sans-serif"),
        this.drawWrappedText(e, s, f, m + 36, h, 16)),
      e.restore());
  }
  drawFormalSimplex(e, t, n) {
    let i = ve(t, this.model.getAllNodes());
    if (i.length < 2) return;
    let [s, o, a] = H(this.model, t),
      l = n ? 0.8 : 0.28;
    if (
      (e.save(),
      (e.strokeStyle = `rgba(${s},${o},${a},${l})`),
      (e.lineWidth = t.nodes.length >= 3 ? 1.6 : 1.1),
      i.length === 2)
    ) {
      (e.beginPath(), e.moveTo(i[0].x, i[0].y), e.lineTo(i[1].x, i[1].y), e.stroke(), e.restore());
      return;
    }
    (e.beginPath(),
      e.moveTo(i[0].x, i[0].y),
      i.slice(1).forEach((d) => e.lineTo(d.x, d.y)),
      e.closePath(),
      e.stroke(),
      (e.fillStyle = `rgba(${s},${o},${a},${t.inferred ? 0.03 : 0.06})`),
      e.fill(),
      e.restore());
  }
  drawSuggestionOverlay(e, t, n) {
    if (!this.settings.showSuggestions || !n.suggested || !n.inferred) return;
    let i = ve(n, this.model.getAllNodes());
    if (i.length < 2) return;
    let [s, o, a] = H(this.model, n);
    (e.save(),
      e.setLineDash([5, 5]),
      (e.strokeStyle = `rgba(${s},${o},${a},0.7)`),
      (e.lineWidth = 1),
      i.length === 2
        ? (e.beginPath(), e.moveTo(i[0].x, i[0].y), e.lineTo(i[1].x, i[1].y), e.stroke())
        : (e.beginPath(),
          e.moveTo(i[0].x, i[0].y),
          i.slice(1).forEach((u) => e.lineTo(u.x, u.y)),
          e.closePath(),
          e.stroke()),
      e.setLineDash([]));
    let l = i.reduce((u, h) => ({ x: u.x + h.x, y: u.y + h.y }), { x: 0, y: 0 });
    ((l.x /= i.length), (l.y /= i.length));
    let d = Math.round((n.confidence ?? n.weight ?? 0) * 100),
      p = n.nodes.length > 2 ? `Form? ${d}%` : `${d}%`;
    e.font = "11px system-ui, sans-serif";
    let c = e.measureText(p).width + 10;
    ((e.fillStyle = this.isDark ? "rgba(10,14,22,0.85)" : "rgba(255,255,255,0.88)"),
      e.beginPath(),
      e.roundRect(l.x - c / 2, l.y - 10, c, 18, 9),
      e.fill(),
      (e.fillStyle = `rgb(${s},${o},${a})`),
      (e.textAlign = "center"),
      e.fillText(p, l.x, l.y + 3),
      e.restore());
  }
  render() {
    if (!this.ctx) return;
    let e = this.ctx,
      t = this.model.getAllNodes(),
      n = [...this.model.simplices.entries()]
        .filter(([, g]) => g.nodes.length - 1 <= this.settings.maxRenderedDim)
        .sort((g, f) => f[1].nodes.length - g[1].nodes.length),
      i = this.controller.getFocusState();
    this.updateViewTransform(t);
    let s = this.getVisibleNodes(t),
      o = this.getProgressiveRenderableNodes(s, i.activeNodeIds),
      a = new Set(o.map((g) => g.id)),
      l = this.getRenderableSimplices(n, a, i.activeSimplexKeys),
      d = this.getRenderableHyperedges(a);
    (e.setTransform(this.dpr, 0, 0, this.dpr, 0, 0),
      e.clearRect(0, 0, this.W, this.H),
      e.translate(this.viewOffsetX, this.viewOffsetY),
      e.scale(this.viewZoom, this.viewZoom),
      this.lastRenderedNodes.clear(),
      o.forEach((g) => this.lastRenderedNodes.add(g.id)));
    let p = [],
      c = 0,
      u = Math.max(4, Math.ceil(o.length * this.settings.labelDensity));
    (this.settings.formalMode
      ? l.forEach(([g, f]) => {
          let m = f.nodes.length - 1;
          (m === 1 && !this.settings.showEdges) ||
            (m === 2 && !this.settings.showClusters) ||
            (m >= 3 && !this.settings.showCores) ||
            (this.drawFormalSimplex(e, f, !i.isActive || i.involvesSimplex(f, g)), this.drawSuggestionOverlay(e, g, f));
        })
      : l.forEach(([g, f]) => {
          let m = f.nodes.length - 1;
          (m === 1 && !this.settings.showEdges) ||
            (m === 2 && !this.settings.showClusters) ||
            (m >= 3 && !this.settings.showCores) ||
            (sn(e, g, f, this.model, t, this.alphaForDimension(m, i.isActive), i), this.drawSuggestionOverlay(e, g, f));
        }),
      this.settings.formalMode ||
        rn(
          e,
          l.map(([, g]) => g),
          this.model,
          this.model.nodes,
          this.settings.showEdges,
          i,
        ),
      d.forEach(([g, f]) => {
        nn(
          e,
          f,
          t,
          this.hyperedgeColor(f),
          this.settings.hyperedgeOpacity,
          !i.isActive || i.involvesRelation({ kind: "hyperedge", ...f }, g),
        );
      }),
      o.forEach((g) => {
        this.controller.lerpAlpha(g, i);
        let f = g.id === i.hoveredNodeId,
          m = !i.isActive || i.involvesNode(g.id),
          v = this.model.getSimplicesForNode(g.id)[0] ?? null,
          [y, b, S] = v ? H(this.model, v) : [136, 135, 128];
        (f &&
          (e.beginPath(), e.arc(g.px, g.py, 15, 0, Math.PI * 2), (e.fillStyle = `rgba(${y},${b},${S},0.10)`), e.fill()),
          e.beginPath(),
          g.isVirtual
            ? (e.arc(g.px, g.py, this.settings.formalMode ? 4.5 : f ? 7 : 5, 0, Math.PI * 2),
              (e.strokeStyle = `rgba(${y},${b},${S},${g.displayAlpha})`),
              (e.lineWidth = 1.5),
              e.stroke())
            : (e.arc(g.px, g.py, this.settings.formalMode ? 4.5 : f ? 7 : 5, 0, Math.PI * 2),
              (e.fillStyle = `rgba(${y},${b},${S},${m ? g.displayAlpha : 0.2})`),
              e.fill()),
          g.isPinned &&
            (e.beginPath(),
            e.moveTo(g.px, g.py - 18),
            e.lineTo(g.px + 5, g.py - 13),
            e.lineTo(g.px, g.py - 8),
            e.lineTo(g.px - 5, g.py - 13),
            e.closePath(),
            (e.fillStyle = `rgba(${y},${b},${S},0.8)`),
            e.fill()),
          (e.font = `${f ? "500" : "400"} 12px system-ui, sans-serif`));
        let w = this.formatNodeLabel(g.id),
          E = c < u,
          I = this.canPlaceLabelFast(p, w, g.px, g.py - 13),
          D = this.measureTextWidth(e, w) + 12,
          R = this.nodeVisualPriority(g),
          O = f || i.lockedNodeId === g.id || g.isPinned,
          se = R.bucket >= 3,
          Je = R.bucket === 2,
          Y = R.bucket === 0;
        if (
          !(O || (se && E && I) || (Je && c < Math.max(2, Math.floor(u * 0.45)) && I) || (!Y && i.isActive && E && I))
        )
          return;
        O || c++;
        let ke = 18,
          $ = g.px - D / 2,
          Ie = g.py - 27;
        (p.push({ left: $, top: Ie, right: $ + D, bottom: Ie + ke }),
          (e.fillStyle = this.isDark ? "rgba(7,10,18,0.28)" : "rgba(255,255,255,0.42)"),
          e.beginPath(),
          e.roundRect($, Ie, D, ke, 9),
          e.fill(),
          (e.fillStyle = this.isDark ? `rgba(255,255,255,${m ? 0.88 : 0.32})` : `rgba(0,0,0,${m ? 0.72 : 0.28})`),
          (e.textAlign = "center"),
          e.fillText(w, g.px, g.py - 13));
      }),
      this.isLassoActive &&
        this.lassoPath.length > 1 &&
        (e.save(),
        (e.strokeStyle = this.isDark ? "rgba(255,255,255,0.45)" : "rgba(0,0,0,0.45)"),
        (e.fillStyle = this.isDark ? "rgba(255,255,255,0.05)" : "rgba(0,0,0,0.04)"),
        e.setLineDash([6, 6]),
        e.beginPath(),
        e.moveTo(this.lassoPath[0].x, this.lassoPath[0].y),
        this.lassoPath.slice(1).forEach((g) => e.lineTo(g.x, g.y)),
        e.stroke(),
        e.lineTo(this.pointer.x, this.pointer.y),
        e.closePath(),
        e.fill(),
        e.restore()),
      e.setTransform(this.dpr, 0, 0, this.dpr, 0, 0),
      this.drawHoveredNodeOverlay(e));
    let h = this.settings.bettiDisplayOnCanvas && this.settings.enableBettiComputation;
    (h && hn(e, this.model, this.isDark),
      this.settings.showHyperedges && un(e, this.model, this.isDark, h ? 46 : 14),
      this.settings.enableBettiComputation &&
        pn(e, this.model, this.isDark, this.getVisibleWorldBounds(), this.hoveredHoleKey));
  }
  getVisibleWorldBounds() {
    let e = this.screenToWorld({ x: 0, y: 0 }),
      t = this.screenToWorld({ x: this.W, y: this.H });
    return { minX: Math.min(e.x, t.x), maxX: Math.max(e.x, t.x), minY: Math.min(e.y, t.y), maxY: Math.max(e.y, t.y) };
  }
  canPlaceLabel(e, t, n, i, s) {
    return this.canPlaceLabelFast(t, n, i, s);
  }
  getBounds() {
    return { width: this.W, height: this.H };
  }
  destroy() {
    (this.engine.stop(),
      activeWindow.removeEventListener("resize", this.resizeHandler),
      this.mediaQuery.removeEventListener("change", this.themeHandler),
      (this.canvas = null),
      (this.ctx = null));
  }
};
var L = require("obsidian"),
  Hi = {
    simplex: {
      title: "Create simplex",
      blurb: "A simplex claims the group and every sub-relation within it is coherent. Its faces will be generated.",
      cta: "Create simplex",
    },
    hyperedge: {
      title: "Create encounter",
      blurb:
        "An encounter claims only that these notes came together as one irreducible whole. No faces are generated, and no pair is asserted to be meaningful on its own.",
      cta: "Create encounter",
    },
  },
  Se = class extends L.Modal {
    constructor(t, n, i, s, o = "simplex") {
      super(t);
      this.initialNodes = n;
      this.persistenceLabel = i;
      this.onSubmit = s;
      this.nodesInput = "";
      this.labelInput = "";
      this.modeInput = "";
      this.weightInput = 1;
      ((this.nodesInput = n.join(", ")), (this.kind = o));
    }
    onOpen() {
      (this.render(),
        window.setTimeout(() => {
          let t = this.contentEl.querySelector("textarea");
          t instanceof HTMLTextAreaElement && t.focus();
        }, 0));
    }
    render() {
      let { contentEl: t } = this;
      t.empty();
      let n = Hi[this.kind];
      (t.createEl("h3", { text: n.title }),
        t.createEl("p", { text: n.blurb }),
        t.createEl("p", {
          text: `Saves to ${this.persistenceLabel}. Use commas, spaces, or new lines to separate nodes.`,
        }),
        new L.Setting(t)
          .setName("Kind")
          .setDesc("Simplex: coherence inherited across subrelations. Encounter: irreducible group emergence.")
          .addDropdown((s) => {
            (s.addOption("simplex", "Simplex (\u25B3)"),
              s.addOption("hyperedge", "Encounter (\u25C7)"),
              s.setValue(this.kind),
              s.onChange((o) => {
                ((this.kind = o === "hyperedge" ? "hyperedge" : "simplex"), this.render());
              }));
          }));
      let i = null;
      (new L.Setting(t)
        .setName("Nodes")
        .setDesc("These will be resolved as notes when possible; unknown values become virtual nodes.")
        .addTextArea((s) => {
          ((i = s),
            s.setValue(this.nodesInput),
            (s.inputEl.rows = 6),
            s.onChange((o) => {
              this.nodesInput = o;
            }));
        }),
        new L.Setting(t).setName("Label").addText((s) => {
          (s.setPlaceholder("Unnamed"),
            s.setValue(this.labelInput),
            s.onChange((o) => {
              this.labelInput = o;
            }));
        }),
        this.kind === "hyperedge" &&
          new L.Setting(t)
            .setName("Mode")
            .setDesc("What kind of encounter this was \u2014 free text, e.g. 'encounter', 'reading', 'argument'.")
            .addText((s) => {
              (s.setPlaceholder("encounter"),
                s.setValue(this.modeInput),
                s.onChange((o) => {
                  this.modeInput = o;
                }));
            }),
        new L.Setting(t)
          .setName("Weight")
          .setDesc("0.1 to 1.0")
          .addSlider((s) => {
            (s.setLimits(0.1, 1, 0.1),
              s.setValue(this.weightInput),
              s.onChange((o) => {
                this.weightInput = o;
              }));
          }),
        new L.Setting(t)
          .addButton((s) => {
            (s.setButtonText(n.cta),
              s.setCta(),
              s.onClick(async () => {
                let o = this.parseNodes(this.nodesInput);
                if (o.length < 2) {
                  (new L.Notice(
                    this.kind === "hyperedge"
                      ? "An encounter needs at least two participants."
                      : "A simplex needs at least two nodes.",
                  ),
                    i?.inputEl.focus());
                  return;
                }
                (await this.onSubmit({
                  kind: this.kind,
                  nodes: o,
                  label: this.labelInput.trim() || void 0,
                  weight: this.weightInput,
                  ...(this.kind === "hyperedge" ? { mode: this.modeInput.trim() || "encounter" } : {}),
                }),
                  this.close());
              }));
          })
          .addExtraButton((s) => {
            (s.setIcon("cross"), s.setTooltip("Cancel"), s.onClick(() => this.close()));
          }));
    }
    parseNodes(t) {
      return t
        .split(/[\n,\s]+/g)
        .map((n) => n.trim())
        .filter(Boolean)
        .filter((n, i, s) => s.indexOf(n) === i);
    }
  };
var we = require("obsidian");
function yn(r) {
  return r.split("/").pop()?.replace(/\.md$/, "") ?? r;
}
var xe = class extends we.Modal {
  constructor(t, n, i, s) {
    super(t);
    this.nodes = n;
    this.faces = i;
    this.onConfirm = s;
  }
  onOpen() {
    let { contentEl: t } = this;
    if (
      (t.empty(),
      t.createEl("h3", { text: "Promote encounter to simplex" }),
      t.createEl("p", {
        text: `You are asserting that ${this.nodes.map(yn).join(" \xB7 ")} is not only meaningful as a whole, but that its sub-relations are meaningful too.`,
      }),
      this.faces.length === 0)
    )
      t.createEl("p", { text: "Every implied face already exists in the complex, so nothing new will be asserted." });
    else {
      t.createEl("p", {
        text: `${this.faces.length} new ${this.faces.length === 1 ? "relation" : "relations"} will be created:`,
      });
      let n = t.createEl("ul", { cls: "simplicial-promote-faces" });
      this.faces.forEach((i) => {
        n.createEl("li", { text: i.map(yn).join(" \u2014 ") });
      });
    }
    (t.createEl("p", {
      cls: "simplicial-promote-note",
      text: "The encounter is kept as provenance, so this can be undone with 'relax to encounter'.",
    }),
      new we.Setting(t)
        .addButton((n) => {
          (n.setButtonText("Promote"),
            n.setCta(),
            n.onClick(async () => {
              (await this.onConfirm(), this.close());
            }));
        })
        .addExtraButton((n) => {
          (n.setIcon("cross"), n.setTooltip("Cancel"), n.onClick(() => this.close()));
        }));
  }
};
var T = require("obsidian");
var Ce = class extends T.ItemView {
  constructor(t, n) {
    super(t);
    this.model = n;
    this.simplexKey = null;
    this.selection = null;
    this.actions = null;
    this.history = null;
    this.settings = null;
    this.nodeProfiles = [];
    this.subsetScorer = null;
  }
  setSettings(t) {
    this.settings = t;
  }
  setNodeProfiles(t) {
    this.nodeProfiles = t;
  }
  setSubsetScorer(t) {
    this.subsetScorer = t;
  }
  getViewType() {
    return q;
  }
  getDisplayText() {
    return "Simplicial metadata";
  }
  setActions(t) {
    ((this.actions = t),
      (this.saveMetadata = t.saveMetadata),
      (this.promoteSimplex = t.promoteSimplex),
      (this.dissolveSimplex = t.dissolveSimplex));
  }
  setHistory(t) {
    this.history = t;
  }
  setSelection(t) {
    let n = typeof t == "string" ? { kind: "simplex", key: t } : t;
    ((this.selection = n), (this.simplexKey = n?.kind === "simplex" ? n.key : null), this.renderPanel());
  }
  async onOpen() {
    (await Promise.resolve(), this.renderPanel());
  }
  renderPanel() {
    let { contentEl: t } = this;
    if ((t.empty(), this.selection?.kind === "hyperedge")) {
      this.renderHyperedgePanel(t, this.selection.key);
      return;
    }
    let n = this.simplexKey ? this.model.getSimplex(this.simplexKey) : null;
    if ((t.createEl("div", { cls: "simplicial-panel-title", text: "Simplex" }), !n)) {
      t.createEl("div", { text: "Select a simplex or an encounter." });
      return;
    }
    (this.settings?.enableExplanationPanel && this.renderExplanationCard(t, n),
      t.createEl("div", { cls: "simplicial-panel-section-label", text: "Nodes" }),
      t.createEl("div", { cls: "simplicial-panel-value", text: n.nodes.join(" \xB7 ") }));
    let i = this.model.getAnalysisSummary(),
      [s, o, a] = H(this.model, n),
      l = t.createDiv({ cls: "simplicial-panel-badges" });
    (this.renderBadge(l, n.autoGenerated ? "Auto face" : n.inferred ? "Inferred" : "Confirmed", [s, o, a]),
      this.renderBadge(l, `dim ${n.nodes.length - 1}`, [s, o, a]),
      n.sourcePath && this.renderBadge(l, n.sourcePath.replace(/\.md$/, ""), [s, o, a], !0),
      n.inferred &&
        (t.createEl("div", {
          cls: "simplicial-panel-value",
          text: `Inferred from: ${(n.inferredSignals ?? []).join(" \xB7 ") || "weighted vault signals"}`,
        }),
        t.createEl("div", {
          cls: "simplicial-panel-value",
          text: `Confidence: ${Math.round((n.confidence ?? n.weight ?? 0) * 100)}%${n.suggested ? " \xB7 suggested" : ""}${n.decayedWeight !== void 0 ? ` \xB7 decayed ${Math.round(n.decayedWeight * 100)}%` : ""}`,
        })));
    let p = n.nodes
      .map((c) => ({ nodeId: c, centrality: this.model.getSimplicesForNode(c).length }))
      .sort((c, u) => u.centrality - c.centrality)
      .map(({ nodeId: c, centrality: u }) => `${c.replace(/\.md$/, "")} (${u})`)
      .join(" \xB7 ");
    if (
      (t.createEl("div", { cls: "simplicial-panel-value", text: `Simplex centrality: ${p || "n/a"}` }),
      i.maxSimplexCentralityNodeId &&
        t.createEl("div", {
          cls: "simplicial-panel-value",
          text: `Global hub: ${i.maxSimplexCentralityNodeId.replace(/\.md$/, "")} (${i.maxSimplexCentrality}) \xB7 avg ${i.averageSimplexCentrality}`,
        }),
      n.inferred ||
        t.createEl("div", {
          cls: "simplicial-panel-value",
          text: `Persistence: ${n.sourcePath?.replace(/\.md$/, "") ?? "in memory"}`,
        }),
      n.autoGenerated)
    ) {
      (t.createEl("div", { cls: "simplicial-panel-value", text: "Auto-generated face. Metadata editing is disabled." }),
        t.createEl("div", { cls: "simplicial-panel-footer", text: `dim: ${n.nodes.length - 1} \xB7 auto: yes` }));
      return;
    }
    (new T.Setting(t).setName("Label").addText((c) => {
      (c.setPlaceholder("Unnamed"),
        c.setValue(n.label ?? ""),
        c.onChange((u) => {
          (window.clearTimeout(c.inputEl._simplicialTimer),
            (c.inputEl._simplicialTimer = window.setTimeout(() => {
              this.saveMetadata?.(this.simplexKey, { label: u });
            }, 500)));
        }));
    }),
      new T.Setting(t)
        .setName("Weight")
        .setDesc(String(n.weight ?? 1))
        .addSlider((c) => {
          let u = n.weight ?? 1;
          (c.setLimits(0.1, 1, 0.1),
            c.setValue(n.weight ?? 1),
            c.onChange((h) => {
              ((u = h), this.model.updateMetadata(this.simplexKey, { weight: h }), this.renderPanel());
            }),
            c.sliderEl.addEventListener("mouseup", () => {
              this.saveMetadata?.(this.simplexKey, { weight: u });
            }));
        }),
      new T.Setting(t)
        .setName("Relax to encounter")
        .setDesc(
          "Withdraw the claim that this group's sub-relations are meaningful, while keeping the group relation itself.",
        )
        .addButton((c) => {
          (c.setButtonText("Relax"),
            c.onClick(async () => {
              this.simplexKey && (await this.actions?.relaxSimplex(this.simplexKey));
            }));
        }),
      new T.Setting(t)
        .addButton((c) => {
          (c.setButtonText("Promote to note"),
            c.onClick(async () => {
              this.simplexKey &&
                (await this.promoteSimplex?.(this.simplexKey), new T.Notice("Simplex promoted to note."));
            }));
        })
        .addExtraButton((c) => {
          (c.setIcon("trash"),
            c.setTooltip("Dissolve simplex"),
            c.onClick(async () => {
              this.simplexKey && (await this.dissolveSimplex?.(this.simplexKey), new T.Notice("Simplex dissolved."));
            }));
        }),
      t.createEl("div", {
        cls: "simplicial-panel-footer",
        text: `dim: ${n.nodes.length - 1} \xB7 auto: ${n.autoGenerated ? "yes" : "no"} \xB7 inferred: ${n.inferred ? "yes" : "no"}`,
      }));
  }
  renderHyperedgePanel(t, n) {
    t.createEl("div", { cls: "simplicial-panel-title", text: "Encounter" });
    let i = this.model.getHyperedge(n);
    if (!i) {
      t.createEl("div", { text: "This encounter is no longer in the graph." });
      return;
    }
    let s = this.settings?.encounterRecurrenceThreshold ?? 3,
      o = this.history?.occurrencesOf(i.nodes) ?? [],
      a = dn(this.model, n, {
        ...(this.subsetScorer ? { score: this.subsetScorer } : {}),
        occurrences: o,
        halfLifeDays: this.settings?.decayHalfLifeDays ?? 90,
      });
    (t.createEl("div", {
      cls: "simplicial-explanation-tension",
      text: "These notes came together as one irreducible whole. No pair among them is asserted to be meaningful on its own.",
    }),
      t.createEl("div", { cls: "simplicial-panel-section-label", text: "Participants" }),
      t.createEl("div", { cls: "simplicial-panel-value", text: i.nodes.join(" \xB7 ") }));
    let l = t.createDiv({ cls: "simplicial-panel-badges" }),
      d = [127, 119, 221];
    (this.renderBadge(l, `order ${i.nodes.length}`, d),
      this.renderBadge(l, i.persistence === "recurring" ? "Recurring" : "Momentary", d),
      i.mode && this.renderBadge(l, i.mode, d, !0),
      i.promotedTo && this.renderBadge(l, "promoted", d, !0),
      i.sourcePath && this.renderBadge(l, i.sourcePath.replace(/\.md$/, ""), d, !0),
      o.length > 0 &&
        t.createEl("div", {
          cls: "simplicial-panel-value",
          text: `First encountered ${new Date(o[0]).toLocaleDateString()}`,
        }),
      a && this.renderEncounterDiagnostics(t, a, s),
      i.crystallizedInto &&
        t.createEl("div", {
          cls: "simplicial-panel-value",
          text: `Crystallized into ${i.crystallizedInto.replace(/\.md$/, "")}`,
        }),
      new T.Setting(t).setName("Label").addText((c) => {
        (c.setPlaceholder("Unnamed"),
          c.setValue(i.label ?? ""),
          c.onChange((u) => {
            (window.clearTimeout(c.inputEl._simplicialTimer),
              (c.inputEl._simplicialTimer = window.setTimeout(() => {
                this.actions?.saveHyperedgeMetadata(n, { label: u });
              }, 500)));
          }));
      }),
      new T.Setting(t)
        .setName("Weight")
        .setDesc(String(i.weight ?? 1))
        .addSlider((c) => {
          let u = i.weight ?? 1;
          (c.setLimits(0.1, 1, 0.1),
            c.setValue(i.weight ?? 1),
            c.onChange((h) => {
              ((u = h), this.model.updateHyperedge(n, { weight: h }));
            }),
            c.sliderEl.addEventListener("mouseup", () => {
              this.actions?.saveHyperedgeMetadata(n, { weight: u });
            }));
        }),
      new T.Setting(t)
        .setName("Promote to simplex")
        .setDesc(
          "Assert that the sub-relations within this group are meaningful too. You will see the exact list first.",
        )
        .addButton((c) => {
          (c.setButtonText("Promote"),
            c.setDisabled(!!i.promotedTo),
            c.onClick(() => this.actions?.promoteEncounter(n)));
        }));
    let p = i.persistence === "recurring";
    (new T.Setting(t)
      .setName("Crystallize concept")
      .setDesc(
        p
          ? "Precipitate a new note naming what keeps emerging here. The encounter stays unpromoted."
          : `Available once this configuration has recurred ${s} times.`,
      )
      .addButton((c) => {
        (c.setButtonText("Crystallize"),
          c.setDisabled(!p),
          c.onClick(async () => {
            await this.actions?.crystallizeEncounter(n);
          }));
      }),
      new T.Setting(t).addExtraButton((c) => {
        (c.setIcon("trash"),
          c.setTooltip("Dissolve encounter"),
          c.onClick(async () => {
            await this.actions?.dissolveHyperedge(n);
          }));
      }),
      t.createEl("div", {
        cls: "simplicial-panel-footer",
        text: `order: ${i.nodes.length} \xB7 faces: none \xB7 kind: encounter`,
      }));
  }
  renderEncounterDiagnostics(t, n, i) {
    let s = mn(n, i),
      o = t.createDiv({ cls: "simplicial-diagnostics" });
    o.createEl("div", { cls: "simplicial-panel-section-label", text: "Diagnostics" });
    let a = n.closure;
    this.renderMeasure(
      o,
      "Closure deficit",
      a === null || a.deficit === null ? "unbounded" : a.deficit.toFixed(2),
      s.closure,
      a?.deficit ?? null,
    );
    let l = n.independence;
    (this.renderMeasure(
      o,
      "Face independence",
      l === null
        ? "not measured"
        : l.independence === null
          ? "n/a"
          : `${l.independence.toFixed(2)} \xB7 group evidence ${l.fullSetScore.toFixed(2)}`,
      s.independence,
      l?.independence ?? null,
    ),
      this.renderMeasure(
        o,
        "Persistence",
        `${n.occurrences.length || 1}\xD7 \xB7 vitality ${n.vitality.toFixed(1)}`,
        s.persistence,
        null,
      ));
    let d = n.peakOverlap;
    this.renderMeasure(o, "Overlap pressure", d ? d.pressure.toFixed(2) : "0.00", s.overlap, d?.pressure ?? null);
  }
  renderMeasure(t, n, i, s, o) {
    let a = t.createDiv({ cls: "simplicial-measure" }),
      l = a.createDiv({ cls: "simplicial-measure-head" });
    (l.createEl("span", { cls: "simplicial-measure-name", text: n }),
      l.createEl("span", { cls: "simplicial-measure-figure", text: i }),
      o !== null &&
        a
          .createDiv({ cls: "simplicial-measure-track" })
          .createDiv({
            cls: "simplicial-measure-bar",
            attr: { style: `width: ${Math.round(Math.max(0, Math.min(1, o)) * 100)}%;` },
          }),
      s && a.createEl("div", { cls: "simplicial-measure-reading", text: s }));
  }
  renderExplanationCard(t, n) {
    let s = this.model.getAnalysisSummary().betti?.holes ?? [],
      o = fn(n, this.nodeProfiles, new Map(), s),
      a = t.createDiv({ cls: "simplicial-explanation-card" });
    if (
      (a.createEl("div", { cls: "simplicial-explanation-headline", text: o.headline }),
      a.createEl("div", { cls: "simplicial-explanation-tension", text: o.tension }),
      a.createEl("div", { cls: "simplicial-explanation-prompt", text: o.prompt }),
      o.signals.length > 0)
    ) {
      let l = a.createDiv({ cls: "simplicial-explanation-signals" });
      (l.createEl("div", { cls: "simplicial-explanation-signals-label", text: "Evidence:" }),
        o.signals.forEach((d) => {
          l.createEl("div", { cls: "simplicial-explanation-signal", text: `\u2022 ${d}` });
        }));
    }
  }
  renderBadge(t, n, i, s = !1) {
    t.createEl("span", {
      cls: "simplicial-panel-badge",
      text: n,
      attr: {
        style: s
          ? `border-color: rgba(${i[0]},${i[1]},${i[2]},0.18); color: rgba(${i[0]},${i[1]},${i[2]},0.78);`
          : `background: rgba(${i[0]},${i[1]},${i[2]},0.12); border-color: rgba(${i[0]},${i[1]},${i[2]},0.28); color: rgb(${i[0]},${i[1]},${i[2]});`,
      },
    });
  }
};
async function Ze(r, e, t) {
  let n = e.replace(/[\\/:*?"<>|]/g, "-").trim() || "simplex",
    i = 0;
  for (;;) {
    let s = `${n}${i === 0 ? "" : `-${i}`}.md`;
    if (!(r.vault.getAbstractFileByPath(s) instanceof T.TFile)) return r.vault.create(s, t);
    i++;
  }
}
var Sn = require("obsidian");
function vn(r, e) {
  let t = [],
    n = [...r.simplices.entries()]
      .map(([a, l]) => ({ key: a, simplex: l, weight: Li(l, e) }))
      .sort((a, l) => l.weight - a.weight),
    i = new Set(),
    s = new Map(),
    o = new Set();
  for (let { simplex: a, weight: l } of n) {
    let d = a.nodes.length - 1;
    if (d === 0)
      for (let p of a.nodes)
        i.has(p) ||
          (i.add(p),
          s.set(p, new Set([p])),
          t.push({ threshold: l, type: "edge-appear", nodes: [p], description: `Node ${p} appears` }));
    else if (d === 1) {
      let [p, c] = a.nodes,
        u = s.get(p),
        h = s.get(c);
      if (u && h && u !== h) {
        let g = new Set([...u, ...h]);
        for (let f of g) s.set(f, g);
        t.push({
          threshold: l,
          type: "component-merge",
          nodes: [p, c],
          description: `Components merge via ${p} \u2194 ${c}`,
        });
      }
    } else if (d === 2) {
      let p = a.nodes.sort().join("|");
      Oi(a.nodes).every((h) => {
        let g = h.sort().join("|");
        return n.some((f) => f.simplex.nodes.length === 2 && f.simplex.nodes.sort().join("|") === g && f.weight > l);
      }) &&
        !o.has(p) &&
        (o.add(p),
        t.push({
          threshold: l,
          type: "triangle-close",
          nodes: a.nodes,
          description: `Triangle closes: ${a.nodes.join(" \xB7 ")}`,
        }));
    }
  }
  return t.sort((a, l) => l.threshold - a.threshold);
}
function Li(r, e) {
  return e === "confidence"
    ? (r.confidence ?? r.weight ?? 0)
    : e === "decayed-weight"
      ? (r.decayedWeight ?? r.weight ?? r.confidence ?? 0)
      : (r.weight ?? r.decayedWeight ?? r.confidence ?? 0);
}
function Oi(r) {
  let e = [];
  for (let t = 0; t < r.length; t++) for (let n = t + 1; n < r.length; n++) e.push([r[t], r[n]]);
  return e;
}
function bn(r) {
  return [...new Set(r.map((t) => Math.round(t.threshold * 100) / 100))].sort((t, n) => t - n);
}
var Ne = class extends Sn.ItemView {
  constructor(t, n, i, s, o, a) {
    super(t);
    this.model = n;
    this.renderer = i;
    this.settings = s;
    this.onSettingsChanged = o;
    this.filtrationEvents = [];
    this.eventMarkers = [];
    this.sliderWrap = null;
    this.sliderEl = null;
    ((this.onRescan = a),
      this.computeFiltrationEvents(),
      this.model.subscribe(() => {
        this.computeFiltrationEvents();
      }));
  }
  setRescanCallback(t) {
    this.onRescan = t;
  }
  computeFiltrationEvents() {
    if (!this.settings.showFiltrationSlider) {
      this.sliderWrap && this.sliderWrap.addClass("simplicial-hidden");
      return;
    }
    (this.sliderWrap && this.sliderWrap.removeClass("simplicial-hidden"),
      (this.filtrationEvents = vn(this.model, this.settings.renderFilterMetric)),
      this.updateEventMarkers());
  }
  refreshSettings() {
    this.computeFiltrationEvents();
  }
  getViewType() {
    return ee;
  }
  getDisplayText() {
    return "Simplicial graph";
  }
  async onOpen() {
    await Promise.resolve();
    let { contentEl: t } = this;
    (t.empty(), t.addClass("simplicial-view"));
    let n = t.createDiv({ cls: "simplicial-view-wrap" }),
      i = t.createDiv({ cls: "simplicial-hud" }),
      s = t.createDiv({ cls: "simplicial-legend" });
    (this.renderFiltrationControls(i), this.renderLegend(s));
    let o = t.createDiv({ cls: "simplicial-filters" });
    (this.addFilterToggle(
      o,
      "edges",
      () => this.settings.showEdges,
      (a) => (this.settings.showEdges = a),
    ),
      this.addFilterToggle(
        o,
        "clusters",
        () => this.settings.showClusters,
        (a) => (this.settings.showClusters = a),
      ),
      this.addFilterToggle(
        o,
        "cores",
        () => this.settings.showCores,
        (a) => (this.settings.showCores = a),
      ),
      this.renderFloatingControls(t),
      this.renderer.init(n));
  }
  async onClose() {
    (await Promise.resolve(), this.renderer.destroy());
  }
  addFilterToggle(t, n, i, s) {
    let o = t.createEl("button", { cls: `simplicial-filter ${i() ? "is-on" : ""}`, text: n });
    o.addEventListener("click", () => {
      let a = !i();
      (s(a), o.toggleClass("is-on", a), this.onSettingsChanged(), this.renderer.render());
    });
  }
  renderFiltrationControls(t) {
    this.sliderWrap = t.createDiv({ cls: "simplicial-filtration" });
    let n = this.sliderWrap.createDiv({ cls: "simplicial-filtration-header" });
    n.createSpan({ text: "Filter" });
    let i = n.createSpan({ cls: "simplicial-filtration-value", text: this.settings.renderFilterThreshold.toFixed(3) }),
      s = this.sliderWrap.createEl("select", { cls: "simplicial-filtration-metric" });
    [
      { value: "weight", label: "weight" },
      { value: "confidence", label: "confidence" },
      { value: "decayed-weight", label: "decayed" },
    ].forEach((f) => {
      let m = s.createEl("option", { text: f.label });
      ((m.value = f.value), (m.selected = this.settings.renderFilterMetric === f.value));
    });
    let a = this.sliderWrap.createDiv({ cls: "simplicial-filtration-sliders" }),
      l = a.createDiv({ cls: "simplicial-slider-row" });
    l.createSpan({ text: "Range", cls: "simplicial-slider-label" });
    let d = l.createEl("input", { type: "range", cls: "simplicial-filtration-slider coarse" });
    ((d.min = "0"),
      (d.max = "1"),
      (d.step = "0.1"),
      (d.value = String(Math.round(this.settings.renderFilterThreshold * 10) / 10)));
    let p = a.createDiv({ cls: "simplicial-slider-row" });
    p.createSpan({ text: "Fine", cls: "simplicial-slider-label" });
    let c = p.createEl("input", { type: "range", cls: "simplicial-filtration-slider fine" });
    ((c.min = "-0.05"), (c.max = "0.05"), (c.step = "0.001"), (c.value = "0"), (this.sliderEl = d));
    let u = Number(d.value),
      h = 0,
      g = () => {
        let f = u + h;
        return (
          (f = Math.max(0, Math.min(1, f))),
          i.setText(f.toFixed(3)),
          (this.settings.renderFilterThreshold = f),
          this.onSettingsChanged(),
          this.renderer.render(),
          f
        );
      };
    (d.addEventListener("input", () => {
      ((u = Number(d.value)), (h = 0), (c.value = "0"), g());
    }),
      c.addEventListener("input", () => {
        ((h = Number(c.value)), g());
      }),
      c.addEventListener("change", () => {
        let f = u + h;
        (f <= 0 || f >= 1) &&
          ((u = Math.max(0, Math.min(1, f))), (d.value = String(Math.round(u * 10) / 10)), (h = 0), (c.value = "0"));
      }),
      s.addEventListener("change", () => {
        ((this.settings.renderFilterMetric = s.value),
          this.onSettingsChanged(),
          this.renderer.render(),
          this.computeFiltrationEvents());
      }),
      this.updateEventMarkers());
  }
  renderFloatingControls(t) {
    let n = t.createDiv({ cls: "simplicial-floating-controls" }),
      i = n.createDiv({ cls: "simplicial-controls-toggle" });
    (i.setText("\u2699\uFE0F"), (i.title = "Canvas settings"));
    let s = n.createDiv({ cls: "simplicial-controls-panel" });
    (s.createDiv({ cls: "simplicial-control-header" }).setText("Inference"),
      this.addDualSlider(
        s,
        "Link Threshold",
        this.settings.linkStrengthThreshold,
        (u) => {
          ((this.settings.linkStrengthThreshold = u),
            this.onSettingsChanged(),
            this.onRescan?.("canvas-link-threshold-changed", 100));
        },
        this.settings.linkThresholdLowerBound,
        this.settings.linkThresholdUpperBound,
        (u, h) => {
          ((this.settings.linkThresholdLowerBound = u),
            (this.settings.linkThresholdUpperBound = h),
            this.onSettingsChanged());
        },
      ),
      this.addCanvasSlider(s, "Insight Threshold", this.settings.insightThreshold, 0, 1, 0.05, (u) => {
        ((this.settings.insightThreshold = u),
          this.onSettingsChanged(),
          this.onRescan?.("canvas-insight-threshold-changed", 100));
      }),
      this.addCanvasSlider(s, "Suggestion Min", this.settings.suggestionThreshold, 0.2, 0.95, 0.05, (u) => {
        ((this.settings.suggestionThreshold = u), this.onSettingsChanged(), this.renderer.render());
      }),
      s.createDiv({ cls: "simplicial-control-header" }).setText("Visibility"),
      this.addCanvasToggle(s, "Show Holes", this.settings.enableBettiComputation, (u) => {
        ((this.settings.enableBettiComputation = u), this.onSettingsChanged(), this.renderer.render());
      }),
      this.addCanvasToggle(s, "Suggestions", this.settings.showSuggestions, (u) => {
        ((this.settings.showSuggestions = u), this.onSettingsChanged(), this.renderer.render());
      }),
      s.createDiv({ cls: "simplicial-control-header" }).setText("Physics"),
      this.addCanvasSlider(s, "Repulsion", this.settings.repulsionStrength, 100, 2e3, 100, (u) => {
        ((this.settings.repulsionStrength = u), this.onSettingsChanged());
      }),
      this.addCanvasSlider(s, "Gravity", this.settings.gravityStrength, 1e-4, 0.02, 1e-4, (u) => {
        ((this.settings.gravityStrength = u), this.onSettingsChanged());
      }),
      s.createDiv({ cls: "simplicial-control-header" }).setText("Actions"),
      s
        .createDiv({ cls: "simplicial-control-row" })
        .createEl("button", { cls: "simplicial-control-button", text: "\u{1F504} rescan vault" })
        .addEventListener("click", () => {
          this.onRescan?.("manual-rescan", 0);
        }),
      i.addEventListener("click", () => {
        s.toggleClass("simplicial-hidden", !s.hasClass("simplicial-hidden"));
      }),
      s.addClass("simplicial-hidden"));
  }
  addCanvasToggle(t, n, i, s) {
    let o = t.createDiv({ cls: "simplicial-control-row" });
    o.createSpan({ text: n });
    let a = o.createEl("input", { type: "checkbox" });
    ((a.checked = i),
      a.addEventListener("change", () => {
        s(a.checked);
      }));
  }
  addCanvasSlider(t, n, i, s, o, a, l) {
    let d = t.createDiv({ cls: "simplicial-control-row" });
    d.createSpan({ text: n });
    let p = d.createEl("input", { type: "range" });
    ((p.min = String(s)), (p.max = String(o)), (p.step = String(a)), (p.value = String(i)));
    let c = a < 0.001 ? 4 : a < 0.01 ? 3 : 2,
      u = d.createSpan({ cls: "simplicial-control-value", text: i.toFixed(c) });
    p.addEventListener("input", () => {
      let h = Number(p.value);
      (u.setText(h.toFixed(c)), l(h));
    });
  }
  addDualSlider(t, n, i, s, o, a, l) {
    let d = t.createDiv({ cls: "simplicial-dual-slider-wrap" }),
      p = d.createDiv({ cls: "simplicial-dual-slider-header" });
    p.createSpan({ text: n });
    let c = p.createSpan({ cls: "simplicial-control-value", text: i.toFixed(4) }),
      u = d.createDiv({ cls: "simplicial-control-row" });
    u.createSpan({ text: "Range", cls: "simplicial-slider-label" });
    let h = u.createEl("input", { type: "range" });
    ((h.min = String(o)), (h.max = String(a)), (h.step = "0.01"), (h.value = String(i)));
    let g = d.createDiv({ cls: "simplicial-control-row" });
    g.createSpan({ text: "Fine", cls: "simplicial-slider-label" });
    let f = g.createEl("input", { type: "range" });
    ((f.min = "-0.05"), (f.max = "0.05"), (f.step = "0.001"), (f.value = "0"));
    let m = i,
      v = 0,
      y = () => {
        let b = Math.max(o, Math.min(a, m + v));
        return (c.setText(b.toFixed(4)), s(b), b);
      };
    (h.addEventListener("input", () => {
      ((m = Number(h.value)), (v = 0), (f.value = "0"), y());
    }),
      f.addEventListener("input", () => {
        ((v = Number(f.value)), y());
      }),
      l?.(o, a));
  }
  updateEventMarkers() {
    if (
      (this.eventMarkers.forEach((i) => i.remove()),
      (this.eventMarkers = []),
      !this.sliderWrap || !this.sliderEl || this.filtrationEvents.length === 0)
    )
      return;
    let t = bn(this.filtrationEvents);
    this.sliderEl.getBoundingClientRect().width !== 0 &&
      t.forEach((i) => {
        let s = this.sliderWrap.createDiv({ cls: "simplicial-filtration-marker" }),
          o = i * 100;
        (s.style.setProperty("left", `${o}%`), (s.title = `Event at ${i.toFixed(2)}`), this.eventMarkers.push(s));
      });
  }
  renderLegend(t) {
    [
      { label: "Link baseline", cls: "is-link" },
      { label: "Tag affinity", cls: "is-tag" },
      { label: "Folder affinity", cls: "is-folder" },
      { label: "Semantic overlap", cls: "is-semantic" },
      { label: "Soft cluster", cls: "is-cluster" },
      { label: "Confirmed simplex", cls: "is-confirmed" },
    ].forEach((i) => {
      let s = t.createDiv({ cls: "simplicial-legend-item" });
      (s.createSpan({ cls: `simplicial-legend-swatch ${i.cls}` }), s.createSpan({ text: i.label }));
    });
  }
};
var x = require("obsidian");
var Me = class extends x.PluginSettingTab {
  constructor(t, n) {
    super(t, n);
    this.plugin = n;
  }
  display() {
    let { containerEl: t } = this;
    (t.empty(),
      this.renderPersistenceSettings(t),
      this.renderHypergraphSettings(t),
      this.renderLayoutSettings(t),
      this.renderInferenceSettings(t),
      this.renderCommandUiSettings(t),
      this.renderBettiSettings(t),
      this.renderEmergentSettings(t),
      this.renderLegacySettings(t),
      this.refreshSettingVisibility());
  }
  renderPersistenceSettings(t) {
    (new x.Setting(t)
      .setName("Persistence mode")
      .setDesc("Choose where confirmed simplices are stored.")
      .addDropdown((n) => {
        (n.addOption("source-note", "Source note"),
          n.addOption("central-file", "Central file"),
          n.setValue(this.plugin.settings.persistenceMode),
          n.onChange(async (i) => {
            let s = i;
            ((this.plugin.settings.persistenceMode = s),
              s === "central-file" && (await B(this.app, this.plugin.settings.centralFile)),
              await this.plugin.saveSettings());
          }));
      }),
      new x.Setting(t).setName("Central file").addText((n) => {
        (n.setValue(this.plugin.settings.centralFile),
          n.onChange(async (i) => {
            ((this.plugin.settings.centralFile = i || "_simplicial.md"),
              this.plugin.settings.persistenceMode === "central-file" &&
                (await B(this.app, this.plugin.settings.centralFile)),
              await this.plugin.saveSettings());
          }));
      }));
  }
  renderLayoutSettings(t) {
    {
      let n = new x.Setting(t)
        .setName("Max rendered dimension")
        .setDesc("Highest simplex dimension to draw. A 10-node simplex has dimension 9.");
      this.addNumberSlider(n, this.plugin.settings.maxRenderedDim, 1, 12, 1, async (i) => {
        ((this.plugin.settings.maxRenderedDim = i), await this.plugin.saveSettings(), this.plugin.renderer.render());
      });
    }
    {
      let n = new x.Setting(t).setName("Noise amount");
      this.addNumberSlider(n, this.plugin.settings.noiseAmount, 0, 0.5, 0.01, async (i) => {
        ((this.plugin.settings.noiseAmount = i),
          this.plugin.engine.configure({ noiseAmount: i }),
          await this.plugin.saveSettings());
      });
    }
    {
      let n = new x.Setting(t).setName("Repulsion strength").setDesc("Higher values push nodes apart more strongly.");
      this.addNumberSlider(n, this.plugin.settings.repulsionStrength, 200, 6e3, 100, async (i) => {
        ((this.plugin.settings.repulsionStrength = i),
          this.plugin.engine.configure({ repulsionStrength: i }),
          await this.plugin.saveSettings());
      });
    }
    {
      let n = new x.Setting(t)
        .setName("Cohesion strength")
        .setDesc("Higher values pull connected simplices together more strongly.");
      this.addNumberSlider(n, this.plugin.settings.cohesionStrength, 0.001, 0.03, 0.001, async (i) => {
        ((this.plugin.settings.cohesionStrength = i),
          this.plugin.engine.configure({ cohesionStrength: i }),
          await this.plugin.saveSettings());
      });
    }
    {
      let n = new x.Setting(t)
        .setName("Gravity strength")
        .setDesc("Higher values keep nodes toward the center instead of drifting to the edges.");
      this.addNumberSlider(n, this.plugin.settings.gravityStrength, 1e-4, 0.01, 1e-4, async (i) => {
        ((this.plugin.settings.gravityStrength = i),
          this.plugin.engine.configure({ gravityStrength: i }),
          await this.plugin.saveSettings());
      });
    }
    {
      let n = new x.Setting(t)
        .setName("Damping")
        .setDesc("Higher values make motion settle more slowly and glide more.");
      this.addNumberSlider(n, this.plugin.settings.dampingFactor, 0.5, 0.99, 0.01, async (i) => {
        ((this.plugin.settings.dampingFactor = i),
          this.plugin.engine.configure({ dampingFactor: i }),
          await this.plugin.saveSettings());
      });
    }
    {
      let n = new x.Setting(t)
        .setName("Boundary padding")
        .setDesc("Minimum distance nodes keep from the canvas edges.");
      this.addNumberSlider(n, this.plugin.settings.boundaryPadding, 0, 200, 5, async (i) => {
        ((this.plugin.settings.boundaryPadding = i),
          this.plugin.engine.configure({ boundaryPadding: i }),
          await this.plugin.saveSettings());
      });
    }
    {
      let n = new x.Setting(t).setName("Sleep threshold");
      this.addNumberSlider(n, this.plugin.settings.sleepThreshold, 0.001, 0.1, 0.001, async (i) => {
        ((this.plugin.settings.sleepThreshold = i),
          this.plugin.engine.configure({ sleepThreshold: i }),
          await this.plugin.saveSettings());
      });
    }
    new x.Setting(t).setName("Dark mode").addDropdown((n) => {
      (n.addOption("auto", "Auto"),
        n.addOption("force-light", "Force light"),
        n.addOption("force-dark", "Force dark"),
        n.setValue(this.plugin.settings.darkMode),
        n.onChange(async (i) => {
          ((this.plugin.settings.darkMode = i), await this.plugin.saveSettings());
        }));
    });
  }
  renderHypergraphSettings(t) {
    (new x.Setting(t).setName("Hypergraph layer").setHeading(),
      new x.Setting(t)
        .setName("Show encounters")
        .setDesc(
          "Render hyperedges (\u25C7) as transient enclosures. An encounter records that notes came together as one irreducible whole, without asserting any pair within it.",
        )
        .addToggle((n) => {
          (n.setValue(this.plugin.settings.showHyperedges),
            n.onChange(async (i) => {
              ((this.plugin.settings.showHyperedges = i),
                await this.plugin.saveSettings(),
                this.plugin.renderer.render());
            }));
        }));
    {
      let n = new x.Setting(t)
        .setName("Encounter opacity")
        .setDesc("How present encounter enclosures are against the simplicial fields.");
      this.addNumberSlider(n, this.plugin.settings.hyperedgeOpacity, 0.1, 1, 0.05, async (i) => {
        ((this.plugin.settings.hyperedgeOpacity = i), await this.plugin.saveSettings(), this.plugin.renderer.render());
      });
    }
    {
      let n = new x.Setting(t)
        .setName("Recurrence threshold")
        .setDesc(
          "How many recorded encounters over the same notes mark a configuration as recurring. Recurrence enables crystallization; it never promotes anything on its own.",
        );
      this.addNumberSlider(n, this.plugin.settings.encounterRecurrenceThreshold, 2, 10, 1, async (i) => {
        ((this.plugin.settings.encounterRecurrenceThreshold = i), await this.plugin.saveSettings());
      });
    }
    (new x.Setting(t)
      .setName("Crystallize folder")
      .setDesc("Where notes created by 'crystallize concept' are placed. Leave empty for the vault root.")
      .addText((n) => {
        (n.setPlaceholder("Concepts"),
          n.setValue(this.plugin.settings.crystallizeFolder),
          n.onChange(async (i) => {
            ((this.plugin.settings.crystallizeFolder = i.trim()), await this.plugin.saveSettings());
          }));
      }),
      new x.Setting(t)
        .setName("Record relation history")
        .setDesc(
          "Keep an append-only log of how relations came to be \u2014 encountered, promoted, relaxed, crystallized, dissolved. Turning this off stops new entries; it never deletes existing ones.",
        )
        .addToggle((n) => {
          (n.setValue(this.plugin.settings.enableRelationHistory),
            n.onChange(async (i) => {
              ((this.plugin.settings.enableRelationHistory = i),
                await this.plugin.saveSettings(),
                new x.Notice(i ? "History resumes on next reload." : "History paused. Existing entries are kept."));
            }));
        }),
      new x.Setting(t)
        .setName("History file")
        .setDesc("Vault path of the append-only relation history.")
        .addText((n) => {
          (n.setPlaceholder("_simplicial-history.md"),
            n.setValue(this.plugin.settings.historyFile),
            n.onChange(async (i) => {
              ((this.plugin.settings.historyFile = i.trim() || "_simplicial-history.md"),
                this.plugin.historyStore.setPath(this.plugin.settings.historyFile),
                await this.plugin.saveSettings());
            }));
        }));
  }
  renderInferenceSettings(t) {
    (new x.Setting(t)
      .setName("Link graph baseline")
      .setDesc("Always show note-to-note vault links as 1-simplices, even without higher-order structure.")
      .addToggle((n) => {
        (n.setValue(this.plugin.settings.linkGraphBaseline),
          n.onChange(async (i) => {
            ((this.plugin.settings.linkGraphBaseline = i), await this.plugin.saveSettings());
          }));
      }),
      new x.Setting(t)
        .setName("Enable inferred edges")
        .setDesc("Use tags, links, titles, content, and folders to infer lightweight edges.")
        .addToggle((n) => {
          (n.setValue(this.plugin.settings.enableInferredEdges),
            n.onChange(async (i) => {
              ((this.plugin.settings.enableInferredEdges = i), await this.plugin.saveSettings());
            }));
        }));
    {
      let n = new x.Setting(t)
        .setName("Inference threshold")
        .setDesc("Minimum combined signal needed before an inferred edge is created.");
      this.addNumberSlider(n, this.plugin.settings.inferenceThreshold, 0.05, 0.6, 0.01, async (i) => {
        ((this.plugin.settings.inferenceThreshold = i), await this.plugin.saveSettings());
      });
    }
    new x.Setting(t)
      .setName("Show suggestions")
      .setDesc("Render closure and soft-cluster suggestions directly on the canvas.")
      .addToggle((n) => {
        (n.setValue(this.plugin.settings.showSuggestions),
          n.onChange(async (i) => {
            ((this.plugin.settings.showSuggestions = i),
              await this.plugin.saveSettings(),
              this.plugin.renderer.render());
          }));
      });
    {
      let n = new x.Setting(t)
        .setName("Suggestion threshold")
        .setDesc("Confidence level required before a suggestion is surfaced in the UI.");
      this.addNumberSlider(n, this.plugin.settings.suggestionThreshold, 0.2, 0.95, 0.01, async (i) => {
        ((this.plugin.settings.suggestionThreshold = i), await this.plugin.saveSettings());
      });
    }
  }
  renderCommandUiSettings(t) {
    {
      let n = new x.Setting(t)
        .setName("Command simplex size")
        .setDesc("How many nodes the create-from-open-note command tries to include.");
      this.addNumberSlider(n, this.plugin.settings.commandSimplexSize, 2, 6, 1, async (i) => {
        ((this.plugin.settings.commandSimplexSize = i), await this.plugin.saveSettings());
      });
    }
    new x.Setting(t)
      .setName("Formal mode")
      .setDesc("Switch from ambient blobs to a crisper geometric rendering with analysis overlays.")
      .addToggle((n) => {
        (n.setValue(this.plugin.settings.formalMode),
          n.onChange(async (i) => {
            ((this.plugin.settings.formalMode = i), await this.plugin.saveSettings(), this.plugin.renderer.render());
          }));
      });
    {
      let n = new x.Setting(t).setName("Sparse edge length").setDesc("Preferred spacing for sparse link-only graphs.");
      this.addNumberSlider(n, this.plugin.settings.sparseEdgeLength, 60, 280, 5, async (i) => {
        ((this.plugin.settings.sparseEdgeLength = i),
          this.plugin.engine.configure({ sparseEdgeLength: i }),
          await this.plugin.saveSettings());
      });
    }
    {
      let n = new x.Setting(t)
        .setName("Sparse gravity boost")
        .setDesc("Extra centering force when the graph is mostly pairwise and sparse.");
      this.addNumberSlider(n, this.plugin.settings.sparseGravityBoost, 1, 4, 0.1, async (i) => {
        ((this.plugin.settings.sparseGravityBoost = i),
          this.plugin.engine.configure({ sparseGravityBoost: i }),
          await this.plugin.saveSettings());
      });
    }
    {
      let n = new x.Setting(t)
        .setName("Label density")
        .setDesc("Controls how many non-focused labels are allowed before decluttering hides the rest.");
      this.addNumberSlider(n, this.plugin.settings.labelDensity, 0.1, 1, 0.05, async (i) => {
        ((this.plugin.settings.labelDensity = i), await this.plugin.saveSettings(), this.plugin.renderer.render());
      });
    }
    new x.Setting(t)
      .setName("Filtration metric")
      .setDesc("Choose which simplex strength field the live filtration slider uses.")
      .addDropdown((n) => {
        (n.addOption("weight", "Weight"),
          n.addOption("confidence", "Confidence"),
          n.addOption("decayed-weight", "Decayed weight"),
          n.setValue(this.plugin.settings.renderFilterMetric),
          n.onChange(async (i) => {
            ((this.plugin.settings.renderFilterMetric = i),
              await this.plugin.saveSettings(),
              this.plugin.renderer.render());
          }));
      });
    {
      let n = new x.Setting(t)
        .setName("Filtration threshold")
        .setDesc("Hide simplices below this threshold in the active filtration metric.");
      this.addNumberSlider(n, this.plugin.settings.renderFilterThreshold, 0, 1, 0.01, async (i) => {
        ((this.plugin.settings.renderFilterThreshold = i),
          await this.plugin.saveSettings(),
          this.plugin.renderer.render());
      });
    }
    new x.Setting(t)
      .setName("Open metadata panel after create")
      .setDesc("Show the metadata panel immediately after the command creates a simplex.")
      .addToggle((n) => {
        (n.setValue(this.plugin.settings.commandAutoOpenPanel),
          n.onChange(async (i) => {
            ((this.plugin.settings.commandAutoOpenPanel = i), await this.plugin.saveSettings());
          }));
      });
    {
      let n = new x.Setting(t)
        .setName("Metadata hover delay")
        .setDesc("Delay before hover-driven metadata UI should appear.");
      this.addNumberSlider(n, this.plugin.settings.metadataHoverDelayMs, 250, 2e3, 50, async (i) => {
        ((this.plugin.settings.metadataHoverDelayMs = i), await this.plugin.saveSettings());
      });
    }
  }
  renderBettiSettings(t) {
    (new x.Setting(t)
      .setName("Enable betti computation")
      .setDesc("Calculate topological invariants (\u03B2\u2080, \u03B2\u2081, \u03B2\u2082) to detect holes and voids.")
      .addToggle((n) => {
        (n.setValue(this.plugin.settings.enableBettiComputation),
          n.onChange(async (i) => {
            ((this.plugin.settings.enableBettiComputation = i),
              await this.plugin.saveSettings(),
              this.plugin.simplicialView?.refreshSettings(),
              this.plugin.renderer.render(),
              new x.Notice(i ? "Betti computation enabled" : "Betti computation disabled"));
          }));
      }),
      new x.Setting(t)
        .setName("Display betti on canvas")
        .setDesc("Show live betti numbers in the top-left hud overlay (requires betti computation to be enabled).")
        .addToggle((n) => {
          (n.setValue(this.plugin.settings.bettiDisplayOnCanvas),
            n.onChange(async (i) => {
              ((this.plugin.settings.bettiDisplayOnCanvas = i),
                await this.plugin.saveSettings(),
                this.plugin.renderer.render(),
                new x.Notice(i ? "Betti HUD will appear in top-left of graph" : "Betti HUD hidden"));
            }));
        }),
      new x.Setting(t)
        .setName("Max betti dimension")
        .setDesc("Compute holes up to this dimension (1 = triangles, 2 = tetrahedra).")
        .addDropdown((n) => {
          (n.addOption("1", "\u03B2\u2081 only (unfilled triangles)"),
            n.addOption("2", "\u03B2\u2081 and \u03B2\u2082 (including voids)"),
            n.setValue(String(this.plugin.settings.maxBettiDim)),
            n.onChange(async (i) => {
              ((this.plugin.settings.maxBettiDim = Number(i)),
                await this.plugin.saveSettings(),
                this.plugin.renderer.render());
            }));
        }),
      new x.Setting(t)
        .setName("Show filtration slider")
        .setDesc("Enable the slider UI with topological event markers in the graph view. (requires reopening the view)")
        .addToggle((n) => {
          (n.setValue(this.plugin.settings.showFiltrationSlider),
            n.onChange(async (i) => {
              ((this.plugin.settings.showFiltrationSlider = i),
                await this.plugin.saveSettings(),
                this.plugin.simplicialView?.refreshSettings(),
                new x.Notice(i ? "Filtration slider enabled" : "Filtration slider hidden"));
            }));
        }),
      new x.Setting(t)
        .setName("Enable explanation panel")
        .setDesc("Show human-readable explanations for inferred simplices in the metadata panel.")
        .addToggle((n) => {
          (n.setValue(this.plugin.settings.enableExplanationPanel),
            n.onChange(async (i) => {
              ((this.plugin.settings.enableExplanationPanel = i),
                await this.plugin.saveSettings(),
                this.plugin.panelView?.setSettings(this.plugin.settings),
                new x.Notice(i ? "Explanation cards enabled" : "Explanation cards disabled"));
            }));
        }));
  }
  renderEmergentSettings(t) {
    (new x.Setting(t).setName("Inference engine (v2)").setHeading(),
      t.createEl("p", {
        cls: "setting-item-description",
        text: "The plugin has two inference systems: emergent (graph-based with semantic clustering) and legacy (rule-based). Choose which to use.",
      }),
      new x.Setting(t)
        .setName("Inference mode")
        .setDesc("Emergent = semantic graph analysis | legacy = rule-based heuristics | hybrid = both")
        .addDropdown((i) => {
          (i.addOption("emergent", "Emergent (semantic graph)"),
            i.addOption("taxonomic", "Legacy (rule-based)"),
            i.addOption("hybrid", "Hybrid (both systems)"),
            i.setValue(this.plugin.settings.inferenceMode),
            i.onChange(async (s) => {
              ((this.plugin.settings.inferenceMode = s),
                await this.plugin.saveSettings(),
                new x.Notice(`Inference mode: ${s}. Rescanning vault...`),
                this.plugin.scheduleFullScan("inference-mode-changed", 100),
                this.refreshSettingVisibility());
            }));
        }));
    let n = t.createDiv({ cls: "emergent-settings" });
    (new x.Setting(n).setName("Emergent inference").setHeading(),
      new x.Setting(n)
        .setName("Domain source")
        .setDesc("How note domains are determined for coloring and edge strength.")
        .addDropdown((i) => {
          (i.addOption("folder", "Folder structure"),
            i.addOption("content-cluster", "Content clustering (tf-idf)"),
            i.addOption("hybrid", "Hybrid (folder + content)"),
            i.setValue(this.plugin.settings.domainSource),
            i.onChange(async (s) => {
              ((this.plugin.settings.domainSource = s),
                await this.plugin.saveSettings(),
                new x.Notice(`Domain source: ${s}. Rescanning...`),
                this.plugin.scheduleFullScan("domain-source-changed", 100));
            }));
        }));
    {
      let i = new x.Setting(n)
        .setName("Content cluster count")
        .setDesc("Number of semantic clusters (used when domain source is content-cluster or hybrid).");
      this.addNumberSlider(i, this.plugin.settings.contentClusterCount, 2, 12, 1, async (s) => {
        ((this.plugin.settings.contentClusterCount = s),
          await this.plugin.saveSettings(),
          this.plugin.settings.domainSource !== "folder" &&
            (new x.Notice(`Cluster count: ${s}. Rescanning...`),
            this.plugin.scheduleFullScan("cluster-count-changed", 100)));
      });
    }
    {
      let i = new x.Setting(n)
        .setName("Link strength threshold")
        .setDesc(
          "Minimum edge strength for emergent mode to create a visible link (0.0 = all edges, 1.0 = only strongest).",
        );
      this.addNumberSlider(i, this.plugin.settings.linkStrengthThreshold, 0, 1, 0.01, async (s) => {
        ((this.plugin.settings.linkStrengthThreshold = s),
          await this.plugin.saveSettings(),
          new x.Notice(`Link threshold: ${s.toFixed(2)}. Rescanning...`),
          this.plugin.scheduleFullScan("link-threshold-changed", 100));
      });
    }
    this._emergentSettingsDiv = n;
  }
  renderLegacySettings(t) {
    (new x.Setting(t).setName("Legacy inference weights").setHeading(),
      t.createEl("p", {
        cls: "setting-item-description",
        text: "These weights only apply when using legacy or hybrid inference mode. They control rule-based edge detection.",
      }),
      this.addWeightSlider(
        t,
        "Link weight",
        "Strength added by a resolved outbound link.",
        "linkWeight",
        "enableLinkInference",
        0,
        0.6,
        0.01,
      ),
      this.addWeightSlider(
        t,
        "Mutual link bonus",
        "Extra weight when both notes link each other.",
        "mutualLinkBonus",
        "enableMutualLinkBonus",
        0,
        0.6,
        0.01,
      ),
      this.addWeightSlider(
        t,
        "Shared tag weight",
        "Weight contributed by each shared tag.",
        "sharedTagWeight",
        "enableSharedTags",
        0,
        0.2,
        0.01,
      ),
      this.addWeightSlider(
        t,
        "Title overlap weight",
        "Maximum title-token overlap contribution.",
        "titleOverlapWeight",
        "enableTitleOverlap",
        0,
        0.3,
        0.01,
      ),
      this.addWeightSlider(
        t,
        "Content overlap weight",
        "Maximum body-text overlap contribution.",
        "contentOverlapWeight",
        "enableContentOverlap",
        0,
        0.3,
        0.01,
      ),
      this.addWeightSlider(
        t,
        "Same folder weight",
        "Boost when two notes share the same folder (Legacy mode only).",
        "sameFolderWeight",
        "enableSameFolderInference",
        0,
        0.2,
        0.01,
      ),
      this.addWeightSlider(
        t,
        "Top folder weight",
        "Boost when two notes share the same top-level folder (Legacy mode only).",
        "sameTopFolderWeight",
        "enableSameTopFolderInference",
        0,
        0.2,
        0.01,
      ));
  }
  refreshSettingVisibility() {
    let t = this._emergentSettingsDiv;
    if (!t) return;
    let n = this.plugin.settings.inferenceMode === "emergent" || this.plugin.settings.inferenceMode === "hybrid";
    t.style.display = n ? "block" : "none";
  }
  addNumberSlider(t, n, i, s, o, a) {
    t.addSlider((l) => {
      let d = t.controlEl.createSpan({ cls: "simplicial-setting-value" }),
        p = (c) => {
          let u = o >= 1 ? 0 : (`${o}`.split(".")[1]?.length ?? 0);
          return c
            .toFixed(u)
            .replace(/\.0+$/, "")
            .replace(/(\.\d*?)0+$/, "$1");
        };
      (d.setText(p(n)),
        l.setLimits(i, s, o),
        l.setValue(n),
        l.onChange(async (c) => {
          (d.setText(p(c)), await a(c));
        }));
    });
  }
  addWeightSlider(t, n, i, s, o, a, l, d) {
    let p = new x.Setting(t).setName(n).setDesc(i),
      c = null,
      u = (h) => {
        let g = d >= 1 ? 0 : (`${d}`.split(".")[1]?.length ?? 0);
        return h
          .toFixed(g)
          .replace(/\.0+$/, "")
          .replace(/(\.\d*?)0+$/, "$1");
      };
    (p.addToggle((h) => {
      (h.setTooltip("Enable or disable this inference signal"),
        h.setValue(this.plugin.settings[o]),
        h.onChange(async (g) => {
          ((this.plugin.settings[o] = g), c?.setDisabled(!g), await this.plugin.saveSettings());
        }));
    }),
      p.addSlider((h) => {
        c = h;
        let g = p.controlEl.createSpan({ cls: "simplicial-setting-value" });
        (g.setText(u(this.plugin.settings[s])),
          h.setLimits(a, l, d),
          h.setValue(this.plugin.settings[s]),
          h.setDisabled(!this.plugin.settings[o]),
          h.onChange(async (f) => {
            (g.setText(u(f)), (this.plugin.settings[s] = f), await this.plugin.saveSettings());
          }));
      }));
  }
};
var Ee = class extends M.Plugin {
  constructor() {
    super(...arguments);
    this.panelView = null;
    this.simplicialView = null;
    this.saveTimer = null;
    this.rescanTimer = null;
    this.subsetScorer = null;
  }
  async onload() {
    let t = (await this.loadData()) ?? {};
    ((this.settings = { ...Ct(), ...t }),
      this.settings.maxRenderedDim === 3 && (this.settings.maxRenderedDim = 12),
      C.info("plugin", "Loading plugin", {
        persistenceMode: this.settings.persistenceMode,
        centralFile: this.settings.centralFile,
        showEdges: this.settings.showEdges,
        showClusters: this.settings.showClusters,
        showCores: this.settings.showCores,
        pinnedNodeCount: Object.keys(this.settings.pinnedNodes).length,
      }),
      (this.model = new de()),
      (this.history = new ce()),
      (this.historyStore = new he(this.app, this.settings.historyFile)),
      this.settings.enableRelationHistory && this.history.onAppend((i) => this.historyStore.record(i)),
      (this.engine = new me()),
      this.engine.configure({
        noiseAmount: this.settings.noiseAmount,
        sleepThreshold: this.settings.sleepThreshold,
        repulsionStrength: this.settings.repulsionStrength,
        cohesionStrength: this.settings.cohesionStrength,
        gravityStrength: this.settings.gravityStrength,
        dampingFactor: this.settings.dampingFactor,
        boundaryPadding: this.settings.boundaryPadding,
        sparseEdgeLength: this.settings.sparseEdgeLength,
        sparseGravityBoost: this.settings.sparseGravityBoost,
      }),
      (this.controller = new ge(
        this.model,
        () => this.engine.wake(),
        (i) => this.panelView?.setSelection(i),
        (i) => {
          this.openPanel(i, !1);
        },
        () => this.queueSaveSettings(),
        (i) => this.saveInteractionState(i),
      )));
    let n = this.settings.interactionState;
    (n && this.controller.setInteractionTracker(ut(n)),
      (this.renderer = new be(this.model, this.engine, this.controller, this.settings, {
        onContextMenu: (i, s) => this.openCanvasContextMenu(i, s),
        onLassoCreate: (i) => {
          this.openCreateSimplexModal(i, i[0] ?? "");
        },
        onNodeOpen: (i) => {
          this.openNodeNote(i);
        },
        onHoleHover: (i, s) => {
          if (i && s) {
            let o = i.boundaryNodes.map((a) => a.split("/").pop()?.replace(/\.md$/, "") ?? a);
            new M.Notice(
              `Hole: ${s.headline}
${o.join(" \xB7 ")}`,
              3e3,
            );
          }
        },
        onHoleClick: (i, s) => {
          let o = i.boundaryNodes.map((a) => a.split("/").pop()?.replace(/\.md$/, "") ?? a);
          new M.Notice(
            `\u{1F573}\uFE0F ${s.headline}

Notes: ${o.join(" \xB7 ")}

${s.prompt}`,
            8e3,
          );
        },
      })),
      (this.index = new pe(
        this.app,
        this.model,
        this.settings,
        () => this.engine.wake(),
        (i) => this.recordEncounter(i, "parser"),
      )),
      this.restorePinnedNodes(),
      this.registerView(ee, (i) => {
        let s = new Ne(
          i,
          this.model,
          this.renderer,
          this.settings,
          () => this.queueSaveSettings(),
          (o, a) => this.scheduleFullScan(o, a),
        );
        return ((this.simplicialView = s), s);
      }),
      this.registerView(q, (i) => {
        let s = new Ce(i, this.model);
        return (
          s.setActions({
            saveMetadata: (o, a) => this.persistSimplexMetadata(o, a),
            promoteSimplex: (o) => this.promoteSimplex(o),
            dissolveSimplex: (o) => this.dissolveSimplex(o),
            relaxSimplex: (o) => this.relaxSimplex(o),
            saveHyperedgeMetadata: (o, a) => this.saveHyperedgeMetadata(o, a),
            promoteEncounter: (o) => this.promoteEncounter(o),
            crystallizeEncounter: (o) => this.crystallizeEncounter(o),
            dissolveHyperedge: (o) => this.dissolveHyperedge(o),
          }),
          s.setHistory(this.history),
          s.setSettings(this.settings),
          s.setSubsetScorer(this.subsetScorer),
          (this.panelView = s),
          s
        );
      }),
      this.addRibbonIcon("network", "Simplicial graph", () => {
        this.activateView();
      }),
      this.addCommand({
        id: "open-simplicial",
        name: "Open simplicial graph",
        callback: () => {
          this.activateView();
        },
      }),
      this.addCommand({
        id: "insert-simplex-symbol",
        name: "Insert triangle simplex marker",
        editorCallback: (i) => i.replaceSelection("\u25B3 "),
      }),
      this.addCommand({
        id: "insert-hyperedge-symbol",
        name: "Insert encounter hyperedge marker",
        editorCallback: (i) => i.replaceSelection("\u25C7 "),
      }),
      this.addCommand({
        id: "form-simplex-from-open-note",
        name: "Simplicial: form simplex from open note",
        callback: () => {
          this.formSimplexFromOpenNote();
        },
      }),
      this.addCommand({
        id: "create-encounter",
        name: "Simplicial: create encounter from open note",
        callback: () => {
          this.createEncounterFromOpenNote();
        },
      }),
      this.addCommand({
        id: "toggle-edges",
        name: "Toggle simplicial edges",
        callback: () => {
          activeDocument.activeElement?.tagName === "INPUT" ||
            activeDocument.activeElement?.tagName === "TEXTAREA" ||
            ((this.settings.showEdges = !this.settings.showEdges), this.saveSettings(), this.renderer.render());
        },
      }),
      this.addCommand({
        id: "toggle-clusters",
        name: "Toggle simplicial clusters",
        callback: () => {
          activeDocument.activeElement?.tagName === "INPUT" ||
            activeDocument.activeElement?.tagName === "TEXTAREA" ||
            ((this.settings.showClusters = !this.settings.showClusters), this.saveSettings(), this.renderer.render());
        },
      }),
      this.addCommand({
        id: "toggle-cores",
        name: "Toggle simplicial cores",
        callback: () => {
          activeDocument.activeElement?.tagName === "INPUT" ||
            activeDocument.activeElement?.tagName === "TEXTAREA" ||
            ((this.settings.showCores = !this.settings.showCores), this.saveSettings(), this.renderer.render());
        },
      }),
      this.addCommand({
        id: "clear-simplicial-focus",
        name: "Clear simplicial focus",
        callback: () => {
          if (
            activeDocument.activeElement?.tagName === "INPUT" ||
            activeDocument.activeElement?.tagName === "TEXTAREA"
          ) {
            activeDocument.activeElement.blur();
            return;
          }
          (this.controller.clearFocus(), this.renderer.render());
        },
      }),
      this.addCommand({
        id: "focus-hovered-node",
        name: "Focus hovered simplicial node",
        callback: () => {
          activeDocument.activeElement?.tagName === "INPUT" ||
            activeDocument.activeElement?.tagName === "TEXTAREA" ||
            (this.controller.focusHoveredNode(), this.renderer.render());
        },
      }),
      this.addCommand({
        id: "open-hovered-simplex-panel",
        name: "Open metadata panel for hovered simplex",
        callback: () => {
          activeDocument.activeElement?.tagName === "INPUT" ||
            activeDocument.activeElement?.tagName === "TEXTAREA" ||
            this.openPanelForCurrentSelection();
        },
      }),
      this.addSettingTab(new Me(this.app, this)),
      this.model.subscribe(() => {
        this.engine.wake();
      }),
      await this.logPersistenceState(),
      this.settings.enableRelationHistory && (await this.historyStore.load(this.history), this.syncEncounterState()),
      this.scheduleFullScan("startup", 0),
      this.app.workspace.onLayoutReady(() => this.scheduleFullScan("layout-ready", 50)),
      this.registerEvent(this.app.metadataCache.on("resolved", () => this.scheduleFullScan("metadata-resolved", 50))));
  }
  onunload() {
    (this.rescanTimer !== null && window.clearTimeout(this.rescanTimer),
      C.info("plugin", "Unloading plugin", {
        indexedNodeCount: this.model.nodes.size,
        simplexCount: this.model.simplices.size,
        hyperedgeCount: this.model.hyperedges.size,
        historyEventCount: this.history.size,
      }),
      this.renderer.destroy(),
      this.index.destroy());
  }
  restorePinnedNodes() {
    (C.info("plugin", "Restoring pinned nodes", { pinnedNodeCount: Object.keys(this.settings.pinnedNodes).length }),
      Object.entries(this.settings.pinnedNodes).forEach(([t, n]) => {
        this.model.setNode(t, { isPinned: !0, px: n.px, py: n.py });
      }));
  }
  async saveSettings() {
    let t = {};
    (this.model.getAllNodes().forEach((n) => {
      n.isPinned && (t[n.id] = { px: n.px, py: n.py });
    }),
      (this.settings.pinnedNodes = t),
      await this.saveData(this.settings),
      this.index?.updateSettings(this.settings),
      C.info("plugin", "Saved persistence state", {
        persistenceMode: this.settings.persistenceMode,
        centralFile: this.settings.centralFile,
        pinnedNodeCount: Object.keys(this.settings.pinnedNodes).length,
        filters: {
          edges: this.settings.showEdges,
          clusters: this.settings.showClusters,
          cores: this.settings.showCores,
        },
        inference: {
          linkBaseline: this.settings.linkGraphBaseline,
          enabled: this.settings.enableInferredEdges,
          threshold: this.settings.inferenceThreshold,
          suggestions: this.settings.showSuggestions,
          suggestionThreshold: this.settings.suggestionThreshold,
        },
        layout: {
          repulsion: this.settings.repulsionStrength,
          cohesion: this.settings.cohesionStrength,
          gravity: this.settings.gravityStrength,
          damping: this.settings.dampingFactor,
          boundaryPadding: this.settings.boundaryPadding,
          sparseEdgeLength: this.settings.sparseEdgeLength,
          sparseGravityBoost: this.settings.sparseGravityBoost,
          labelDensity: this.settings.labelDensity,
          renderFilterMetric: this.settings.renderFilterMetric,
          renderFilterThreshold: this.settings.renderFilterThreshold,
        },
        commandUi: {
          simplexSize: this.settings.commandSimplexSize,
          autoOpenPanel: this.settings.commandAutoOpenPanel,
          metadataHoverDelayMs: this.settings.metadataHoverDelayMs,
          formalMode: this.settings.formalMode,
        },
      }));
  }
  queueSaveSettings() {
    (this.saveTimer !== null && window.clearTimeout(this.saveTimer),
      (this.saveTimer = window.setTimeout(() => {
        ((this.saveTimer = null), this.saveSettings());
      }, 150)));
  }
  saveInteractionState(t) {
    ((this.settings.interactionState = ht(t)), this.queueSaveSettings());
  }
  async activateView() {
    await this.app.workspace.getLeaf(!0).setViewState({ type: ee, active: !0 });
    let t = this.app.workspace.getRightLeaf(!1);
    t && (await t.setViewState({ type: q, active: !1 }));
  }
  async persistSimplexMetadata(t, n) {
    (C.info("plugin", "Persisting simplex metadata", {
      simplexKey: t,
      updates: n,
      persistenceMode: this.settings.persistenceMode,
    }),
      this.model.updateMetadata(t, n));
    let i = this.model.getSimplex(t);
    if (!i?.sourcePath) {
      (C.warn("plugin", "Simplex has no sourcePath; only settings state will be saved", { simplexKey: t }),
        await this.saveSettings());
      return;
    }
    (await this.persistSimplex(i), await this.saveSettings());
  }
  formSimplexFromOpenNote() {
    let n = this.app.workspace.getActiveViewOfType(M.MarkdownView)?.file;
    if (!n) {
      new M.Notice("Open a note first.");
      return;
    }
    let s = this.app.metadataCache.getFileCache(n)?.links?.map((d) => d.link) ?? [],
      o = s
        .map((d) => this.app.metadataCache.getFirstLinkpathDest(d, n.path)?.path ?? d)
        .filter((d, p, c) => c.indexOf(d) === p),
      a = Math.max(2, Math.min(6, this.settings.commandSimplexSize)),
      l = [n.path, ...o].slice(0, a);
    if (
      (C.info("plugin", "Form simplex from open note requested", {
        sourcePath: n.path,
        linkCount: s.length,
        desiredSize: a,
        proposedNodes: l,
      }),
      l.length < a)
    ) {
      new M.Notice(`Need at least ${a - 1} resolvable outgoing links to form this simplex.`);
      return;
    }
    this.openCreateSimplexModal(l, n.path);
  }
  recordEncounter(t, n) {
    let i = this.history.occurrencesOf(t.nodes);
    (n === "parser" && i.length > 0) ||
      (this.history.append({
        type: i.length > 0 ? "recurred" : "encountered",
        kind: "hyperedge",
        nodes: t.nodes,
        actor: n,
        ...(t.label || t.mode
          ? { detail: { ...(t.label ? { label: t.label } : {}), ...(t.mode ? { mode: t.mode } : {}) } }
          : {}),
      }),
      this.syncEncounterState());
  }
  syncEncounterState() {
    st(this.model, this.history, this.settings.encounterRecurrenceThreshold);
  }
  rebuildSubsetScorer() {
    let t = this.index.getInferenceContexts();
    ((this.subsetScorer = t.length > 0 ? dt(t, this.settings) : null),
      this.panelView?.setSubsetScorer(this.subsetScorer));
  }
  createEncounterFromOpenNote() {
    let n = this.app.workspace.getActiveViewOfType(M.MarkdownView)?.file;
    if (!n) {
      new M.Notice("Open a note first.");
      return;
    }
    let o = (this.app.metadataCache.getFileCache(n)?.links?.map((l) => l.link) ?? [])
        .map((l) => this.app.metadataCache.getFirstLinkpathDest(l, n.path)?.path ?? l)
        .filter((l, d, p) => p.indexOf(l) === d),
      a = [n.path, ...o];
    if (a.length < 2) {
      new M.Notice("Need at least one resolvable outgoing link to record an encounter.");
      return;
    }
    (C.info("plugin", "Create encounter from open note requested", { sourcePath: n.path, participantCount: a.length }),
      this.openCreateRelationModal(a, n.path, "hyperedge"));
  }
  async createHyperedge(t, n) {
    let i = t.nodes.map((l) => this.resolveDraftNode(l, n)),
      s = this.settings.persistenceMode === "central-file" ? this.settings.centralFile : n,
      o = {
        nodes: i,
        label: t.label,
        weight: t.weight,
        mode: t.mode ?? "encounter",
        occurredAt: Date.now(),
        persistence: "momentary",
        sourcePath: s,
      },
      a = this.model.addHyperedge(o);
    return a
      ? (this.recordEncounter({ ...o, nodes: this.model.getHyperedge(a).nodes }, "user"),
        await this.persistHyperedge(this.model.getHyperedge(a)),
        a)
      : "";
  }
  async persistHyperedge(t) {
    if (
      t.sourcePath === this.settings.centralFile ||
      (!t.sourcePath && this.settings.persistenceMode === "central-file")
    ) {
      let { file: o, content: a } = await bt(this.app, this.settings.centralFile, {
        ...t,
        sourcePath: this.settings.centralFile,
      });
      (await this.app.vault.modify(o, a), this.index.recordWrite(o.path, a));
      return;
    }
    let i = this.app.vault.getAbstractFileByPath(t.sourcePath ?? "");
    if (!(i instanceof M.TFile)) {
      C.warn("plugin", "Unable to persist hyperedge to source note", { nodeKey: N(t.nodes), sourcePath: t.sourcePath });
      return;
    }
    let s = await vt(this.app, i, t);
    (await this.app.vault.modify(i, s), this.index.recordWrite(i.path, s));
  }
  async removeHyperedgeFromNote(t) {
    let n = N(t.nodes),
      s =
        t.sourcePath === this.settings.centralFile ||
        (!t.sourcePath && this.settings.persistenceMode === "central-file")
          ? await B(this.app, this.settings.centralFile)
          : this.app.vault.getAbstractFileByPath(t.sourcePath ?? "");
    if (!(s instanceof M.TFile)) return;
    let o = await St(this.app, s, n);
    (await this.app.vault.modify(s, o), this.index.recordWrite(s.path, o));
  }
  promoteEncounter(t) {
    let n = this.model.getHyperedge(t);
    if (!n) return;
    let i = this.model.facesImpliedByPromotion(t);
    new xe(this.app, n.nodes, i, async () => {
      let s = this.model.promoteToSimplex(t);
      if (!s) return;
      this.history.append({
        type: "promoted",
        kind: "hyperedge",
        nodes: n.nodes,
        actor: "user",
        prior: { persistence: n.persistence ?? "momentary" },
        detail: { createdFaceCount: s.createdFaces.length },
      });
      let o = this.model.getSimplex(s.simplexKey);
      (o && (await this.persistSimplex(o)),
        await this.persistHyperedge(this.model.getHyperedge(t)),
        this.controller.selectSimplex(s.simplexKey),
        await this.openPanel({ kind: "simplex", key: s.simplexKey }, !1),
        new M.Notice(
          s.createdFaces.length > 0
            ? `Promoted. ${s.createdFaces.length} face${s.createdFaces.length === 1 ? "" : "s"} asserted.`
            : "Promoted. Every implied face already existed.",
        ));
    }).open();
  }
  async relaxSimplex(t) {
    let n = this.model.getSimplex(t);
    if (!n || n.autoGenerated) return;
    let i = this.model.relaxToHyperedge(t);
    if (!i) return;
    this.history.append({
      type: "relaxed",
      kind: "simplex",
      nodes: n.nodes,
      actor: "user",
      prior: { label: n.label ?? null, weight: n.weight ?? null },
    });
    let s = this.app.vault.getAbstractFileByPath(n.sourcePath ?? "");
    if (s instanceof M.TFile) {
      let o = await ne(this.app, s, t);
      (await this.app.vault.modify(s, o), this.index.recordWrite(s.path, o));
    }
    (await this.persistHyperedge(this.model.getHyperedge(i)),
      this.controller.selectHyperedge(i),
      await this.openPanel({ kind: "hyperedge", key: i }, !1),
      new M.Notice("Relaxed to encounter. The group relation is kept; its faces are not asserted."));
  }
  async crystallizeEncounter(t) {
    let n = this.model.getHyperedge(t);
    if (!n) return;
    let i = n.label?.trim() || `encounter-${N(n.nodes).replace(/[|/]/g, "-")}`,
      s = this.settings.crystallizeFolder.replace(/\/+$/, ""),
      a = [
        "---",
        "crystallizedFrom:",
        n.nodes.map((d) => `  - "[[${d.replace(/\.md$/, "")}]]"`).join(`
`),
        `crystallizedAt: ${Date.now()}`,
        "---",
        "",
        `# ${i}`,
        "",
        "This note names a concept that emerged from a recurring encounter between:",
        "",
        ...n.nodes.map((d) => `- [[${d.replace(/\.md$/, "")}]]`),
        "",
        "The encounter is retained unpromoted \u2014 the triad recurring is evidence, not proof,",
        "that its pairs are meaningful on their own.",
        "",
      ].join(`
`),
      l = await Ze(this.app, s ? `${s}/${i}` : i, a);
    (this.index.recordWrite(l.path, a),
      this.model.crystallizeHyperedge(t, l.path),
      this.history.append({
        type: "crystallized",
        kind: "hyperedge",
        nodes: n.nodes,
        actor: "user",
        detail: { conceptNote: l.path },
      }),
      await this.persistHyperedge(this.model.getHyperedge(t)),
      new M.Notice(`Crystallized into ${l.basename}. The encounter is unchanged.`),
      await this.app.workspace.getLeaf(!0).openFile(l));
  }
  async dissolveHyperedge(t) {
    let n = this.model.getHyperedge(t);
    n &&
      (await this.removeHyperedgeFromNote(n),
      this.model.removeHyperedge(t),
      this.history.append({
        type: "dissolved",
        kind: "hyperedge",
        nodes: n.nodes,
        actor: "user",
        prior: { label: n.label ?? null, mode: n.mode ?? null },
      }),
      this.controller.clearFocus(),
      this.panelView?.setSelection(null),
      new M.Notice("Encounter dissolved. Its history is kept."));
  }
  async saveHyperedgeMetadata(t, n) {
    let i = this.model.updateHyperedge(t, n);
    i && (await this.persistHyperedge(i));
  }
  async promoteSimplex(t) {
    let n = this.model.getSimplex(t);
    if (!n || n.autoGenerated) return;
    this.controller.logPromote(t, n.nodes);
    let i = n.label?.trim() || `simplex-${t.replace(/\|/g, "-")}`,
      s = n.nodes.map((p) => `- [[${p.replace(/\.md$/, "")}]]`).join(`
`),
      o = await Ze(this.app, i, s),
      a = { ...n, sourcePath: o.path, userDefined: !0, inferred: !1, suggested: !1, autoGenerated: !1 };
    if (n.sourcePath && n.sourcePath !== o.path) {
      let p = this.app.vault.getAbstractFileByPath(n.sourcePath);
      if (p instanceof M.TFile) {
        let c = await ne(this.app, p, t);
        (await this.app.vault.modify(p, c), this.index.recordWrite(p.path, c));
      }
    }
    let l = await _e(this.app, o, a);
    (await this.app.vault.modify(o, l), this.index.recordWrite(o.path, l), this.model.removeSimplex(t));
    let d = this.model.addSimplex(a);
    (this.controller.selectSimplex(d),
      await this.openPanel(d, !1),
      new M.Notice(`Simplex now owned by ${o.basename}.`));
  }
  openCreateSimplexModal(t, n) {
    this.openCreateRelationModal(t, n, "simplex");
  }
  openCreateRelationModal(t, n, i) {
    let s = this.settings.persistenceMode === "central-file" ? this.settings.centralFile : n;
    new Se(
      this.app,
      t,
      s,
      async (o) => {
        if (o.kind === "hyperedge") {
          let d = await this.createHyperedge(o, n);
          if (!d) return;
          (this.controller.selectHyperedge(d),
            this.settings.commandAutoOpenPanel && (await this.openPanel({ kind: "hyperedge", key: d }, !1)),
            C.info("plugin", "Encounter created from guided modal", {
              relationKey: d,
              sourcePath: s,
              hyperedgeCount: this.model.hyperedges.size,
            }),
            new M.Notice(
              this.settings.persistenceMode === "central-file"
                ? `Encounter added to ${this.settings.centralFile}. No faces were generated.`
                : "Encounter added to note frontmatter. No faces were generated.",
            ));
          return;
        }
        let a = {
            nodes: o.nodes.map((d) => this.resolveDraftNode(d, n)),
            label: o.label,
            weight: o.weight,
            sourcePath: s,
            userDefined: !0,
            autoGenerated: !1,
          },
          l = this.model.addSimplex(a);
        (await this.persistSimplex(this.model.getSimplex(l)),
          this.history.append({ type: "created", kind: "simplex", nodes: a.nodes, actor: "user" }),
          this.controller.selectSimplex(l),
          this.settings.commandAutoOpenPanel && (await this.openPanel(l, !1)),
          C.info("plugin", "Simplex created from guided modal", {
            simplexKey: l,
            sourcePath: a.sourcePath,
            simplexCount: this.model.simplices.size,
          }),
          new M.Notice(
            this.settings.persistenceMode === "central-file"
              ? `Simplex added to ${this.settings.centralFile}.`
              : "Simplex added to note frontmatter.",
          ));
      },
      i,
    ).open();
  }
  async openPanelForCurrentSelection() {
    let t =
      this.controller.hoveredSimplexKey ??
      (this.controller.hoveredNodeId && this.model.getSimplicesForNode(this.controller.hoveredNodeId)[0]?.nodes
        ? N(this.model.getSimplicesForNode(this.controller.hoveredNodeId)[0].nodes)
        : null);
    await this.openPanel(t, !0);
  }
  async logPersistenceState() {
    (C.info("plugin", "Persistence state", {
      mode: this.settings.persistenceMode,
      centralFile: this.settings.centralFile,
    }),
      this.settings.persistenceMode === "central-file"
        ? await wt(this.app, this.settings.centralFile)
        : C.info("persistence", "Source-note persistence active", { mode: this.settings.persistenceMode }));
  }
  async persistSimplex(t) {
    if (
      t.sourcePath === this.settings.centralFile ||
      (!t.sourcePath && this.settings.persistenceMode === "central-file")
    ) {
      let { file: o, content: a } = await xt(this.app, this.settings.centralFile, {
        ...t,
        sourcePath: this.settings.centralFile,
      });
      (await this.app.vault.modify(o, a),
        this.index.recordWrite(o.path, a),
        C.info("plugin", "Persisted simplex to central file", { simplexKey: N(t.nodes), path: o.path }));
      return;
    }
    let i = this.app.vault.getAbstractFileByPath(t.sourcePath ?? "");
    if (!(i instanceof M.TFile)) {
      C.warn("plugin", "Unable to persist simplex to source note", {
        simplexKey: N(t.nodes),
        sourcePath: t.sourcePath,
      });
      return;
    }
    let s = await _e(this.app, i, t);
    (await this.app.vault.modify(i, s),
      this.index.recordWrite(i.path, s),
      C.info("plugin", "Persisted simplex to source note", { simplexKey: N(t.nodes), path: i.path }));
  }
  openCanvasContextMenu(t, n) {
    let i = new M.Menu();
    if (
      (t.nodeId &&
        (i.addItem((s) =>
          s
            .setTitle("Open note")
            .setIcon("file-text")
            .onClick(() => {
              this.openNodeNote(t.nodeId);
            }),
        ),
        i.addItem((s) =>
          s
            .setTitle("Focus node")
            .setIcon("crosshair")
            .onClick(() => {
              ((this.controller.hoveredNodeId = t.nodeId), this.controller.focusHoveredNode(), this.renderer.render());
            }),
        ),
        i.addItem((s) =>
          s
            .setTitle("Create simplex from node + neighbors")
            .setIcon("plus-circle")
            .onClick(() => {
              this.createSimplexFromNode(t.nodeId);
            }),
        ),
        i.addItem((s) =>
          s
            .setTitle("Record encounter from node + neighbors")
            .setIcon("diamond")
            .onClick(() => {
              this.createEncounterFromNode(t.nodeId);
            }),
        ),
        i.addItem((s) =>
          s
            .setTitle(this.model.nodes.get(t.nodeId)?.isPinned ? "Unpin node" : "Pin node")
            .setIcon("pin")
            .onClick(() => {
              (this.controller.togglePin(t.nodeId), this.renderer.render());
            }),
        )),
      t.simplexKey &&
        (i.addItem((s) =>
          s
            .setTitle("Open metadata")
            .setIcon("info")
            .onClick(() => {
              this.openPanel(t.simplexKey, !0);
            }),
        ),
        i.addItem((s) =>
          s
            .setTitle("Promote to note")
            .setIcon("up-right-from-square")
            .onClick(() => {
              this.promoteSimplex(t.simplexKey);
            }),
        ),
        i.addItem((s) =>
          s
            .setTitle("Dissolve simplex")
            .setIcon("trash")
            .onClick(() => {
              this.dissolveSimplex(t.simplexKey);
            }),
        ),
        i.addItem((s) =>
          s
            .setTitle("Relax to encounter")
            .setIcon("diamond")
            .onClick(() => {
              this.relaxSimplex(t.simplexKey);
            }),
        ),
        i.addItem((s) =>
          s
            .setTitle("Show in formal view")
            .setIcon("sigma")
            .onClick(async () => {
              ((this.settings.formalMode = !0),
                await this.saveSettings(),
                this.controller.selectSimplex(t.simplexKey),
                this.renderer.render());
            }),
        )),
      t.hyperedgeKey)
    ) {
      let s = this.model.getHyperedge(t.hyperedgeKey);
      (i.addItem((o) =>
        o
          .setTitle("Open encounter")
          .setIcon("info")
          .onClick(() => {
            this.openPanel({ kind: "hyperedge", key: t.hyperedgeKey }, !0);
          }),
      ),
        i.addItem((o) =>
          o
            .setTitle("Promote to simplex")
            .setIcon("triangle")
            .onClick(() => this.promoteEncounter(t.hyperedgeKey)),
        ),
        s?.persistence === "recurring" &&
          i.addItem((o) =>
            o
              .setTitle("Crystallize concept")
              .setIcon("sparkles")
              .onClick(() => {
                this.crystallizeEncounter(t.hyperedgeKey);
              }),
          ),
        i.addItem((o) =>
          o
            .setTitle("Dissolve encounter")
            .setIcon("trash")
            .onClick(() => {
              this.dissolveHyperedge(t.hyperedgeKey);
            }),
        ));
    }
    i.showAtMouseEvent(n);
  }
  async openNodeNote(t) {
    let n = this.app.vault.getAbstractFileByPath(t),
      i = n instanceof M.TFile ? n : re(t, t, this.app);
    if (!(i instanceof M.TFile)) {
      new M.Notice("This node is not backed by a note yet.");
      return;
    }
    await this.app.workspace.getLeaf(!0).openFile(i);
  }
  createSimplexFromNode(t) {
    let n = this.model.getNeighbors(t),
      i = [t, ...n].slice(0, Math.max(2, this.settings.commandSimplexSize));
    if (i.length < 2) {
      new M.Notice("Need at least one connected neighbor to form a simplex.");
      return;
    }
    this.openCreateSimplexModal(i, t);
  }
  createEncounterFromNode(t) {
    let n = [t, ...this.model.getNeighbors(t)];
    if (n.length < 2) {
      new M.Notice("Need at least one connected neighbor to record an encounter.");
      return;
    }
    this.openCreateRelationModal(n, t, "hyperedge");
  }
  async dissolveSimplex(t) {
    let n = this.model.getSimplex(t);
    if (!n || n.autoGenerated) return;
    if (
      (this.controller.logDissolve(t, n.nodes),
      n.sourcePath === this.settings.centralFile || (!n.sourcePath && this.settings.persistenceMode === "central-file"))
    ) {
      let s = await B(this.app, this.settings.centralFile),
        o = await ne(this.app, s, t);
      (await this.app.vault.modify(s, o), this.index.recordWrite(s.path, o));
    } else {
      let s = n.sourcePath ?? "",
        o = this.app.vault.getAbstractFileByPath(s);
      if (!(o instanceof M.TFile)) return;
      let a = await ne(this.app, o, t);
      (await this.app.vault.modify(o, a), this.index.recordWrite(o.path, a));
    }
    (this.model.removeSimplex(t),
      this.controller.clearFocus(),
      this.panelView?.setSelection(null),
      C.info("plugin", "Dissolved simplex", { simplexKey: t, persistenceMode: this.settings.persistenceMode }));
  }
  async openPanel(t, n) {
    let i = this.app.workspace.getRightLeaf(!1);
    if (!i) return;
    let s = typeof t == "string" ? { kind: "simplex", key: t } : t;
    (await i.setViewState({ type: q, active: n }),
      this.panelView?.setSelection(s),
      C.info("plugin", "Opened metadata panel", { kind: s?.kind ?? null, relationKey: s?.key ?? null, active: n }));
  }
  resolveDraftNode(t, n) {
    return this.app.metadataCache.getFirstLinkpathDest(t, n)?.path ?? t.trim();
  }
  scheduleFullScan(t, n) {
    (this.rescanTimer !== null && window.clearTimeout(this.rescanTimer),
      (this.rescanTimer = window.setTimeout(async () => {
        ((this.rescanTimer = null),
          C.info("plugin", "Running full scan", { reason: t }),
          await this.index.fullScan(),
          this.syncEncounterState(),
          this.rebuildSubsetScorer(),
          this.renderer.render(),
          C.info("plugin", "Full scan complete", {
            reason: t,
            indexedNodeCount: this.model.nodes.size,
            simplexCount: this.model.simplices.size,
          }));
      }, n)));
  }
};
